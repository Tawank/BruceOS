FROM espressif/idf:v6.0.2

# Host tools used by the board build wrapper.
RUN apt-get update \
 && apt-get install -y --no-install-recommends \
    git \
    bash \
    ca-certificates \
 && rm -rf /var/lib/apt/lists/*

WORKDIR /work
