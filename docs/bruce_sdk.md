# BruceOS Core SDK Reference

## Modules

| Module | Description |
|---|---|
| [`app_config.h`](#app_configh) | App settings. |
| [`app_runner.h`](#app_runnerh) | Runs apps and built-in commands. |
| [`archive.h`](#archiveh) | ".tar.gz" and ".zip" archive create/list/extract. |
| [`args.h`](#argsh) | Command-line argument parser (argparse-style). |
| [`audio.h`](#audioh) | Tone and PCM audio playback. |
| [`base64.h`](#base64h) | Base64 encoding (RFC 4648). |
| [`bluetooth.h`](#bluetoothh) | BLE advertisement scanning. |
| [`bluetooth_hid.h`](#bluetooth_hidh) | Bluetooth keyboard and gamepad connections. |
| [`clipboard.h`](#clipboardh) | Shared copy/paste clipboard. |
| [`clock.h`](#clockh) | Clock, time zone, and NTP settings. |
| [`compress.h`](#compressh) | DEFLATE compression/decompression (zlib-backed). |
| [`config.h`](#configh) | Global Bruce Config API. |
| [`device.h`](#deviceh) | Battery, power, and device sensors. |
| [`dialog.h`](#dialogh) | Messages, menus, input dialogs, and text viewers. |
| [`disk.h`](#diskh) | Disk and partition access. |
| [`display.h`](#displayh) | Screen drawing and display settings. |
| [`environment.h`](#environmenth) | Environment variables. |
| [`ext_mem_loader.h`](#ext_mem_loaderh) | Loader to external memory. |
| [`filetype.h`](#filetypeh) | Identifies what a file is: description, MIME type, icon, and which app_runner program opens it. |
| [`format.h`](#formath) | Formatting helpers. |
| [`gpio.h`](#gpioh) | GPIO pin access. |
| [`hash.h`](#hashh) | Cryptographic and checksum digests. |
| [`html.h`](#htmlh) | HTML parser. |
| [`http.h`](#httph) | HTTP client. |
| [`http_server.h`](#http_serverh) | HTTP server. |
| [`i2c.h`](#i2ch) | I2C bus access. |
| [`icon.h`](#iconh) | Built-in icons. |
| [`image.h`](#imageh) | JPEG, PNG and GIF decoding, resizing, and drawing. |
| [`input.h`](#inputh) | Keyboard, button, touch, and encoder event queue. |
| [`ir.h`](#irh) | Infrared sending and receiving. |
| [`manifest.h`](#manifesth) | App manifest parsing. |
| [`memory.h`](#memoryh) | Memory allocation (RAM, PSRAM, swap). |
| [`notification.h`](#notificationh) | On-screen notifications. |
| [`nrf24.h`](#nrf24h) | nRF24 radio control and scanning. |
| [`partition_manager.h`](#partition_managerh) | Manages the "user area" partitions. |
| [`permission.h`](#permissionh) | App permissions. |
| [`process.h`](#processh) | Running app and process control. |
| [`pubsub.h`](#pubsubh) | Named message topics. |
| [`result.h`](#resulth) | Common Core SDK result codes. |
| [`runtime.h`](#runtimeh) | Time, delays, timers, and isGUI request. |
| [`spi.h`](#spih) | SPI device access. |
| [`ssh.h`](#sshh) | SSH connections and remote shells. |
| [`status_icon.h`](#status_iconh) | Status bar icons. |
| [`stdio.h`](#stdioh) | Standard input and output. |
| [`storage.h`](#storageh) | File and directory access. |
| [`tcp.h`](#tcph) | TCP network connections. |
| [`tty.h`](#ttyh) | Terminal size and mode settings. |
| [`wifi.h`](#wifih) | Wi-Fi connections, scanning, and status. |

Generated by `tools/gen_sdk_docs.py` from the Doxygen-style comments in `src/core_sdk/*.h`. Do not edit this file by hand -- edit the header comments and re-run the script.

Every core_sdk function documents the [`bruce_permission_t`](../src/core_sdk/permission.h) permission(s) it checks, where the header comments state one. `none` means the header explicitly says the function is not permission-gated; `built-in only` means external ELF/JS/WASM apps always get `BRUCE_ERR_PERMISSION` regardless of granted permissions. No Permissions section at all means the header comments don't state a permission requirement either way.

## Permissions Reference

Functions that explicitly document a `bruce_permission_t` check, grouped by permission name (see `src/core_sdk/permission.h`).

### `bt`

- `bluetooth_hid__connected_device` (bluetooth_hid.h)

### `config`

- `clock__set_local` (clock.h)
- `clock__sync_ntp` (clock.h)
- `config__get_color_primary` (config.h)
- `config__set_color_primary` (config.h)
- `config__set_color_secondary` (config.h)
- `config__set_color_background` (config.h)
- `config__set_color_surface` (config.h)
- `config__set_color_text` (config.h)
- `config__set_color_text_muted` (config.h)
- `config__set_color_border` (config.h)
- `config__set_color_success` (config.h)
- `config__set_color_warning` (config.h)
- `config__set_color_error` (config.h)
- `config__set_colors` (config.h)
- `config__get_display_buffered_rendering` (config.h)
- `config__set_display_buffered_rendering` (config.h)
- `config__get_display_rotation` (config.h)
- `config__set_display_rotation` (config.h)
- `config__get_display_dma_framebuffer` (config.h)
- `config__set_display_dma_framebuffer` (config.h)
- `config__get_launcher` (config.h)
- `config__set_launcher` (config.h)
- `config__set_display_dim_timeout` (config.h)
- `config__set_display_brightness` (config.h)
- `config__set_time_automatic_update_via_ntp` (config.h)
- `config__set_time_timezone` (config.h)
- `config__set_time_dst` (config.h)
- `config__set_time_clock24hr` (config.h)
- `config__set_sound_enabled` (config.h)
- `config__set_sound_volume` (config.h)
- `config__set_keyboard_lang` (config.h)
- `config__set_hotkeys` (config.h)
- `config__set_led_enabled` (config.h)
- `config__set_led_brightness` (config.h)
- `config__set_led_color` (config.h)
- `config__set_led_blink_enabled` (config.h)
- `config__set_led_effect` (config.h)
- `config__set_led_effect_speed` (config.h)
- `config__set_led_effect_direction` (config.h)
- `config__get_startup_apps` (config.h)
- `config__set_startup_apps` (config.h)
- `config__add_startup_app` (config.h)
- `config__remove_startup_app` (config.h)
- `config__get_dev_mode` (config.h)
- `config__set_dev_mode` (config.h)

### `execute`

- `app_runner__run` (app_runner.h)
- `app_runner__run_with_environment` (app_runner.h)
- `app_runner__run_path` (ext_mem_loader.h)
- `app_runner__run_path_with_environment` (ext_mem_loader.h)

### `gpio`

- `gpio__configure` (gpio.h)
- `gpio__read` (gpio.h)
- `gpio__write` (gpio.h)
- `i2c__open` (i2c.h)
- `i2c__probe` (i2c.h)
- `i2c__write` (i2c.h)
- `i2c__read` (i2c.h)
- `i2c__write_read` (i2c.h)
- `i2c__close` (i2c.h)
- `spi__open` (spi.h)
- `spi__transfer` (spi.h)
- `spi__close` (spi.h)

### `http`

- `http__request` (http.h)
- `http_server__start` (http_server.h)
- `http_server__stop` (http_server.h)
- `http_server__get_status` (http_server.h)

### `input`

- `input__inject` (input.h)

### `ir`

- `ir__transmit_raw` (ir.h)
- `ir__transmit` (ir.h)
- `ir__transmit_parsed` (ir.h)
- `ir__receive` (ir.h)
- `ir__transmit_file` (ir.h)
- `ir__transmit_record` (ir.h)

### `process`

- `device__restart` (device.h)
- `device__power_off` (device.h)
- `memory__get_layout` (memory.h)
- `process__switch_next` (process.h)
- `process__switch_previous` (process.h)
- `process__foreground` (process.h)
- `process__signal` (process.h)
- `process__terminate` (process.h)
- `process__pause` (process.h)
- `process__resume` (process.h)
- `process__kill` (process.h)
- `process__wait_status` (process.h)

### `rf`

- `nrf24__probe` (nrf24.h)
- `nrf24__set_channel` (nrf24.h)
- `nrf24__get_channel` (nrf24.h)
- `nrf24__scan` (nrf24.h)

### `ssh`

- `ssh__connect` (ssh.h)
- `ssh__host_key_sha256` (ssh.h)
- `ssh__verify_host_key_sha256` (ssh.h)
- `ssh__authenticate_password` (ssh.h)
- `ssh__generate_keypair_ex` (ssh.h)
- `ssh__authenticate_key` (ssh.h)
- `ssh__open_shell` (ssh.h)
- `ssh__resize_pty` (ssh.h)
- `ssh__read` (ssh.h)
- `ssh__write` (ssh.h)
- `ssh__close` (ssh.h)

### `storage`

- `archive__tar_gz_create` (archive.h)
- `archive__tar_gz_list` (archive.h)
- `archive__tar_gz_extract` (archive.h)
- `archive__zip_create` (archive.h)
- `archive__zip_list` (archive.h)
- `archive__zip_extract` (archive.h)
- `clipboard__paste_files` (clipboard.h)
- `clipboard__paste_file_as` (clipboard.h)
- `clipboard__paste_binary` (clipboard.h)
- `filetype__identify` (filetype.h)
- `ir__transmit_file` (ir.h)
- `storage__exists` (storage.h)
- `storage__open` (storage.h)
- `storage__list` (storage.h)

### `wifi`

- `clock__sync_ntp` (clock.h)
- `tcp__connect` (tcp.h)
- `tcp__listen` (tcp.h)
- `tcp__accept` (tcp.h)
- `tcp__read` (tcp.h)
- `tcp__write` (tcp.h)
- `tcp__close` (tcp.h)

## Header Reference

# `app_config.h`

**App settings.**

Separate from the shared /config/bruce.conf singleton (core_sdk/config.h).
Each app_name gets its own JSON document at /config/<app_name>.conf,
addressed with a dot-separated json_path (e.g. "auth.user" reaches
{"auth":{"user":...}}). Intermediate objects are created automatically
when setting a value.

app_name and every path segment must start with a letter or underscore and
contain only letters, digits, or underscores. Getters fall back to the
supplied default when the app has no config file, the path is absent, or
the stored value is a different type; setters create the file and any
missing intermediate objects on demand.

**Constants**

| Name | Value |
|---|---|
| `BRUCE_APP_CONFIG_NAME_MAX_LEN` | `31` |
| `BRUCE_APP_CONFIG_STRING_MAX_LEN` | `128` |

---

## app_config__get_bool()

```c
bool app_config__get_bool(const char *app_name, const char *json_path, bool default_value);
```

Reads a boolean value, falling back to default_value.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `app_name` | `const char *` | Name of the app whose config file to read. |
| `json_path` | `const char *` | Dot-separated path to the value within the app's config document. |
| `default_value` | `bool` | Value to return if the path is absent or not a bool. |

### Returns

`bool`


---

## app_config__set_bool()

```c
bruce_result_t app_config__set_bool(const char *app_name, const char *json_path, bool value);
```

Sets a boolean value, creating the file/path if needed.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `app_name` | `const char *` | Name of the app whose config file to write. |
| `json_path` | `const char *` | Dot-separated path to the value within the app's config document. |
| `value` | `bool` | New boolean value to store. |

### Returns

`bruce_result_t`


---

## app_config__get_int()

```c
int app_config__get_int(const char *app_name, const char *json_path, int default_value);
```

Reads an integer value, falling back to default_value.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `app_name` | `const char *` | Name of the app whose config file to read. |
| `json_path` | `const char *` | Dot-separated path to the value within the app's config document. |
| `default_value` | `int` | Value to return if the path is absent or not an int. |

### Returns

`int`


---

## app_config__set_int()

```c
bruce_result_t app_config__set_int(const char *app_name, const char *json_path, int value);
```

Sets an integer value, creating the file/path if needed.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `app_name` | `const char *` | Name of the app whose config file to write. |
| `json_path` | `const char *` | Dot-separated path to the value within the app's config document. |
| `value` | `int` | New integer value to store. |

### Returns

`bruce_result_t`


---

## app_config__get_string()

```c
bool app_config__get_string(
    const char *app_name, const char *json_path, const char *default_value, char *out_value, size_t capacity
);
```

Copies the stored string into out_value.

Always NUL-terminated within capacity, or default_value if the path is
absent/not a string. Returns whether a stored value was found.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `app_name` | `const char *` | Name of the app whose config file to read. |
| `json_path` | `const char *` | Dot-separated path to the value within the app's config document. |
| `default_value` | `const char *` | String copied into out_value when the path is absent or not a string. |
| `out_value` | `char *` | Buffer to receive the NUL-terminated string. |
| `capacity` | `size_t` | Size of out_value in bytes. |

### Returns

`bool`


---

## app_config__set_string()

```c
bruce_result_t app_config__set_string(const char *app_name, const char *json_path, const char *value);
```

Sets a string value, creating the file/path if needed.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `app_name` | `const char *` | Name of the app whose config file to write. |
| `json_path` | `const char *` | Dot-separated path to the value within the app's config document. |
| `value` | `const char *` | New string value to store. |

### Returns

`bruce_result_t`


---

## app_config__get_json()

```c
bool app_config__get_json(
    const char *app_name, const char *json_path, const char *default_json, char *out_json, size_t capacity
);
```

Serializes the stored JSON value at json_path into out_json.

Falls back to default_json when the path is absent. This is intended for
structured per-app values such as arrays of objects while keeping the
public app_config API small. Returns whether a stored value was found.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `app_name` | `const char *` | Name of the app whose config file to read. |
| `json_path` | `const char *` | Dot-separated path to the value within the app's config document. |
| `default_json` | `const char *` | JSON text copied into out_json when the path is absent. |
| `out_json` | `char *` | Buffer to receive the serialized JSON text. |
| `capacity` | `size_t` | Size of out_json in bytes. |

### Returns

`bool`


---

## app_config__set_json()

```c
bruce_result_t app_config__set_json(const char *app_name, const char *json_path, const char *value_json);
```

Sets a raw JSON value, creating the file/path if needed.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `app_name` | `const char *` | Name of the app whose config file to write. |
| `json_path` | `const char *` | Dot-separated path to the value within the app's config document. |
| `value_json` | `const char *` | JSON text to store at json_path. |

### Returns

`bruce_result_t`


---

## app_config__get_bool_array()

```c
size_t
app_config__get_bool_array(const char *app_name, const char *json_path, bool *out_values, size_t capacity);
```

Fills out_values with up to `capacity` bool entries from an array value.

Returns the number of entries filled.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `app_name` | `const char *` | Name of the app whose config file to read. |
| `json_path` | `const char *` | Dot-separated path to the array value. |
| `out_values` | `bool *` | Array to receive the stored bool entries. |
| `capacity` | `size_t` | Number of entries out_values can hold. |

### Returns

`size_t`


---

## app_config__set_bool_array()

```c
bruce_result_t
app_config__set_bool_array(const char *app_name, const char *json_path, const bool *values, size_t count);
```

Sets a bool array value, creating the file/path if needed.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `app_name` | `const char *` | Name of the app whose config file to write. |
| `json_path` | `const char *` | Dot-separated path to the array value. |
| `values` | `const bool *` | Bool entries to store. |
| `count` | `size_t` | Number of entries in values. |

### Returns

`bruce_result_t`


---

## app_config__get_int_array()

```c
size_t
app_config__get_int_array(const char *app_name, const char *json_path, int *out_values, size_t capacity);
```

Fills out_values with up to `capacity` int entries from an array value.

Returns the number of entries filled.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `app_name` | `const char *` | Name of the app whose config file to read. |
| `json_path` | `const char *` | Dot-separated path to the array value. |
| `out_values` | `int *` | Array to receive the stored int entries. |
| `capacity` | `size_t` | Number of entries out_values can hold. |

### Returns

`size_t`


---

## app_config__set_int_array()

```c
bruce_result_t
app_config__set_int_array(const char *app_name, const char *json_path, const int *values, size_t count);
```

Sets an int array value, creating the file/path if needed.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `app_name` | `const char *` | Name of the app whose config file to write. |
| `json_path` | `const char *` | Dot-separated path to the array value. |
| `values` | `const int *` | Int entries to store. |
| `count` | `size_t` | Number of entries in values. |

### Returns

`bruce_result_t`


---

## app_config__get_string_array()

```c
size_t app_config__get_string_array(
    const char *app_name, const char *json_path, char *const *out_values, size_t value_size, size_t capacity
);
```

Fills out_values with up to `capacity` string entries.

out_values[i] must point at a writable buffer of at least value_size
bytes. Returns the number of entries filled.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `app_name` | `const char *` | Name of the app whose config file to read. |
| `json_path` | `const char *` | Dot-separated path to the array value. |
| `out_values` | `char *const *` | Array of caller-owned buffers to receive the strings. |
| `value_size` | `size_t` | Size in bytes of each buffer pointed to by out_values. |
| `capacity` | `size_t` | Number of entries out_values can hold. |

### Returns

`size_t`


---

## app_config__set_string_array()

```c
bruce_result_t app_config__set_string_array(
    const char *app_name, const char *json_path, const char *const *values, size_t count
);
```

Sets a string array value, creating the file/path if needed.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `app_name` | `const char *` | Name of the app whose config file to write. |
| `json_path` | `const char *` | Dot-separated path to the array value. |
| `values` | `const char *const *` | String entries to store. |
| `count` | `size_t` | Number of entries in values. |

### Returns

`bruce_result_t`


---

## app_config__remove()

```c
bruce_result_t app_config__remove(const char *app_name, const char *json_path);
```

Removes the value at json_path.

BRUCE_ERR_NOT_FOUND if it was not set.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `app_name` | `const char *` | Name of the app whose config file to modify. |
| `json_path` | `const char *` | Dot-separated path to the value to remove. |

### Returns

`bruce_result_t`


---

# `app_runner.h`

**Runs apps and built-in commands.**

---

## bruce_app_entry_t()

```c
typedef int (*bruce_app_entry_t)(int argc, char **argv);
```


---

## bruce_launch_mode_t()

```c
typedef enum {
    BRUCE_LAUNCH_FOREGROUND = 0,
    BRUCE_LAUNCH_BACKGROUND = 1,
} bruce_launch_mode_t;
```


---

## app_runner__register()

```c
bruce_result_t app_runner__register(
    const char *name, const char *description, const char *category, bruce_app_entry_t entry,
    uint32_t stack_bytes
);
```

Registers a built-in command.

Name and description must be non-empty and remain valid for the lifetime
of the system. `category` groups commands for docs and command search
(`man`, launcher, shell completion) - it has no effect on lookup or
execution; pass one of the short, title-case names used in
app_runner__register_defaults() (e.g. "System", "Storage", "Network"), or
NULL/"" if the command has none (excluded from category listings).
Returns BRUCE_ERR_ALREADY_EXISTS for a duplicate name and
BRUCE_ERR_RESOURCE_LIMIT if the registry is full.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `name` | `const char *` | Command name used to invoke it; must be unique. |
| `description` | `const char *` | One-line summary shown in command listings/`man`. |
| `category` | `const char *` | Grouping label for docs/search, or NULL/"" for none. |
| `entry` | `bruce_app_entry_t` | Entry point called with conventional C argc/argv. |
| `stack_bytes` | `uint32_t` | Stack size to give the process running this command. |

### Returns

`bruce_result_t`


---

## app_runner__command_count()

```c
size_t app_runner__command_count(void);
```

Returns the number of registered built-in commands.

Entries are returned in registration order and remain owned by AppRunner.
Returns NULL when `index` is out of range.

### Returns

`size_t`


---

## app_runner__command_name()

```c
const char *app_runner__command_name(size_t index);
```

Name of the built-in command at `index`.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `index` | `size_t` | Zero-based index into the command registry. |

### Returns

`const char *`


---

## app_runner__command_description()

```c
const char *app_runner__command_description(size_t index);
```

Description of the built-in command at `index`.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `index` | `size_t` | Zero-based index into the command registry. |

### Returns

`const char *`


---

## app_runner__command_category()

```c
const char *app_runner__command_category(size_t index);
```

Category of the built-in command at `index`.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `index` | `size_t` | Zero-based index into the command registry. |

### Returns

`const char *`


---

## app_runner__run()

```c
int app_runner__run(const char *app_name, const char *arg, bruce_launch_mode_t mode);
```

Starts a named built-in or loader-registered application.

See core_sdk/ext_mem_loader.h. On success this returns a positive
bruce_process_id_t. On failure it returns a negative BRUCE_ERR_* value
(including BRUCE_ERR_NOT_FOUND and BRUCE_ERR_BUSY). `arg` is shell-style
text. Registered built-ins use conventional C arguments: argv[0] is
`app_name`, argv[argc] is NULL, and NULL or an empty `arg` therefore
creates argc == 1. Loader-resolved applications define their own argv[0]
convention.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `app_name` | `const char *` | Name of the built-in or loader-registered app to start. |
| `arg` | `const char *` | Shell-style argument text, or NULL/"" for none. |
| `mode` | `bruce_launch_mode_t` | Whether to launch in the foreground or background. |

### Returns

`int`

#### Permissions

- `execute`


---

## app_runner__run_with_environment()

```c
int app_runner__run_with_environment(
    const char *app_name, const char *arg, bruce_launch_mode_t mode,
    const bruce_environment_variable_t *environment, size_t environment_count
);
```

Starts an app with temporary environment variables.

The overlay is deep-copied before this function returns.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `app_name` | `const char *` | Name of the built-in or loader-registered app to start. |
| `arg` | `const char *` | Shell-style argument text, or NULL/"" for none. |
| `mode` | `bruce_launch_mode_t` | Whether to launch in the foreground or background. |
| `environment` | `const bruce_environment_variable_t *` | Extra environment variables to give the child. |
| `environment_count` | `size_t` | Number of entries in environment. |

### Returns

`int`

#### Permissions

- `execute`


---

## app_runner__run_command()

```c
bruce_result_t app_runner__run_command(const char *command_line, bruce_launch_mode_t default_mode);
```

Runs a command line, including environment variable assignments.

An explicit BG=0 or BG=1 selects foreground/background; otherwise
default_mode is used. GUI=1 requests a GUI process. OVERLAY=1 preserves
the visible framebuffer for an overlay-only GUI process. These
assignments are still included in the child's environment.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `command_line` | `const char *` | Full command line, e.g. "app_name arg1 arg2". |
| `default_mode` | `bruce_launch_mode_t` | Launch mode used when no explicit BG= assignment is present. |

### Returns

`bruce_result_t`


---

## app_runner__parse_args()

```c
bruce_result_t app_runner__parse_args(const char *arg, char ***out_argv, int *out_argc);
```

Splits a command line into arguments.

Ensures quoting/escaping rules are identical everywhere: splits on runs of
spaces/tabs; supports single quotes (fully literal), double quotes
(backslash escapes only `\"` and `\\`), and a bare backslash outside
quotes to escape the next character. NULL or an empty `arg` produces
argc == 0 with *out_argv left NULL. Returns BRUCE_ERR_INVALID_ARGUMENT for
an unterminated quote or a trailing unescaped backslash, and
BRUCE_ERR_NO_MEMORY on allocation failure. The caller must free a
successful result with app_runner__free_args().

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `arg` | `const char *` | Shell-style argument text to tokenize. |
| `out_argv` | `char ***` | Receives a newly allocated argv array. |
| `out_argc` | `int *` | Receives the number of tokens in *out_argv. |

### Returns

`bruce_result_t`


---

## app_runner__free_args()

```c
void app_runner__free_args(char **argv, int argc);
```

Frees an argv produced by app_runner__parse_args().

Safe to call with argv == NULL (e.g. when argc == 0).

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `argv` | `char **` | Array previously returned via app_runner__parse_args()'s out_argv. |
| `argc` | `int` | Token count previously returned via app_runner__parse_args()'s out_argc. |

### Returns

`void`


---

## app_runner__icon_for_path()

```c
const char *app_runner__icon_for_path(const char *path);
```

Returns the configured icon for a file path.

"file" when no icon is configured for its extension. The returned string
is owned by Core.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `path` | `const char *` | File path whose extension selects the icon. |

### Returns

`const char *`


---

## app_runner__environment_requests_gui()

```c
bool app_runner__environment_requests_gui(
    const bruce_environment_variable_t *environment, size_t environment_count
);
```

Checks whether an environment requests a graphical interface.

Not yet applied to any process. Returns true iff the last matching
entry's value is "1". Shared by app_runner__run()'s built-in path and by
loader modules, which must determine this for the process context they
are about to spawn before it exists (see migration_plan.md, "Dialog and
process interaction").

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `environment` | `const bruce_environment_variable_t *` | Environment overlay to scan. |
| `environment_count` | `size_t` | Number of entries in environment. |

### Returns

`bool`


---

# `archive.h`

**".tar.gz" and ".zip" archive create/list/extract.**

".tar.gz": core_sdk/compress.h (the gzip layer) plus a vendored
tar-format reader/writer (components/microtar). ".zip": a vendored
zip container reader/writer (components/minizip) that talks to zlib
directly for its own per-entry deflate, the same way libpng does - it
doesn't go through core_sdk/compress.h. Both are wired to Bruce's own
storage__ calls instead of stdio - see core/archive/archive.c (tar.gz)
and core/archive/archive_zip.c (zip) for the I/O shims. Used by the
`tar`/`zip`/`archive`/`archive-extract` bnu commands and filemanager's
"Extract here" action.

Creating a ".tar.gz" is a single streaming pass (tar formatting -> gzip
compression -> storage__write), never holding more than one I/O chunk in
memory regardless of archive size. Creating/reading a ".zip" streams
each entry independently the same way, straight against storage__ -
a zip's central directory supports real random access (see
archive__zip_create()'s doc comment), so unlike ".tar.gz" nothing extra
is needed to read one back.

Listing/extracting a ".tar.gz" is the one exception: gzip decompression
is inherently forward-only (there's no seeking backward in a deflate
stream without re-decompressing from the start), but the tar reader
needs to seek between entries. So both first fully decompress the
archive to a same-directory scratch ".tar" file, read/extract from that
with ordinary seeks, then delete the scratch file - meaning free storage
space at least equal to the decompressed archive size is needed
temporarily, on top of the archive itself. Fine for the
sprite/config/small-archive sizes this targets; worth knowing before
pointing this at something huge.

**Constants**

| Name | Value |
|---|---|
| `BRUCE_ARCHIVE_ENTRY_NAME_MAX` | `100` |

---

## bruce_archive_entry_t()

```c
typedef struct {
    /* Entry's path within the archive, e.g. "photos/cat.jpg". Always
     * forward-slash separated regardless of host path conventions. */
    char name[BRUCE_ARCHIVE_ENTRY_NAME_MAX];
    /* Uncompressed size in bytes; 0 for directories. */
    size_t size;
    bool is_directory;
} bruce_archive_entry_t;
```


---

## bruce_archive_list_fn()

```c
typedef bool (*bruce_archive_list_fn)(void *context, const bruce_archive_entry_t *entry);
```

Called once per archive entry by archive__tar_gz_list().

@return false to stop listing early (e.g. caller found what it needed); true to continue.


---

## archive__tar_gz_create()

```c
bruce_result_t
archive__tar_gz_create(const char *archive_path, const char *const *entry_paths, size_t entry_count, int level);
```

Creates a new ".tar.gz" archive from a set of existing paths.

Each of `entry_paths` is added under its own basename; a directory is
added recursively (its own name becomes an archive-relative root, e.g.
adding "/sd/photos" produces entries named "photos/", "photos/a.jpg", ...).
`archive_path` is truncated/created fresh - any existing file there is
overwritten only once the whole archive has been built successfully
(nothing already at `archive_path` is touched if this fails partway).

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `archive_path` | `const char *` | Destination ".tar.gz" path. |
| `entry_paths` | `const char *const *` | Existing file/directory paths to add. |
| `entry_count` | `size_t` | Number of paths in entry_paths. |
| `level` | `int` | BRUCE_COMPRESS_LEVEL_DEFAULT, or 0-9 (see core_sdk/compress.h). |

### Returns

`bruce_result_t`

#### Permissions

- `storage`


---

## archive__tar_gz_list()

```c
bruce_result_t archive__tar_gz_list(const char *archive_path, bruce_archive_list_fn callback, void *context);
```

Lists a ".tar.gz" archive's entries without extracting them.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `archive_path` | `const char *` | Archive to list. |
| `callback` | `bruce_archive_list_fn` | Called once per entry, in archive order. |
| `context` | `void *` | Passed through to every callback call unchanged. |

### Returns

`bruce_result_t`

#### Permissions

- `storage`


---

## archive__tar_gz_extract()

```c
bruce_result_t archive__tar_gz_extract(const char *archive_path, const char *dest_dir);
```

Extracts every entry of a ".tar.gz" archive under `dest_dir`.

`dest_dir` must already exist. Entry names are validated before anything
is written: an absolute name or one containing a ".." component is
rejected (BRUCE_ERR_INVALID_PATH) rather than allowed to write outside
`dest_dir` ("tar-slip"). Missing intermediate directories under
`dest_dir` are created as needed.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `archive_path` | `const char *` | Archive to extract. |
| `dest_dir` | `const char *` | Existing directory to extract into. |

### Returns

`bruce_result_t`

#### Permissions

- `storage`


---

## archive__zip_create()

```c
bruce_result_t
archive__zip_create(const char *archive_path, const char *const *entry_paths, size_t entry_count, int level);
```

Creates a new ".zip" archive from a set of existing paths.

Same entry-naming/recursion rules as archive__tar_gz_create(). Unlike
".tar.gz", a ".zip"'s entries are compressed individually and its
central directory supports real random access, so - unlike
archive__tar_gz_list()/_extract() - no scratch decompression step is
needed for reading one back; the underlying storage__seek() is used
directly.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `archive_path` | `const char *` | Destination ".zip" path. |
| `entry_paths` | `const char *const *` | Existing file/directory paths to add. |
| `entry_count` | `size_t` | Number of paths in entry_paths. |
| `level` | `int` | BRUCE_COMPRESS_LEVEL_DEFAULT, or 0-9 (see core_sdk/compress.h). |

### Returns

`bruce_result_t`

#### Permissions

- `storage`


---

## archive__zip_list()

```c
bruce_result_t archive__zip_list(const char *archive_path, bruce_archive_list_fn callback, void *context);
```

Lists a ".zip" archive's entries without extracting them.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `archive_path` | `const char *` | Archive to list. |
| `callback` | `bruce_archive_list_fn` | Called once per entry, in archive order. |
| `context` | `void *` | Passed through to every callback call unchanged. |

### Returns

`bruce_result_t`

#### Permissions

- `storage`


---

## archive__zip_extract()

```c
bruce_result_t archive__zip_extract(const char *archive_path, const char *dest_dir);
```

Extracts every entry of a ".zip" archive under `dest_dir`.

Same "dest_dir must exist"/"zip-slip"-rejection/intermediate-directory
rules as archive__tar_gz_extract().

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `archive_path` | `const char *` | Archive to extract. |
| `dest_dir` | `const char *` | Existing directory to extract into. |

### Returns

`bruce_result_t`

#### Permissions

- `storage`


---

# `args.h`

**Command-line argument parser (argparse-style).**

Generic option/flag/positional-argument and subcommand parser, independent
of the rest of core_sdk. Not permission-gated.
Docs: https://www.dmulholl.com/docs/args/master/

---

## ArgParser()

```c
typedef struct ArgParser ArgParser;
```


---

## ap_callback_t()

```c
typedef int (*ap_callback_t)(char *cmd_name, ArgParser *cmd_parser);
```


---

## ap_status_t()

```c
typedef enum {
    AP_STATUS_OK = 0,
    AP_STATUS_HELP,
    AP_STATUS_VERSION,
    AP_STATUS_INVALID_ARGUMENT,
    AP_STATUS_NO_MEMORY,
} ap_status_t;
```


---

## ap_new_parser()

```c
ArgParser *ap_new_parser(void);
```

Creates an empty argument parser.

### Returns

`ArgParser *`


---

## ap_free()

```c
void ap_free(ArgParser *parser);
```

Destroys an argument parser.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to destroy. |

### Returns

`void`


---

## ap_set_helptext()

```c
void ap_set_helptext(ArgParser *parser, const char *helptext);
```

Sets the program description shown in help output.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to configure. |
| `helptext` | `const char *` | Program description to display. |

### Returns

`void`


---

## ap_get_helptext()

```c
char *ap_get_helptext(ArgParser *parser);
```

Returns the program description shown in help output.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to query. |

### Returns

`char *`


---

## ap_set_version()

```c
void ap_set_version(ArgParser *parser, const char *version);
```

Sets the version text shown by the version option.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to configure. |
| `version` | `const char *` | Version text to display. |

### Returns

`void`


---

## ap_get_version()

```c
char *ap_get_version(ArgParser *parser);
```

Returns the configured version text.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to query. |

### Returns

`char *`


---

## ap_parse()

```c
bool ap_parse(ArgParser *parser, int argc, char **argv);
```

Parses conventional main()-style arguments without modifying argv.

Parse errors, help, and version requests never exit the calling process.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to run. |
| `argc` | `int` | Argument count, as passed to main(). |
| `argv` | `char **` | Argument vector, as passed to main(); not modified. |

### Returns

`bool`


---

## ap_get_status()

```c
ap_status_t ap_get_status(ArgParser *parser);
```

Outcome of the most recent ap_parse() call.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to query. |

### Returns

`ap_status_t`


---

## ap_print_help()

```c
void ap_print_help(ArgParser *parser);
```

Prints the parser's generated help text.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to print help for. |

### Returns

`void`


---

## ap_first_pos_arg_ends_option_parsing()

```c
void ap_first_pos_arg_ends_option_parsing(ArgParser *parser);
```

Stops parsing options after the first positional argument.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to configure. |

### Returns

`void`


---

## ap_all_args_as_pos_args()

```c
void ap_all_args_as_pos_args(ArgParser *parser);
```

Treats every argument as positional.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to configure. |

### Returns

`void`


---

## ap_allow_extra_args()

```c
void ap_allow_extra_args(ArgParser *parser);
```

Accepts positional arguments beyond the declared named positionals.

They remain available through ap_get_arg_at_index()/ap_get_args().

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to configure. |

### Returns

`void`


---

## ap_unknown_options_as_args()

```c
void ap_unknown_options_as_args(ArgParser *parser);
```

Treats unregistered option-looking tokens as positional arguments.

Continues to recognize registered options.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to configure. |

### Returns

`void`


---

## ap_add_flag()

```c
void ap_add_flag(ArgParser *parser, const char *name);
```

Adds a boolean command-line flag.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to configure. |
| `name` | `const char *` | Flag name. |

### Returns

`void`


---

## ap_add_str_opt()

```c
void ap_add_str_opt(ArgParser *parser, const char *name, const char *fallback);
```

Adds a string option.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to configure. |
| `name` | `const char *` | Option name. |
| `fallback` | `const char *` | Value used when the option is omitted. |

### Returns

`void`


---

## ap_add_int_opt()

```c
void ap_add_int_opt(ArgParser *parser, const char *name, int fallback);
```

Adds an integer option.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to configure. |
| `name` | `const char *` | Option name. |
| `fallback` | `int` | Value used when the option is omitted. |

### Returns

`void`


---

## ap_add_dbl_opt()

```c
void ap_add_dbl_opt(ArgParser *parser, const char *name, double fallback);
```

Adds a decimal-number option.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to configure. |
| `name` | `const char *` | Option name. |
| `fallback` | `double` | Value used when the option is omitted. |

### Returns

`void`


---

## ap_add_greedy_str_opt()

```c
void ap_add_greedy_str_opt(ArgParser *parser, const char *name);
```

Adds a string option that accepts multiple values.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to configure. |
| `name` | `const char *` | Option name. |

### Returns

`void`


---

## ap_set_opt_help()

```c
void ap_set_opt_help(ArgParser *parser, const char *name, const char *helptext);
```

Sets the help text for an option or flag.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to configure. |
| `name` | `const char *` | Option or flag name. |
| `helptext` | `const char *` | Help text to display. |

### Returns

`void`


---

## ap_count()

```c
int ap_count(ArgParser *parser, const char *name);
```

Returns how many values were provided for an option.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to query. |
| `name` | `const char *` | Option name. |

### Returns

`int`


---

## ap_found()

```c
bool ap_found(ArgParser *parser, const char *name);
```

Checks whether an option or flag was provided.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to query. |
| `name` | `const char *` | Option or flag name. |

### Returns

`bool`


---

## ap_get_str_value()

```c
char *ap_get_str_value(ArgParser *parser, const char *name);
```

Returns the first string value for an option.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to query. |
| `name` | `const char *` | Option name. |

### Returns

`char *`


---

## ap_get_str_value_at_index()

```c
char *ap_get_str_value_at_index(ArgParser *parser, const char *name, int index);
```

Returns one string value for an option.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to query. |
| `name` | `const char *` | Option name. |
| `index` | `int` | Zero-based value index. |

### Returns

`char *`


---

## ap_get_str_values()

```c
char **ap_get_str_values(ArgParser *parser, const char *name);
```

Returns all string values for an option.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to query. |
| `name` | `const char *` | Option name. |

### Returns

`char **`


---

## ap_get_int_value()

```c
int ap_get_int_value(ArgParser *parser, const char *name);
```

Returns the first integer value for an option.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to query. |
| `name` | `const char *` | Option name. |

### Returns

`int`


---

## ap_get_int_value_at_index()

```c
int ap_get_int_value_at_index(ArgParser *parser, const char *name, int index);
```

Returns one integer value for an option.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to query. |
| `name` | `const char *` | Option name. |
| `index` | `int` | Zero-based value index. |

### Returns

`int`


---

## ap_get_int_values()

```c
int *ap_get_int_values(ArgParser *parser, const char *name);
```

Returns all integer values for an option.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to query. |
| `name` | `const char *` | Option name. |

### Returns

`int *`


---

## ap_get_dbl_value()

```c
double ap_get_dbl_value(ArgParser *parser, const char *name);
```

Returns the first decimal value for an option.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to query. |
| `name` | `const char *` | Option name. |

### Returns

`double`


---

## ap_get_dbl_value_at_index()

```c
double ap_get_dbl_value_at_index(ArgParser *parser, const char *name, int index);
```

Returns one decimal value for an option.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to query. |
| `name` | `const char *` | Option name. |
| `index` | `int` | Zero-based value index. |

### Returns

`double`


---

## ap_get_dbl_values()

```c
double *ap_get_dbl_values(ArgParser *parser, const char *name);
```

Returns all decimal values for an option.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to query. |
| `name` | `const char *` | Option name. |

### Returns

`double *`


---

## ap_add_required_arg()

```c
void ap_add_required_arg(ArgParser *parser, const char *name, const char *helptext);
```

Declares a required named positional argument.

Named positionals are declared in order; required arguments must precede
optional arguments.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to configure. |
| `name` | `const char *` | Name of the positional argument. |
| `helptext` | `const char *` | Help text shown for this argument. |

### Returns

`void`


---

## ap_add_optional_arg()

```c
void ap_add_optional_arg(ArgParser *parser, const char *name, const char *helptext);
```

Declares an optional named positional argument.

Named positionals are declared in order; required arguments must precede
optional arguments.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to configure. |
| `name` | `const char *` | Name of the positional argument. |
| `helptext` | `const char *` | Help text shown for this argument. |

### Returns

`void`


---

## ap_get_arg()

```c
char *ap_get_arg(ArgParser *parser, const char *name);
```

Reads the value of a declared named positional argument.

Returns a borrowed argv string, or NULL when an optional argument was
omitted or the name is unknown.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to query. |
| `name` | `const char *` | Name of the positional argument, as declared. |

### Returns

`char *`


---

## ap_has_args()

```c
bool ap_has_args(ArgParser *parser);
```

Checks whether any positional arguments were provided.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to query. |

### Returns

`bool`


---

## ap_count_args()

```c
int ap_count_args(ArgParser *parser);
```

Returns the number of positional arguments.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to query. |

### Returns

`int`


---

## ap_get_arg_at_index()

```c
char *ap_get_arg_at_index(ArgParser *parser, int index);
```

Returns a positional argument by index.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to query. |
| `index` | `int` | Zero-based argument index. |

### Returns

`char *`


---

## ap_get_args()

```c
char **ap_get_args(ArgParser *parser);
```

Returns all positional arguments as strings.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to query. |

### Returns

`char **`


---

## ap_get_args_as_ints()

```c
int *ap_get_args_as_ints(ArgParser *parser);
```

Returns all positional arguments as integers.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to query. |

### Returns

`int *`


---

## ap_get_args_as_doubles()

```c
double *ap_get_args_as_doubles(ArgParser *parser);
```

Returns all positional arguments as decimal numbers.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to query. |

### Returns

`double *`


---

## ap_new_cmd()

```c
ArgParser *ap_new_cmd(ArgParser *parent_parser, const char *name);
```

Adds a subcommand and returns its parser.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parent_parser` | `ArgParser *` | Parent parser to configure. |
| `name` | `const char *` | Subcommand name. |

### Returns

`ArgParser *`


---

## ap_set_cmd_callback()

```c
void ap_set_cmd_callback(ArgParser *cmd_parser, ap_callback_t cmd_callback);
```

Sets the function called for a subcommand.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `cmd_parser` | `ArgParser *` | Subcommand parser to configure. |
| `cmd_callback` | `ap_callback_t` | Function to call after parsing the subcommand. |

### Returns

`void`


---

## ap_found_cmd()

```c
bool ap_found_cmd(ArgParser *parent_parser);
```

Checks whether a subcommand was provided.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parent_parser` | `ArgParser *` | Parent parser to query. |

### Returns

`bool`


---

## ap_get_cmd_name()

```c
char *ap_get_cmd_name(ArgParser *parent_parser);
```

Returns the selected subcommand name.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parent_parser` | `ArgParser *` | Parent parser to query. |

### Returns

`char *`


---

## ap_get_cmd_parser()

```c
ArgParser *ap_get_cmd_parser(ArgParser *parent_parser);
```

Returns the selected subcommand parser.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parent_parser` | `ArgParser *` | Parent parser to query. |

### Returns

`ArgParser *`


---

## ap_get_cmd_exit_code()

```c
int ap_get_cmd_exit_code(ArgParser *parent_parser);
```

Returns the selected subcommand callback result.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parent_parser` | `ArgParser *` | Parent parser to query. |

### Returns

`int`


---

## ap_enable_help_command()

```c
void ap_enable_help_command(ArgParser *parent_parser, bool enable);
```

Enables or disables the built-in help subcommand.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parent_parser` | `ArgParser *` | Parent parser to configure. |
| `enable` | `bool` | True to enable the help subcommand. |

### Returns

`void`


---

## ap_get_parent()

```c
ArgParser *ap_get_parent(ArgParser *parser);
```

Returns a subcommand parser’s parent.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Subcommand parser to query. |

### Returns

`ArgParser *`


---

## ap_print()

```c
void ap_print(ArgParser *parser);
```

Prints the parser configuration for debugging.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to print. |

### Returns

`void`


---

## ap_had_memory_error()

```c
bool ap_had_memory_error(ArgParser *parser);
```

Checks whether parser setup ran out of memory.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to query. |

### Returns

`bool`


---

## ap_get_zeroth_root_arg()

```c
char *ap_get_zeroth_root_arg(ArgParser *parser);
```

Returns the program name from the root argument list.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `ArgParser *` | Parser to query. |

### Returns

`char *`


---

# `audio.h`

**Tone and PCM audio playback.**

**Constants**

| Name | Value |
|---|---|
| `BRUCE_AUDIO_MIN_FREQUENCY_HZ` | `20u` |
| `BRUCE_AUDIO_MAX_FREQUENCY_HZ` | `20000u` |
| `BRUCE_AUDIO_MAX_TONE_DURATION_MS` | `60000u` |

---

## audio__tone()

```c
bruce_result_t audio__tone(uint32_t frequency_hz, uint32_t duration_ms, bool non_blocking);
```

Plays a square-wave tone using the board speaker or buzzer.

A non-blocking tone is queued in a worker task; a blocking tone returns
after its duration. Concurrent tones are serialized (and, per
audio__stream_open() below, serialized against any open stream too) and
the bounded asynchronous queue returns BRUCE_ERR_RESOURCE_LIMIT when full.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `frequency_hz` | `uint32_t` | Tone frequency in Hz (BRUCE_AUDIO_MIN/MAX_FREQUENCY_HZ). |
| `duration_ms` | `uint32_t` | Tone duration in milliseconds (up to BRUCE_AUDIO_MAX_TONE_DURATION_MS). |
| `non_blocking` | `bool` | If true, queue the tone and return immediately instead of waiting out its duration. |

### Returns

`bruce_result_t`

#### Permissions

_Not permission-gated._


---

## audio__stream_sample_rate()

```c
uint32_t audio__stream_sample_rate(void);
```

Fixed sample rate (Hz) that every stream runs at.

There is no way to open a stream at a different rate. On a backend with
no PCM support the returned rate is a nominal placeholder.

### Returns

`uint32_t`

#### Permissions

_Not permission-gated._


---

## audio__stream_open()

```c
bruce_result_t audio__stream_open(uint8_t channels);
```

Opens PCM audio output.

Shares the same underlying output as audio__tone() -- while a stream is
open, a concurrent audio__tone() call still works but blocks until the
stream write in progress, if any, finishes. Only one stream may be open
device-wide at a time; calling this while another process's stream is
open returns BRUCE_ERR_BUSY. If the owning process exits or is killed
without calling audio__stream_close() itself, the stream is torn down
automatically.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `channels` | `uint8_t` | Channel count: 1 = mono, 2 = stereo. |

### Returns

`bruce_result_t`

#### Permissions

_Not permission-gated._


---

## audio__stream_writable_frames()

```c
size_t audio__stream_writable_frames(void);
```

Returns how many PCM frames can be written immediately.

Apps that cannot drop audio can consult this before synthesizing more.

### Returns

`size_t`

#### Permissions

_Not permission-gated._


---

## audio__stream_write()

```c
size_t audio__stream_write(const int16_t *samples, size_t frame_count);
```

Writes PCM frames to the open stream.

Takes signed 16-bit PCM interleaved per the `channels` passed to
audio__stream_open(), always at audio__stream_sample_rate() Hz. It never
blocks: it returns the number of frames actually copied to the bounded
stream FIFO. On a backend with no PCM support this is a silent no-op.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `samples` | `const int16_t *` | Interleaved signed 16-bit PCM samples to write. |
| `frame_count` | `size_t` | Number of frames available in samples. |

### Returns

`size_t`

#### Permissions

_Not permission-gated._


---

## audio__stream_close()

```c
bruce_result_t audio__stream_close(void);
```

Closes the streaming PCM output opened by audio__stream_open().

### Returns

`bruce_result_t`

#### Permissions

_Not permission-gated._


---

# `base64.h`

**Base64 encoding (RFC 4648).**

**Constants**

| Name | Value |
|---|---|
| `BRUCE_BASE64_ENCODED_SIZE(byte_count)` | `((((byte_count) + 2) / 3) * 4)` |
| `BRUCE_BASE64_DECODED_SIZE(text_length)` | `((((text_length) + 3) / 4) * 3)` |

---

## base64__encode()

```c
bruce_result_t base64__encode(const void *data, size_t size, char *out, size_t out_capacity);
```

Encodes `size` bytes of `data` as base64 text.

`out` receives BRUCE_BASE64_ENCODED_SIZE(size) characters plus a NUL
terminator; `out_capacity` must be at least that large or this fails
with BRUCE_ERR_RESOURCE_LIMIT without writing anything. The output is a
single unbroken run of characters -- a caller that wants line-wrapped
output (e.g. matching the classic 76-column base64(1) format) inserts
its own breaks when writing it out.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `data` | `const void *` | Bytes to encode. |
| `size` | `size_t` | Number of bytes in data. |
| `out` | `char *` | Buffer to receive the encoded text and its NUL terminator. |
| `out_capacity` | `size_t` | Size of out in bytes. |

### Returns

`bruce_result_t`


---

## base64__decode()

```c
bruce_result_t
base64__decode(const char *text, size_t text_length, uint8_t *out, size_t out_capacity, size_t *out_size);
```

Decodes base64 text back into raw bytes.

Whitespace (space, tab, CR, LF) anywhere in `text` is ignored, so
previously line-wrapped text decodes without preprocessing. Padding
('=') is only accepted at the very end of the (whitespace-stripped)
input. Fails with BRUCE_ERR_INVALID_ARGUMENT for malformed input (bad
alphabet, misplaced padding, or a length that isn't a multiple of 4 once
whitespace is stripped), or BRUCE_ERR_RESOURCE_LIMIT if out isn't large
enough for BRUCE_BASE64_DECODED_SIZE(text_length) bytes. `text`/`out` may
be NULL when `text_length`/`out_capacity` (respectively) is 0.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `text` | `const char *` | Base64 text to decode. |
| `text_length` | `size_t` | Number of characters in text. |
| `out` | `uint8_t *` | Buffer to receive the decoded bytes. |
| `out_capacity` | `size_t` | Size of out in bytes. |
| `out_size` | `size_t *` | Receives the number of bytes written to out. |

### Returns

`bruce_result_t`


---

# `bluetooth.h`

**BLE advertisement scanning.**

**Constants**

| Name | Value |
|---|---|
| `BRUCE_BLUETOOTH_NAME_MAX` | `63` |
| `BRUCE_BLUETOOTH_ADDRESS_LEN` | `6` |

---

## bluetooth__device_t()

```c
typedef struct {
    uint8_t address[BRUCE_BLUETOOTH_ADDRESS_LEN];
    uint8_t address_type;
    int8_t rssi;
    char name[BRUCE_BLUETOOTH_NAME_MAX + 1];
} bluetooth__device_t;
```


---

## bluetooth__scan_ble()

```c
int bluetooth__scan_ble(bluetooth__device_t *devices, size_t capacity, uint32_t timeout_ms);
```

Performs a synchronous BLE advertisement scan.

Returns the number of unique devices copied to `devices`, or a negative
BRUCE_ERR_* value. A zero timeout selects the Core default. Results are
ordered by descending RSSI.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `devices` | `bluetooth__device_t *` | Array to receive scanned devices. |
| `capacity` | `size_t` | Number of entries devices can hold. |
| `timeout_ms` | `uint32_t` | Scan duration in milliseconds, or 0 for the Core default. |

### Returns

`int`


---

# `bluetooth_hid.h`

**Bluetooth keyboard and gamepad connections.**

---

## bluetooth_hid__usage_t()

```c
typedef enum {
    BRUCE_BLUETOOTH_HID_UNKNOWN = 0,
    BRUCE_BLUETOOTH_HID_KEYBOARD,
    BRUCE_BLUETOOTH_HID_GAMEPAD,
} bluetooth_hid__usage_t;
```


---

## bluetooth_hid__device_t()

```c
typedef struct {
    uint8_t address[BRUCE_BLUETOOTH_ADDRESS_LEN];
    int8_t rssi;
    bluetooth_hid__usage_t usage;
    char name[BRUCE_BLUETOOTH_NAME_MAX + 1];
} bluetooth_hid__device_t;
```


---

## bluetooth_hid__is_supported()

```c
bool bluetooth_hid__is_supported(void);
```

Whether Classic Bluetooth HID is available on this target.

Only targets with a Classic-capable radio support it.

### Returns

`bool`


---

## bluetooth_hid__scan()

```c
int bluetooth_hid__scan(bluetooth_hid__device_t *devices, size_t capacity, uint32_t timeout_ms);
```

Scans for nearby Classic Bluetooth HID devices.

Returns the number of devices copied to `devices`, or a negative
BRUCE_ERR_* value (including BRUCE_ERR_UNSUPPORTED on targets without a
Classic-capable radio).

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `devices` | `bluetooth_hid__device_t *` | Array to receive scanned devices. |
| `capacity` | `size_t` | Number of entries devices can hold. |
| `timeout_ms` | `uint32_t` | Scan duration in milliseconds. |

### Returns

`int`


---

## bluetooth_hid__connect()

```c
bruce_result_t
bluetooth_hid__connect(const uint8_t address[BRUCE_BLUETOOTH_ADDRESS_LEN], uint32_t timeout_ms);
```

Connects to a Classic Bluetooth HID device.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `address` | `const uint8_t [BRUCE_BLUETOOTH_ADDRESS_LEN]` | Bluetooth address of the device to connect to. |
| `timeout_ms` | `uint32_t` | Connection timeout in milliseconds. |

### Returns

`bruce_result_t`


---

## bluetooth_hid__disconnect()

```c
bruce_result_t bluetooth_hid__disconnect(void);
```

Disconnects the currently connected Classic Bluetooth HID device, if any.

### Returns

`bruce_result_t`


---

## bluetooth_hid__is_connected()

```c
bool bluetooth_hid__is_connected(void);
```

Whether a Classic Bluetooth HID device is currently connected.

### Returns

`bool`


---

## bluetooth_hid__connected_device()

```c
bruce_result_t bluetooth_hid__connected_device(bluetooth_hid__device_t *out_device);
```

Returns the currently connected HID device's identity.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `out_device` | `bluetooth_hid__device_t *` | Receives the connected device's identity. |

### Returns

`bruce_result_t`

#### Permissions

- `bt`


---

# `clipboard.h`

**Shared copy/paste clipboard.**

A single system-wide clipboard slot, independent of any process's
lifetime: one app (e.g. the file manager) can copy something and a
different app -- launched later, by a different process, possibly after
the copying process has already exited -- can paste it (e.g. the text
editor, or the file manager again in a different directory). Setting
either kind of content replaces whatever was there before; there is
exactly one slot, no history and no separate text/file/binary buffers.

**Constants**

| Name | Value |
|---|---|
| `BRUCE_CLIPBOARD_MAX_FILES` | `64` |
| `BRUCE_CLIPBOARD_MAX_BINARY_BYTES` | `(512u * 1024u)` |

---

## bruce_clipboard_kind_t()

```c
typedef enum {
    BRUCE_CLIPBOARD_EMPTY = 0,
    BRUCE_CLIPBOARD_TEXT,
    BRUCE_CLIPBOARD_FILES,
    /* Raw bytes with no filesystem home of their own yet, e.g. an image
     * fetched by the browser but never saved to storage -- see
     * clipboard__set_binary(). */
    BRUCE_CLIPBOARD_BINARY,
} bruce_clipboard_kind_t;
```


---

## bruce_clipboard_file_mode_t()

```c
typedef enum {
    BRUCE_CLIPBOARD_FILE_COPY = 0,
    BRUCE_CLIPBOARD_FILE_CUT,
} bruce_clipboard_file_mode_t;
```


---

## clipboard__kind()

```c
bruce_clipboard_kind_t clipboard__kind(void);
```

What's currently on the clipboard.

### Returns

`bruce_clipboard_kind_t`


---

## clipboard__set_text()

```c
bruce_result_t clipboard__set_text(const char *text);
```

Copies text onto the clipboard, replacing any prior content.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `text` | `const char *` | Null-terminated text to store; copied internally. |

### Returns

`bruce_result_t`


---

## clipboard__get_text()

```c
const char *clipboard__get_text(void);
```

Returns the clipboard's text, or NULL if clipboard__kind() is not BRUCE_CLIPBOARD_TEXT.

The returned pointer is borrowed: valid until the next clipboard__set_text(),
clipboard__set_files(), or clipboard__clear() call from any process.

### Returns

`const char *`


---

## clipboard__set_files()

```c
bruce_result_t clipboard__set_files(const char *const *paths, size_t count, bruce_clipboard_file_mode_t mode);
```

Records file/directory paths on the clipboard for a later clipboard__paste_files(), replacing any prior content.

            originals in place) or cut (remove each original once it, and
            everything under it, has been pasted successfully).

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `paths` | `const char *const *` | Absolute source paths; copied internally. |
| `count` | `size_t` | Number of paths, from 1 to BRUCE_CLIPBOARD_MAX_FILES. |
| `mode` | `bruce_clipboard_file_mode_t` | Whether the eventual paste should copy (leaving the |

### Returns

`bruce_result_t`


---

## clipboard__file_count()

```c
size_t clipboard__file_count(void);
```

Number of paths on the clipboard (0 unless clipboard__kind() is BRUCE_CLIPBOARD_FILES).

### Returns

`size_t`


---

## clipboard__get_file()

```c
const char *clipboard__get_file(size_t index);
```

Returns one clipboard path by index, or NULL if index is out of range.

The returned pointer is borrowed under the same rules as clipboard__get_text().

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `index` | `size_t` | Zero-based index, below clipboard__file_count(). |

### Returns

`const char *`


---

## clipboard__file_mode()

```c
bruce_clipboard_file_mode_t clipboard__file_mode(void);
```

Whether the clipboard's files were cut or copied; meaningless when clipboard__file_count() is 0.

### Returns

`bruce_clipboard_file_mode_t`


---

## clipboard__paste_files()

```c
bruce_result_t clipboard__paste_files(const char *target_directory);
```

Copies (or moves, for a cut) every clipboard file/directory into target_directory.

Recurses into directories, creating them at the destination as needed.
Refuses (BRUCE_ERR_ALREADY_EXISTS) rather than overwriting any
destination path that already exists, and refuses
(BRUCE_ERR_INVALID_ARGUMENT) a target that is one of the clipboard's own
directories or nested inside one, rather than recursing into its own
output forever. Stops at the first failure; a cut has already removed
whichever earlier entries it fully copied, but does not roll them back.
The clipboard itself is left untouched either way, so the same content
can be pasted again.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `target_directory` | `const char *` | Absolute destination directory; must already exist. |

### Returns

`bruce_result_t`

#### Permissions

- `storage`


---

## clipboard__paste_file_as()

```c
bruce_result_t clipboard__paste_file_as(size_t index, const char *target_path, bool overwrite);
```

Copies/moves a single clipboard entry to an exact destination path.

Same recursive copy/move engine as clipboard__paste_files(), but for one
clipboard entry chosen by index and a caller-supplied destination path
(rather than target_directory + the source's own basename) - lets a
caller (e.g. the file manager) paste a copy under a different name, or
explicitly allow overwriting an existing destination instead of getting
BRUCE_ERR_ALREADY_EXISTS back. Refuses (BRUCE_ERR_INVALID_ARGUMENT) a
target_path equal to the source itself, or one of the source's own
directories or nested inside one, same as clipboard__paste_files() does
for its target_directory.

                  existing destination, matching clipboard__paste_files();
                  when true, removes it (file, or whole directory tree)
                  first.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `index` | `size_t` | Zero-based clipboard file index, below clipboard__file_count(). |
| `target_path` | `const char *` | Absolute destination path, including the final name. |
| `overwrite` | `bool` | When false, refuses (BRUCE_ERR_ALREADY_EXISTS) an |

### Returns

`bruce_result_t`

#### Permissions

- `storage`


---

## clipboard__set_binary()

```c
bruce_result_t clipboard__set_binary(const void *data, size_t len, const char *filename);
```

Copies raw bytes onto the clipboard, replacing any prior content.

For content that has no filesystem path of its own (yet) to hand to
clipboard__set_files() instead -- e.g. an image the browser fetched but
the user hasn't chosen to save anywhere.

                separators) for whatever later writes these bytes out --
                see clipboard__binary_filename() -- or NULL to leave it
                unset.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `data` | `const void *` | Bytes to store; copied internally. |
| `len` | `size_t` | Length of data in bytes, from 1 to BRUCE_CLIPBOARD_MAX_BINARY_BYTES. |
| `filename` | `const char *` | Optional suggested filename (a bare name, no directory |

### Returns

`bruce_result_t`


---

## clipboard__binary_size()

```c
size_t clipboard__binary_size(void);
```

Size in bytes of the clipboard's binary payload (0 unless clipboard__kind() is BRUCE_CLIPBOARD_BINARY).

### Returns

`size_t`


---

## clipboard__get_binary()

```c
const void *clipboard__get_binary(void);
```

Returns the clipboard's binary payload, or NULL if clipboard__kind() is not BRUCE_CLIPBOARD_BINARY.

The returned pointer is borrowed under the same rules as clipboard__get_text().

### Returns

`const void *`


---

## clipboard__binary_filename()

```c
const char *clipboard__binary_filename(void);
```

The binary payload's suggested filename, or NULL if none was given.

Meaningless when clipboard__kind() is not BRUCE_CLIPBOARD_BINARY. A
pasting app (e.g. the file manager) should fall back to asking the user
for a name when this is NULL -- some copier may not have one to offer.

### Returns

`const char *`


---

## clipboard__paste_binary()

```c
bruce_result_t clipboard__paste_binary(const char *target_path);
```

Writes the clipboard's binary payload to target_path, creating it.

Refuses (BRUCE_ERR_ALREADY_EXISTS) rather than overwriting an existing
file there, matching clipboard__paste_files(). Reports
BRUCE_ERR_INVALID_STATE if clipboard__kind() is not
BRUCE_CLIPBOARD_BINARY. The clipboard itself is left untouched either
way, so the same content can be pasted again.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `target_path` | `const char *` | Absolute destination file path; must not already exist. |

### Returns

`bruce_result_t`

#### Permissions

- `storage`


---

## clipboard__clear()

```c
void clipboard__clear(void);
```

Clears the clipboard; clipboard__kind() becomes BRUCE_CLIPBOARD_EMPTY.

### Returns

`void`


---

# `clock.h`

**Clock, time zone, and NTP settings.**

---

## bruce_clock_datetime_t()

```c
typedef struct {
    uint16_t year;
    uint8_t month;
    uint8_t day;
    uint8_t hour;
    uint8_t minute;
    uint8_t second;
} bruce_clock_datetime_t;
```


---

## bruce_clock_sync_status_t()

```c
typedef enum {
    BRUCE_CLOCK_SYNC_IDLE = 0,
    BRUCE_CLOCK_SYNC_IN_PROGRESS,
    BRUCE_CLOCK_SYNC_SUCCEEDED,
    BRUCE_CLOCK_SYNC_FAILED,
} bruce_clock_sync_status_t;
```


---

## clock__get_utc()

```c
bruce_result_t clock__get_utc(bruce_clock_datetime_t *out);
```

Reads the system clock, which is kept in UTC.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `out` | `bruce_clock_datetime_t *` | Receives the current UTC date/time. |

### Returns

`bruce_result_t`


---

## clock__get_local()

```c
bruce_result_t clock__get_local(bruce_clock_datetime_t *out);
```

Reads the system clock converted to local time.

Applies Config's fixed UTC offset and optional manual one-hour DST
adjustment.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `out` | `bruce_clock_datetime_t *` | Receives the current local date/time. |

### Returns

`bruce_result_t`


---

## clock__set_local()

```c
bruce_result_t clock__set_local(const bruce_clock_datetime_t *local);
```

Sets the system clock from a local date and time.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `local` | `const bruce_clock_datetime_t *` | Local date/time to convert to UTC and apply. |

### Returns

`bruce_result_t`

#### Permissions

- `config`


---

## clock__sync_ntp()

```c
bruce_result_t clock__sync_ntp(uint32_t timeout_ms);
```

Synchronizes the clock over the network.

Wi-Fi must already be connected.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `timeout_ms` | `uint32_t` | Sync timeout in milliseconds, or 0 for 10 seconds. |

### Returns

`bruce_result_t`

#### Permissions

- `config`
- `wifi`


---

## clock__get_sync_status()

```c
bruce_clock_sync_status_t clock__get_sync_status(void);
```

Returns the status of the latest clock synchronization.

### Returns

`bruce_clock_sync_status_t`


---

## clock__get_ntp_server()

```c
const char *clock__get_ntp_server(void);
```

Returns the configured NTP server address.

### Returns

`const char *`


---

# `compress.h`

**DEFLATE compression/decompression (zlib-backed).**

Used to compress/decompress sprites, bitmaps, and general file data, and
as the codec layer under core_sdk/archive.h's ".tar.gz" support. No
permission of its own - this only ever touches memory the caller already
gave it; a caller doing file I/O around it (e.g. writing the compressed
output to storage) needs whatever permission that I/O itself requires.

Two ways to use it:

  - The one-shot compress__compute()/decompress__compute() functions, for
    a buffer that's already fully in memory and whose *decompressed* size
    is already known ahead of time (e.g. a sprite's own header already
    states its uncompressed width*height*bpp) - the common case for
    sprites/bitmaps.
  - The streaming compress__start()/compress__update()/compress__end() and
    decompress__start()/decompress__update()/decompress__end() functions,
    for arbitrary-size data (a whole file, an archive member) that
    shouldn't ever need to sit fully in RAM at once. This is a thin,
    honest wrapper over zlib's own deflate()/inflate() call contract
    rather than one that hides it - compressed output size isn't bounded
    by input chunk size, so any API that pretended otherwise would either
    lie about completion or require an unbounded output buffer.

**Constants**

| Name | Value |
|---|---|
| `BRUCE_COMPRESS_LEVEL_DEFAULT` | `(-1)` |
| `BRUCE_COMPRESS_LEVEL_FASTEST` | `1` |
| `BRUCE_COMPRESS_LEVEL_BEST` | `9` |

---

## bruce_compress_format_t()

```c
typedef enum {
    /* RFC1951 raw deflate: no header, no trailer, no checksum. Smallest
     * output, for embedding inside a caller's own container that already
     * tracks the original size (e.g. a sprite file's header). */
    BRUCE_COMPRESS_FORMAT_RAW = 0,
    /* RFC1950 zlib wrapper: 2-byte header + Adler-32 trailer. */
    BRUCE_COMPRESS_FORMAT_ZLIB,
    /* RFC1952 gzip container: magic/OS-byte header + CRC-32/size trailer -
     * what ".gz" files and the gzip layer of ".tar.gz" use. */
    BRUCE_COMPRESS_FORMAT_GZIP,
    /* decompress__start() only: sniff zlib-vs-gzip from the stream's own
     * header instead of the caller having to know which one it is. */
    BRUCE_COMPRESS_FORMAT_AUTO,
} bruce_compress_format_t;
```


---

## bruce_compress_ctx_t()

```c
typedef struct bruce_compress_ctx bruce_compress_ctx_t;
```


---

## compress__bound()

```c
size_t compress__bound(bruce_compress_format_t format, size_t input_size);
```

Upper bound on compress__compute()'s output size for `input_size` bytes of input, to size `out` with.

Compression can (rarely, for already-dense input) make data slightly
larger; this is the worst case, not the expected case.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `format` | `bruce_compress_format_t` |  |
| `input_size` | `size_t` |  |

### Returns

`size_t`


---

## compress__compute()

```c
bruce_result_t compress__compute(
    bruce_compress_format_t format, int level, const void *data, size_t size, void *out, size_t out_capacity,
    size_t *out_size
);
```

Compresses a buffer already fully in memory, in one call.

Equivalent to compress__start() + compress__update() (with finish=true) +
compress__end(), for the common case where `data` is small enough to
already be fully in RAM. Prefer the streaming API for anything
file-sized.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `format` | `bruce_compress_format_t` | Output container. Must not be BRUCE_COMPRESS_FORMAT_AUTO. |
| `level` | `int` | BRUCE_COMPRESS_LEVEL_DEFAULT, or 0-9 (0 = store, 9 = smallest/slowest). |
| `data` | `const void *` | Bytes to compress. |
| `size` | `size_t` | Number of bytes in data. |
| `out` | `void *` | Buffer to receive the compressed output. |
| `out_capacity` | `size_t` | Size of out; compress__bound(format, size) is always enough. |
| `out_size` | `size_t *` | Receives the number of bytes written to out. |

### Returns

`bruce_result_t`


---

## decompress__compute()

```c
bruce_result_t decompress__compute(
    bruce_compress_format_t format, const void *data, size_t size, void *out, size_t out_capacity, size_t *out_size
);
```

Decompresses a buffer already fully in memory, in one call.

The decompressed size must already be known and `out_capacity` must be at
least that large - this never grows `out` or guesses; it fails with
BRUCE_ERR_RESOURCE_LIMIT if the decompressed data doesn't fit. Prefer the
streaming API when the decompressed size isn't known ahead of time (the
general "decompress a file" case).

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `format` | `bruce_compress_format_t` | Container to expect `data` to be in. |
| `data` | `const void *` | Compressed bytes. |
| `size` | `size_t` | Number of bytes in data. |
| `out` | `void *` | Buffer to receive the decompressed output. |
| `out_capacity` | `size_t` | Size of out. |
| `out_size` | `size_t *` | Receives the number of bytes written to out. |

### Returns

`bruce_result_t`


---

## compress__start()

```c
bruce_compress_ctx_t *compress__start(bruce_compress_format_t format, int level);
```

Starts a new streaming compression.

@return A new context, or NULL on allocation failure or an invalid format/level.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `format` | `bruce_compress_format_t` | Output container. Must not be BRUCE_COMPRESS_FORMAT_AUTO. |
| `level` | `int` | BRUCE_COMPRESS_LEVEL_DEFAULT, or 0-9. |

### Returns

`bruce_compress_ctx_t *`


---

## compress__update()

```c
bruce_result_t compress__update(
    bruce_compress_ctx_t *ctx, const void *in, size_t in_size, bool finish, void *out, size_t out_capacity,
    size_t *out_written, bool *out_finished
);
```

Feeds input to and/or drains output from an in-progress compression.

Usage loop: call with a new chunk of `in`; if `*out_written` comes back
equal to `out_capacity`, more compressed output may be pending - call
again with `in`/`in_size` as NULL/0 (same `finish`) to keep draining
before feeding more input. Pass `finish=true` on the call carrying the
last input byte, then keep draining (in=NULL) until `*out_finished` comes
back true - that is the last chunk of compressed output.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `ctx` | `bruce_compress_ctx_t *` | Context returned by compress__start(). |
| `in` | `const void *` | New input bytes, or NULL to only drain pending output. |
| `in_size` | `size_t` | Number of bytes in in, or 0. |
| `finish` | `bool` | True on (and after) the call carrying the last input byte. |
| `out` | `void *` | Buffer to receive compressed output. |
| `out_capacity` | `size_t` | Size of out. |
| `out_written` | `size_t *` | Receives the number of bytes written to out this call. |
| `out_finished` | `bool *` | Receives true once this was the final chunk of output (only possible when finish=true). |

### Returns

`bruce_result_t`


---

## compress__end()

```c
void compress__end(bruce_compress_ctx_t *ctx);
```

Frees a streaming compression context.

Must be called exactly once per compress__start(), whether or not
out_finished was ever reached (an early abort is fine).

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `ctx` | `bruce_compress_ctx_t *` |  |

### Returns

`void`


---

## decompress__start()

```c
bruce_compress_ctx_t *decompress__start(bruce_compress_format_t format);
```

Starts a new streaming decompression.

  sniffs zlib-vs-gzip from the stream's own header.
@return A new context, or NULL on allocation failure.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `format` | `bruce_compress_format_t` | Container `in` is expected to be in. BRUCE_COMPRESS_FORMAT_AUTO |

### Returns

`bruce_compress_ctx_t *`


---

## decompress__update()

```c
bruce_result_t decompress__update(
    bruce_compress_ctx_t *ctx, const void *in, size_t in_size, void *out, size_t out_capacity, size_t *out_written,
    bool *out_finished
);
```

Feeds input to and/or drains output from an in-progress decompression.

Same drain loop as compress__update() (call again with in=NULL whenever
`*out_written == out_capacity`), except there's no `finish` flag to pass -
the compressed stream's own trailer marks its end, so `*out_finished`
becomes true on its own once the last byte of decompressed output has
been produced from the input fed so far.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `ctx` | `bruce_compress_ctx_t *` | Context returned by decompress__start(). |
| `in` | `const void *` | New compressed input bytes, or NULL to only drain pending output. |
| `in_size` | `size_t` | Number of bytes in in, or 0. |
| `out` | `void *` | Buffer to receive decompressed output. |
| `out_capacity` | `size_t` | Size of out. |
| `out_written` | `size_t *` | Receives the number of bytes written to out this call. |
| `out_finished` | `bool *` | Receives true once the compressed stream's end has been reached. |

### Returns

`bruce_result_t`


---

## decompress__end()

```c
void decompress__end(bruce_compress_ctx_t *ctx);
```

Frees a streaming decompression context.

Must be called exactly once per decompress__start(), whether or not
out_finished was ever reached (an early abort, or a corrupt/truncated
stream, is fine).

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `ctx` | `bruce_compress_ctx_t *` |  |

### Returns

`void`


---

# `config.h`

**Global Bruce Config API.**

The Core owns one configuration singleton. Getters are type-safe and
return scalar values directly or read-only pointers into that singleton;
they do not allocate or copy. Pointers remain valid until the
configuration is changed, loaded, or reset and must never be freed by
callers.

Getters return zero, false, or NULL if initialization or permission
checks fail. Setters return BRUCE_OK or a BRUCE_ERR_* result, validate
input, and persist immediately to /config/bruce.conf. Built-in modules
always pass permission checks; external ELF/JS processes require
`config`, except that protected credentials are never exposed to
external processes.

**Constants**

| Name | Value |
|---|---|
| `BRUCE_CONFIG_STARTUP_APP_MAX_COUNT` | `8` |
| `BRUCE_CONFIG_HOTKEY_MAX_COUNT` | `8` |
| `BRUCE_CONFIG_HOTKEY_MAX_LEN` | `32` |
| `BRUCE_CONFIG_HOTKEY_ACTION_MAX_LEN` | `64` |

---

## bruce_config_wifi_credential_t()

```c
typedef struct {
    const char *ssid;
    const char *password;
} bruce_config_wifi_credential_t;
```


---

## bruce_config_startup_apps_t()

```c
typedef struct {
    const char *items[BRUCE_CONFIG_STARTUP_APP_MAX_COUNT];
    size_t count;
} bruce_config_startup_apps_t;
```


---

## bruce_config_hotkey_t()

```c
typedef struct {
    const char *key;
    /* AppRunner command line executed when the chord is pressed. */
    const char *action;
} bruce_config_hotkey_t;
```


---

## bruce_config_hotkeys_t()

```c
typedef struct {
    bruce_config_hotkey_t items[BRUCE_CONFIG_HOTKEY_MAX_COUNT];
    size_t count;
} bruce_config_hotkeys_t;
```


---

## config__get_wifi_ap_ssid()

```c
const char *config__get_wifi_ap_ssid(void);
```

Configured Wi-Fi AP SSID.

Permanently protected: never exposed to external (ELF/JS) processes, even
with the `config` permission. Built-in modules are unaffected.

### Returns

`const char *`

#### Permissions

_Not permission-gated._


---

## config__get_wifi_ap_password()

```c
const char *config__get_wifi_ap_password(void);
```

Configured Wi-Fi AP password.

Permanently protected: never exposed to external (ELF/JS) processes, even
with the `config` permission. Built-in modules are unaffected.

### Returns

`const char *`

#### Permissions

_Not permission-gated._


---

## config__set_wifi_ap()

```c
bruce_result_t config__set_wifi_ap(const char *ssid, const char *password);
```

Sets the Wi-Fi AP SSID and password.

Permanently protected: never exposed to external (ELF/JS) processes, even
with the `config` permission. Built-in modules are unaffected.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `ssid` | `const char *` | New AP SSID. |
| `password` | `const char *` | New AP password. |

### Returns

`bruce_result_t`

#### Permissions

_Not permission-gated._


---

## config__wifi_credential_count()

```c
size_t config__wifi_credential_count(void);
```

Number of saved Wi-Fi station credentials.

Permanently protected: never exposed to external (ELF/JS) processes, even
with the `config` permission. Built-in modules are unaffected.

### Returns

`size_t`

#### Permissions

_Not permission-gated._


---

## config__wifi_credential_at()

```c
const bruce_config_wifi_credential_t *config__wifi_credential_at(size_t index);
```

Reads a saved Wi-Fi station credential by index.

Permanently protected: never exposed to external (ELF/JS) processes, even
with the `config` permission. Built-in modules are unaffected.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `index` | `size_t` | Zero-based index, below config__wifi_credential_count(). |

### Returns

`const bruce_config_wifi_credential_t *`

#### Permissions

_Not permission-gated._


---

## config__find_wifi_credential()

```c
const bruce_config_wifi_credential_t *config__find_wifi_credential(const char *ssid);
```

Finds a saved Wi-Fi station credential by SSID.

Permanently protected: never exposed to external (ELF/JS) processes, even
with the `config` permission. Built-in modules are unaffected.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `ssid` | `const char *` | SSID to look up. Returns NULL if not found. |

### Returns

`const bruce_config_wifi_credential_t *`

#### Permissions

_Not permission-gated._


---

## config__add_or_update_wifi_credential()

```c
bruce_result_t config__add_or_update_wifi_credential(const char *ssid, const char *password);
```

Adds a new saved Wi-Fi station credential, or updates an existing one with the same SSID.

Permanently protected: never exposed to external (ELF/JS) processes, even
with the `config` permission. Built-in modules are unaffected.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `ssid` | `const char *` | SSID of the credential to add or update. |
| `password` | `const char *` | Password to store for this SSID. |

### Returns

`bruce_result_t`

#### Permissions

_Not permission-gated._


---

## config__get_wifi_mac()

```c
const char *config__get_wifi_mac(void);
```

Configured Wi-Fi MAC address override, if any.

Permanently protected: never exposed to external (ELF/JS) processes, even
with the `config` permission. Built-in modules are unaffected.

### Returns

`const char *`

#### Permissions

_Not permission-gated._


---

## config__set_wifi_mac()

```c
bruce_result_t config__set_wifi_mac(const char *value);
```

Sets the Wi-Fi MAC address override.

Permanently protected: never exposed to external (ELF/JS) processes, even
with the `config` permission. Built-in modules are unaffected.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `value` | `const char *` | New MAC address override. |

### Returns

`bruce_result_t`

#### Permissions

_Not permission-gated._


---

## config__get_color_primary()

```c
uint16_t config__get_color_primary(void);
```

Theme / display colors.

Ten RGB565 color roles: primary/secondary (accents), background
(full-screen canvas), surface (a raised panel/window on top of that
canvas), text/textMuted (body copy vs. de-emphasized copy), border
(dividers and window strokes), and success/warning/error (status
semantics). Core's own dialog renderer and built-in modules that draw
chrome (bruce_launcher, terminal, notification_service, ...) read these
instead of hard-coding colors. Core only stores and validates the ten
values - it has no notion of a named theme; a catalog of presets (legacy
single-accent sets, community palettes such as Dracula or Nord, ...) and
the UI/CLI to pick one is `modules/config`'s job (see its `theme`
subcommand), which resolves a name to either one of these ten setters
(a single role) or config__set_colors() below (a whole preset, applied
and persisted in one batch).

### Returns

`uint16_t`

#### Permissions

- `config`


---

## config__set_color_primary()

```c
bruce_result_t config__set_color_primary(uint16_t value);
```

Sets the primary accent color.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `value` | `uint16_t` | New RGB565 color value. |

### Returns

`bruce_result_t`

#### Permissions

- `config`


---

## config__get_color_secondary()

```c
uint16_t config__get_color_secondary(void);
```

Secondary accent color. @permission config

### Returns

`uint16_t`


---

## config__set_color_secondary()

```c
bruce_result_t config__set_color_secondary(uint16_t value);
```

Sets the secondary accent color.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `value` | `uint16_t` | New RGB565 color value. |

### Returns

`bruce_result_t`

#### Permissions

- `config`


---

## config__get_color_background()

```c
uint16_t config__get_color_background(void);
```

Full-screen canvas background color. @permission config

### Returns

`uint16_t`


---

## config__set_color_background()

```c
bruce_result_t config__set_color_background(uint16_t value);
```

Sets the background color.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `value` | `uint16_t` | New RGB565 color value. |

### Returns

`bruce_result_t`

#### Permissions

- `config`


---

## config__get_color_surface()

```c
uint16_t config__get_color_surface(void);
```

Raised panel/window surface color. @permission config

### Returns

`uint16_t`


---

## config__set_color_surface()

```c
bruce_result_t config__set_color_surface(uint16_t value);
```

Sets the surface color.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `value` | `uint16_t` | New RGB565 color value. |

### Returns

`bruce_result_t`

#### Permissions

- `config`


---

## config__get_color_text()

```c
uint16_t config__get_color_text(void);
```

Body copy text color. @permission config

### Returns

`uint16_t`


---

## config__set_color_text()

```c
bruce_result_t config__set_color_text(uint16_t value);
```

Sets the body text color.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `value` | `uint16_t` | New RGB565 color value. |

### Returns

`bruce_result_t`

#### Permissions

- `config`


---

## config__get_color_text_muted()

```c
uint16_t config__get_color_text_muted(void);
```

De-emphasized text color. @permission config

### Returns

`uint16_t`


---

## config__set_color_text_muted()

```c
bruce_result_t config__set_color_text_muted(uint16_t value);
```

Sets the muted text color.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `value` | `uint16_t` | New RGB565 color value. |

### Returns

`bruce_result_t`

#### Permissions

- `config`


---

## config__get_color_border()

```c
uint16_t config__get_color_border(void);
```

Divider/window stroke color. @permission config

### Returns

`uint16_t`


---

## config__set_color_border()

```c
bruce_result_t config__set_color_border(uint16_t value);
```

Sets the border color.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `value` | `uint16_t` | New RGB565 color value. |

### Returns

`bruce_result_t`

#### Permissions

- `config`


---

## config__get_color_success()

```c
uint16_t config__get_color_success(void);
```

Success-status color. @permission config

### Returns

`uint16_t`


---

## config__set_color_success()

```c
bruce_result_t config__set_color_success(uint16_t value);
```

Sets the success-status color.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `value` | `uint16_t` | New RGB565 color value. |

### Returns

`bruce_result_t`

#### Permissions

- `config`


---

## config__get_color_warning()

```c
uint16_t config__get_color_warning(void);
```

Warning-status color. @permission config

### Returns

`uint16_t`


---

## config__set_color_warning()

```c
bruce_result_t config__set_color_warning(uint16_t value);
```

Sets the warning-status color.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `value` | `uint16_t` | New RGB565 color value. |

### Returns

`bruce_result_t`

#### Permissions

- `config`


---

## config__get_color_error()

```c
uint16_t config__get_color_error(void);
```

Error-status color. @permission config

### Returns

`uint16_t`


---

## config__set_color_error()

```c
bruce_result_t config__set_color_error(uint16_t value);
```

Sets the error-status color.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `value` | `uint16_t` | New RGB565 color value. |

### Returns

`bruce_result_t`

#### Permissions

- `config`


---

## bruce_config_theme_colors_t()

```c
typedef struct {
    uint16_t primary;
    uint16_t secondary;
    uint16_t background;
    uint16_t surface;
    uint16_t text;
    uint16_t text_muted;
    uint16_t border;
    uint16_t success;
    uint16_t warning;
    uint16_t error;
} bruce_config_theme_colors_t;
```


---

## config__set_colors()

```c
bruce_result_t config__set_colors(const bruce_config_theme_colors_t *colors);
```

Applies all ten color_* roles and persists once.

Instead of the ten separate writes ten config__set_color_*() calls would
each incur. Callers that set a whole theme at once (a named preset, a
full WebUI theme payload, ...) should prefer this over calling the
individual setters in a row; callers changing a single role should keep
using that role's own setter.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `colors` | `const bruce_config_theme_colors_t *` | All ten color roles to apply and persist together. |

### Returns

`bruce_result_t`

#### Permissions

- `config`


---

## config__parse_theme_color()

```c
bool config__parse_theme_color(const char *text, uint16_t *out_rgb565);
```

Parses a color written as CSS-style hex.

"RGB", "#RGB", "RRGGBB", "#RRGGBB" (24-bit, downsampled to RGB565) or a
bare 4-digit RGB565 value such as "a80f" (native, used as-is) -- into
`*out_rgb565`. Leading '#' is optional and case is ignored. Returns false
(leaving `*out_rgb565` untouched) on a NULL/empty string or a length that
matches none of those forms. This is plain parsing, not a config field:
it's exposed so any caller that lets a user type a hex color
(bruce.conf's theme loader, the WebUI theme editor, `modules/config`'s
theme subcommand, ...) gets identical, forgiving parsing without
duplicating it.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `text` | `const char *` | Hex color text to parse. |
| `out_rgb565` | `uint16_t *` | Receives the parsed RGB565 value on success. |

### Returns

`bool`


---

## config__get_display_buffered_rendering()

```c
bool config__get_display_buffered_rendering(void);
```

Controls whether Core retains a full RGB565 framebuffer.

Changes apply after reboot. When false, drawing is streamed directly to
the panel and framebuffer-dependent features such as snapshots are
unavailable.

### Returns

`bool`

#### Permissions

- `config`


---

## config__set_display_buffered_rendering()

```c
bruce_result_t config__set_display_buffered_rendering(bool value);
```

Sets whether Core retains a full RGB565 framebuffer.

Changes apply after reboot.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `value` | `bool` | New setting. |

### Returns

`bruce_result_t`

#### Permissions

- `config`


---

## config__get_display_rotation()

```c
int config__get_display_rotation(void);
```

Display rotation in quarter turns clockwise (0..3).

Changes apply after reboot.

### Returns

`int`

#### Permissions

- `config`


---

## config__set_display_rotation()

```c
bruce_result_t config__set_display_rotation(int value);
```

Sets the display rotation.

Changes apply after reboot.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `value` | `int` | Quarter turns clockwise (0..3). |

### Returns

`bruce_result_t`

#### Permissions

- `config`


---

## config__get_display_dma_framebuffer()

```c
bool config__get_display_dma_framebuffer(void);
```

Controls whether the full framebuffer is DMA-capable.

Changes apply after reboot and only affects buffered rendering.

### Returns

`bool`

#### Permissions

- `config`


---

## config__set_display_dma_framebuffer()

```c
bruce_result_t config__set_display_dma_framebuffer(bool value);
```

Sets whether the full framebuffer is DMA-capable.

Changes apply after reboot and only affects buffered rendering.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `value` | `bool` | New setting. |

### Returns

`bruce_result_t`

#### Permissions

- `config`


---

## config__get_launcher()

```c
const char *config__get_launcher(void);
```

Launcher command run at startup.

### Returns

`const char *`

#### Permissions

- `config`


---

## config__set_launcher()

```c
bruce_result_t config__set_launcher(const char *value);
```

Sets the launcher command run at startup.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `value` | `const char *` | New launcher command line. |

### Returns

`bruce_result_t`

#### Permissions

- `config`


---

## config__get_display_dim_timeout()

```c
int config__get_display_dim_timeout(void);
```

Screen dim timeout, in seconds. @permission config

### Returns

`int`


---

## config__set_display_dim_timeout()

```c
bruce_result_t config__set_display_dim_timeout(int value);
```

Sets the screen dim timeout.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `value` | `int` | New timeout in seconds. |

### Returns

`bruce_result_t`

#### Permissions

- `config`


---

## config__get_display_brightness()

```c
int config__get_display_brightness(void);
```

Display brightness (0-100). @permission config

### Returns

`int`


---

## config__set_display_brightness()

```c
bruce_result_t config__set_display_brightness(int value);
```

Sets the display brightness.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `value` | `int` | New brightness (0-100). |

### Returns

`bruce_result_t`

#### Permissions

- `config`


---

## config__get_time_automatic_update_via_ntp()

```c
bool config__get_time_automatic_update_via_ntp(void);
```

Whether the clock automatically syncs via NTP. @permission config

### Returns

`bool`


---

## config__set_time_automatic_update_via_ntp()

```c
bruce_result_t config__set_time_automatic_update_via_ntp(bool value);
```

Sets whether the clock automatically syncs via NTP.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `value` | `bool` | New setting. |

### Returns

`bruce_result_t`

#### Permissions

- `config`


---

## config__get_time_timezone()

```c
float config__get_time_timezone(void);
```

UTC offset in hours (may be fractional). @permission config

### Returns

`float`


---

## config__set_time_timezone()

```c
bruce_result_t config__set_time_timezone(float value);
```

Sets the UTC offset.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `value` | `float` | New UTC offset in hours (may be fractional). |

### Returns

`bruce_result_t`

#### Permissions

- `config`


---

## config__get_time_dst()

```c
bool config__get_time_dst(void);
```

Whether the manual one-hour DST adjustment is applied. @permission config

### Returns

`bool`


---

## config__set_time_dst()

```c
bruce_result_t config__set_time_dst(bool value);
```

Sets the manual one-hour DST adjustment.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `value` | `bool` | New setting. |

### Returns

`bruce_result_t`

#### Permissions

- `config`


---

## config__get_time_clock24hr()

```c
bool config__get_time_clock24hr(void);
```

Whether times are displayed in 24-hour format. @permission config

### Returns

`bool`


---

## config__set_time_clock24hr()

```c
bruce_result_t config__set_time_clock24hr(bool value);
```

Sets whether times are displayed in 24-hour format.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `value` | `bool` | New setting. |

### Returns

`bruce_result_t`

#### Permissions

- `config`


---

## config__get_sound_enabled()

```c
bool config__get_sound_enabled(void);
```

Whether sound is enabled. @permission config

### Returns

`bool`


---

## config__set_sound_enabled()

```c
bruce_result_t config__set_sound_enabled(bool value);
```

Sets whether sound is enabled.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `value` | `bool` | New setting. |

### Returns

`bruce_result_t`

#### Permissions

- `config`


---

## config__get_sound_volume()

```c
int config__get_sound_volume(void);
```

Sound volume (0-100). @permission config

### Returns

`int`


---

## config__set_sound_volume()

```c
bruce_result_t config__set_sound_volume(int value);
```

Sets the sound volume.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `value` | `int` | New volume (0-100). |

### Returns

`bruce_result_t`

#### Permissions

- `config`


---

## config__get_keyboard_lang()

```c
const char *config__get_keyboard_lang(void);
```

Configured on-screen keyboard language. @permission config

### Returns

`const char *`


---

## config__set_keyboard_lang()

```c
bruce_result_t config__set_keyboard_lang(const char *value);
```

Sets the on-screen keyboard language.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `value` | `const char *` | New keyboard language code. |

### Returns

`bruce_result_t`

#### Permissions

- `config`


---

## config__get_hotkeys()

```c
const bruce_config_hotkeys_t *config__get_hotkeys(void);
```

Configured hotkey chords. @permission config

### Returns

`const bruce_config_hotkeys_t *`


---

## config__set_hotkeys()

```c
bruce_result_t config__set_hotkeys(const bruce_config_hotkey_t *values, size_t count);
```

Replaces the whole hotkey list.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `values` | `const bruce_config_hotkey_t *` | New hotkey entries. |
| `count` | `size_t` | Number of entries in values. |

### Returns

`bruce_result_t`

#### Permissions

- `config`


---

## config__get_led_enabled()

```c
bool config__get_led_enabled(void);
```

Whether the status LED is enabled. @permission config

### Returns

`bool`


---

## config__set_led_enabled()

```c
bruce_result_t config__set_led_enabled(bool value);
```

Sets whether the status LED is enabled.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `value` | `bool` | New setting. |

### Returns

`bruce_result_t`

#### Permissions

- `config`


---

## config__get_led_brightness()

```c
int config__get_led_brightness(void);
```

Status LED brightness (0-100). @permission config

### Returns

`int`


---

## config__set_led_brightness()

```c
bruce_result_t config__set_led_brightness(int value);
```

Sets the status LED brightness.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `value` | `int` | New brightness (0-100). |

### Returns

`bruce_result_t`

#### Permissions

- `config`


---

## config__get_led_color()

```c
uint32_t config__get_led_color(void);
```

Status LED color. @permission config

### Returns

`uint32_t`


---

## config__set_led_color()

```c
bruce_result_t config__set_led_color(uint32_t value);
```

Sets the status LED color.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `value` | `uint32_t` | New color value. |

### Returns

`bruce_result_t`

#### Permissions

- `config`


---

## config__get_led_blink_enabled()

```c
bool config__get_led_blink_enabled(void);
```

Whether the status LED blinks. @permission config

### Returns

`bool`


---

## config__set_led_blink_enabled()

```c
bruce_result_t config__set_led_blink_enabled(bool value);
```

Sets whether the status LED blinks.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `value` | `bool` | New setting. |

### Returns

`bruce_result_t`

#### Permissions

- `config`


---

## config__get_led_effect()

```c
int config__get_led_effect(void);
```

Selected status LED effect. @permission config

### Returns

`int`


---

## config__set_led_effect()

```c
bruce_result_t config__set_led_effect(int value);
```

Sets the status LED effect.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `value` | `int` | New effect identifier. |

### Returns

`bruce_result_t`

#### Permissions

- `config`


---

## config__get_led_effect_speed()

```c
int config__get_led_effect_speed(void);
```

Status LED effect speed. @permission config

### Returns

`int`


---

## config__set_led_effect_speed()

```c
bruce_result_t config__set_led_effect_speed(int value);
```

Sets the status LED effect speed.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `value` | `int` | New effect speed. |

### Returns

`bruce_result_t`

#### Permissions

- `config`


---

## config__get_led_effect_direction()

```c
int config__get_led_effect_direction(void);
```

Status LED effect direction. @permission config

### Returns

`int`


---

## config__set_led_effect_direction()

```c
bruce_result_t config__set_led_effect_direction(int value);
```

Sets the status LED effect direction.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `value` | `int` | New effect direction. |

### Returns

`bruce_result_t`

#### Permissions

- `config`


---

## config__get_startup_apps()

```c
const bruce_config_startup_apps_t *config__get_startup_apps(void);
```

Startup app list.

### Returns

`const bruce_config_startup_apps_t *`

#### Permissions

- `config`


---

## config__set_startup_apps()

```c
bruce_result_t config__set_startup_apps(const char *const *values, size_t count);
```

Replaces the whole startup app list.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `values` | `const char *const *` | New startup app keys. |
| `count` | `size_t` | Number of entries in values. |

### Returns

`bruce_result_t`

#### Permissions

- `config`


---

## config__add_startup_app()

```c
bruce_result_t config__add_startup_app(const char *key);
```

Adds an app to the startup list.

Adding an existing key is a successful no-op.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `key` | `const char *` | App key to add. |

### Returns

`bruce_result_t`

#### Permissions

- `config`


---

## config__remove_startup_app()

```c
bruce_result_t config__remove_startup_app(const char *key);
```

Removes an app from the startup list.

Returns BRUCE_ERR_NOT_FOUND if the key isn't present.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `key` | `const char *` | App key to remove. |

### Returns

`bruce_result_t`

#### Permissions

- `config`


---

## config__get_dev_mode()

```c
bool config__get_dev_mode(void);
```

Developer-mode flag.

### Returns

`bool`

#### Permissions

- `config`


---

## config__set_dev_mode()

```c
bruce_result_t config__set_dev_mode(bool value);
```

Sets the developer-mode flag.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `value` | `bool` | New setting. |

### Returns

`bruce_result_t`

#### Permissions

- `config`


---

# `device.h`

**Battery, power, and device sensors.**

**Constants**

| Name | Value |
|---|---|
| `BRUCE_DEVICE_TOPIC_TOUCH` | `"device.touch"` |
| `BRUCE_DEVICE_TOPIC_BATTERY` | `"device.battery"` |

---

## device__get_battery()

```c
int device__get_battery(void);
```

Returns battery charge from 0 through 100, or a negative BRUCE_ERR_* value.

Analog-only boards estimate charge from battery voltage.

### Returns

`int`


---

## device__restart()

```c
bruce_result_t device__restart(uint32_t delay_ms);
```

Schedules a device restart after delay_ms.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `delay_ms` | `uint32_t` | Delay before restarting, in milliseconds. |

### Returns

`bruce_result_t`

#### Permissions

- `process`


---

## device__power_off()

```c
bruce_result_t device__power_off(uint32_t delay_ms);
```

Schedules device power-off after delay_ms.

Releases a configured power-hold latch, then enters deep sleep. Boards
without a latch enter deep sleep directly.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `delay_ms` | `uint32_t` | Delay before powering off, in milliseconds. |

### Returns

`bruce_result_t`

#### Permissions

- `process`


---

## bruce_device_touch_message_t()

```c
typedef struct {
    bruce_input_action_t action; /* BRUCE_INPUT_PRESS/RELEASE/CHANGE */
    int32_t x;
    int32_t y;
} bruce_device_touch_message_t;
```


---

## bruce_device_battery_message_t()

```c
typedef struct {
    int32_t percent; /* 0-100 */
} bruce_device_battery_message_t;
```


---

# `dialog.h`

**Messages, menus, input dialogs, and text viewers.**

Not permission-gated. Rendering is chosen from the process's launch
context, never by an app-specific renderer.

---

## bruce_dialog_kind_t()

```c
typedef enum {
    BRUCE_DIALOG_INFO,
    BRUCE_DIALOG_SUCCESS,
    BRUCE_DIALOG_WARNING,
    BRUCE_DIALOG_ERROR,
} bruce_dialog_kind_t;
```


---

## bruce_dialog_choice_t()

```c
typedef struct {
    const char *label;      /* Primary row text. */
    const char *value;      /* Stable action/identifier for the choice. */
    const char *icon_name;  /* Optional built-in icon name, drawn on GUI. */
    const char *right_text; /* Optional short GUI/terminal status text. */
} bruce_dialog_choice_t;
```


---

## bruce_dialog_render_callback_t()

```c
typedef void (*bruce_dialog_render_callback_t)(void *context);
```


---

## bruce_dialog_render_params_t()

```c
typedef struct {
    int padding_top;
    int padding_right;
    int padding_bottom;
    int padding_left;
    bool render_borders;
    int text_size;
    uint16_t background_color;
    uint16_t text_color;
    uint32_t refresh_interval_ms;
    bruce_dialog_render_callback_t render_callback;
    void *render_callback_context;
    bool render_launcher;
    /* Extra vertical space between the title/message block and the first
     * list row, e.g. so a selected first row doesn't visually fuse with the
     * title bar above it. 0 (the default) draws the list flush against
     * them, matching every existing caller. */
    int list_gap;
    /* When true, a SELECT press held for at least DIALOG__LONG_PRESS_MS
     * before release resolves as a long press instead of an immediate
     * selection - dialog__choice_ex()/dialog__pick_file_ex() still return
     * BRUCE_OK with the highlighted row in *out_selected either way, but
     * report which one happened through `out_long_press` below. False (the
     * default) keeps every existing caller returning the instant a SELECT
     * press arrives, exactly as before this field existed. GUI only;
     * ignored on a terminal/non-GUI dialog. */
    bool long_press_enabled;
    /* Set to true/false on every long_press_enabled selection, reporting
     * whether it was a long press. May be NULL if the caller only set
     * long_press_enabled to change dialog__pick_file_ex()'s folder
     * behavior below without needing to know afterward which kind of
     * press it was. Left untouched when long_press_enabled is false. */
    bool *out_long_press;
    /* Set when the returned long-press selection was the synthetic `[..]`
     * row rather than a real file or directory. The returned path is the
     * directory that was being displayed. */
    bool *out_parent_entry;
} bruce_dialog_render_params_t;
```


---

## dialog__message()

```c
bruce_result_t dialog__message(bruce_dialog_kind_t kind, const char *title, const char *message);
```

Shows a simple modal message dialog.

Dialog APIs return BRUCE_OK or BRUCE_ERR_CANCELLED, BRUCE_ERR_BUSY,
BRUCE_ERR_INVALID_ARGUMENT, or another BRUCE_ERR_* result.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `kind` | `bruce_dialog_kind_t` | Message severity/style. |
| `title` | `const char *` | Optional short title shown at the top of the dialog. |
| `message` | `const char *` | Body text shown in the dialog. |

### Returns

`bruce_result_t`


---

## dialog__choice()

```c
bruce_result_t dialog__choice(
    const char *title, const char *message, const bruce_dialog_choice_t *choices, size_t choice_count,
    size_t *out_selected
);
```

Shows a choice-list dialog and waits for a selection.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `title` | `const char *` | Optional short title shown at the top of the dialog. |
| `message` | `const char *` | Optional body text shown above the choice list. |
| `choices` | `const bruce_dialog_choice_t *` | Choices to list. |
| `choice_count` | `size_t` | Number of entries in choices. |
| `out_selected` | `size_t *` | Receives the index of the selected choice. |

### Returns

`bruce_result_t`


---

## dialog__choice_launcher()

```c
bruce_result_t dialog__choice_launcher(
    const char *title, const char *message, const bruce_dialog_choice_t *choices, size_t choice_count,
    size_t *out_selected
);
```

Like dialog__choice(), styled for use from the launcher.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `title` | `const char *` | Optional short title shown at the top of the dialog. |
| `message` | `const char *` | Optional body text shown above the choice list. |
| `choices` | `const bruce_dialog_choice_t *` | Choices to list. |
| `choice_count` | `size_t` | Number of entries in choices. |
| `out_selected` | `size_t *` | Receives the index of the selected choice. |

### Returns

`bruce_result_t`


---

## dialog__choice_ex()

```c
bruce_result_t dialog__choice_ex(
    const char *title, const char *message, const bruce_dialog_choice_t *choices, size_t choice_count,
    size_t *out_selected, const bruce_dialog_render_params_t *render_params
);
```

Choice dialog with GUI render styling.

`render_params` only affects GUI rendering. NULL uses the full display
with the standard title and footer bars. Padding limits the choice
viewport, and `render_borders=false` renders a plain size-1 title without
those bars. `text_size` and the colors style the choices; selected rows
invert the two colors. When `render_callback` is set, it runs inside each
active dialog frame after the choice viewport is drawn.
`refresh_interval_ms` requests periodic frames so caller-owned pixels
outside the viewport can update.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `title` | `const char *` | Optional short title shown at the top of the dialog. |
| `message` | `const char *` | Optional body text shown above the choice list. |
| `choices` | `const bruce_dialog_choice_t *` | Choices to list. |
| `choice_count` | `size_t` | Number of entries in choices. |
| `out_selected` | `size_t *` | Receives the index of the selected choice. |
| `render_params` | `const bruce_dialog_render_params_t *` | GUI render styling, or NULL for the standard look. |

### Returns

`bruce_result_t`


---

## dialog__pick_file()

```c
bruce_result_t dialog__pick_file(
    const char *initial_path, const char *extension_filter, char *out_path, size_t out_path_size,
    const char *title
);
```

Opens a file/directory picker.

`title`, if non-NULL/non-empty, is shown in the GUI picker's top bar
ahead of the current directory, e.g. "NES - /" or "Filemanager - /roms"
(NULL/"" shows just the directory); the browsed directory no longer gets
its own subtitle line underneath. Ignored on non-GUI/terminal picks.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `initial_path` | `const char *` | Directory the picker starts browsing from. |
| `extension_filter` | `const char *` | Optional filter limiting which file extensions are shown. |
| `out_path` | `char *` | Receives the picked file/directory path. |
| `out_path_size` | `size_t` | Size of out_path in bytes. |
| `title` | `const char *` | Optional short title shown ahead of the current directory in the GUI picker. |

### Returns

`bruce_result_t`


---

## dialog__pick_file_ex()

```c
bruce_result_t dialog__pick_file_ex(
    const char *initial_path, const char *extension_filter, char *out_path, size_t out_path_size,
    const char *title, const bruce_dialog_render_params_t *render_params
);
```

Like dialog__pick_file(), but with GUI render styling.

`render_params` additionally styles the GUI listing the same way it
styles dialog__choice() (NULL behaves exactly like dialog__pick_file())
- its `render_callback`/`render_callback_context` are reserved for the
picker's own use (it draws the current volume's name and used/total
space in the bottom bar) and are overridden if set. Ignored on
non-GUI/terminal picks.

`render_params->long_press_enabled` additionally changes what a long
press on a *directory* row does: instead of descending into it, the
picker returns immediately with that directory's own path in `out_path`
(still BRUCE_OK, `*render_params->out_long_press` true) - the same way a
short press on a file returns that file's path - so a caller can offer
folder-level actions (rename/delete/info, ...) the way it already can
for a picked file. A short press on a directory still descends into it
exactly as before, long_press_enabled or not. A long press on the
synthetic `[..]` row returns the directory currently being displayed and
sets `*render_params->out_parent_entry`; a short press navigates upward,
or returns BRUCE_ERR_CANCELLED when used at root.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `initial_path` | `const char *` | Directory the picker starts browsing from. |
| `extension_filter` | `const char *` | Optional filter limiting which file extensions are shown. |
| `out_path` | `char *` | Receives the picked file/directory path. |
| `out_path_size` | `size_t` | Size of out_path in bytes. |
| `title` | `const char *` | Optional short title shown ahead of the current directory in the GUI picker. |
| `render_params` | `const bruce_dialog_render_params_t *` | GUI render styling, or NULL for dialog__pick_file()'s look. |

### Returns

`bruce_result_t`


---

## bruce_dialog_window_draw_fn_t()

```c
typedef void (*bruce_dialog_window_draw_fn_t)(void *context);
```

Draws the frame around a choice dialog.

(rounded border, live status bar, ...) drawn when render_params has
`window_chrome=true`. Core doesn't own that look - the launcher registers
it once at GUI startup via dialog__set_window_renderer(), and every
foreground GUI process sharing the display then gets the same window by
requesting window_chrome. If nothing is registered, window_chrome is
ignored and the normal full-screen layout is used instead.


---

## bruce_dialog_window_renderer_t()

```c
typedef struct {
    /* Space to reserve around the choice viewport for the border/status bar. */
    int padding_top;
    int padding_right;
    int padding_bottom;
    int padding_left;
    /* Font size window-chrome content should use; <= 0 falls back to 1. */
    int text_size;
    /* Paints the full window frame. Called once per dialog__choice() call,
     * before the choice viewport is first drawn, inside an active frame. */
    bruce_dialog_window_draw_fn_t draw_border;
    /* Repaints just the live parts (e.g. clock/battery). Called right after
     * draw_border and then again every status_refresh_interval_ms while the
     * dialog stays open. May be NULL. */
    bruce_dialog_window_draw_fn_t draw_status;
    uint32_t status_refresh_interval_ms;
} bruce_dialog_window_renderer_t;
```


---

## dialog__set_window_renderer()

```c
void dialog__set_window_renderer(const bruce_dialog_window_renderer_t *renderer, void *context);
```

Registers the renderer dialog__choice() uses for window_chrome requests.

The struct is copied, so it may be stack-allocated. Pass NULL to clear a
previously registered renderer.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `renderer` | `const bruce_dialog_window_renderer_t *` | Window-chrome renderer to register, or NULL to clear it. |
| `context` | `void *` | Opaque pointer passed back to the renderer's draw callbacks. |

### Returns

`void`


---

## dialog__default_render_params()

```c
bruce_dialog_render_params_t dialog__default_render_params(int text_size);
```

Returns the render_params dialog__choice() uses when `render_params` is NULL.

(full screen, bordered title/footer bars, themed colors), with
`text_size` substituted (falls back to the normal default when <= 0).
Useful for a caller that wants that standard look at a different font
size.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `text_size` | `int` | Font size to substitute into the returned params. |

### Returns

`bruce_dialog_render_params_t`


---

## dialog__text_input()

```c
bruce_result_t dialog__text_input(
    const char *title, const char *prompt, const char *initial_text, bool mask_input, char *buffer,
    size_t buffer_size
);
```

Text input dialog accepting printable ASCII characters and space.

Fills the caller-owned buffer with the entered string (NUL-terminated,
truncated to buffer_size - 1), and returns BRUCE_OK or
BRUCE_ERR_CANCELLED.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `title` | `const char *` | Optional short title shown at the top of the dialog. |
| `prompt` | `const char *` | Optional short prompt shown at the top of the dialog. |
| `initial_text` | `const char *` | Optional pre-filled text, or NULL. |
| `mask_input` | `bool` | Hides typed characters on GUI (useful for passwords) and disables terminal echo on CLI. |
| `buffer` | `char *` | Caller-owned buffer to receive the entered text. |
| `buffer_size` | `size_t` | Size of buffer in bytes. |

### Returns

`bruce_result_t`


---

## dialog__hex_input()

```c
bruce_result_t dialog__hex_input(
    const char *title, const char *prompt, const char *initial_text, char *buffer, size_t buffer_size
);
```

Text input dialog accepting only 0-9, A-F, a-f.

Fills the caller-owned buffer with the entered string (NUL-terminated,
truncated to buffer_size - 1), and returns BRUCE_OK or
BRUCE_ERR_CANCELLED.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `title` | `const char *` | Optional short title shown at the top of the dialog. |
| `prompt` | `const char *` | Optional short prompt shown at the top of the dialog. |
| `initial_text` | `const char *` | Optional pre-filled text, or NULL. |
| `buffer` | `char *` | Caller-owned buffer to receive the entered text. |
| `buffer_size` | `size_t` | Size of buffer in bytes. |

### Returns

`bruce_result_t`


---

## dialog__number_input()

```c
bruce_result_t dialog__number_input(
    const char *title, const char *prompt, const char *initial_text, char *buffer, size_t buffer_size
);
```

Text input dialog accepting only 0-9 and at most one '.' or '-'.

Fills the caller-owned buffer with the entered string (NUL-terminated,
truncated to buffer_size - 1), and returns BRUCE_OK or
BRUCE_ERR_CANCELLED.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `title` | `const char *` | Optional short title shown at the top of the dialog. |
| `prompt` | `const char *` | Optional short prompt shown at the top of the dialog. |
| `initial_text` | `const char *` | Optional pre-filled text, or NULL. |
| `buffer` | `char *` | Caller-owned buffer to receive the entered text. |
| `buffer_size` | `size_t` | Size of buffer in bytes. |

### Returns

`bruce_result_t`


---

## dialog__create_text_viewer()

```c
bruce_result_t dialog__create_text_viewer(const char *title, const char *text, bruce_viewer_id_t *out_viewer);
```

Opens a scrollable text viewer dialog.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `title` | `const char *` | Optional short title shown at the top of the viewer. |
| `text` | `const char *` | Text to display. |
| `out_viewer` | `bruce_viewer_id_t *` | Receives the new viewer's id. |

### Returns

`bruce_result_t`


---

## dialog__viewer_set_text()

```c
bruce_result_t dialog__viewer_set_text(bruce_viewer_id_t viewer, const char *text);
```

Replaces the text shown in an open viewer.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `viewer` | `bruce_viewer_id_t` | Viewer to update. |
| `text` | `const char *` | New text to display. |

### Returns

`bruce_result_t`


---

## dialog__viewer_set_text_size()

```c
bruce_result_t dialog__viewer_set_text_size(bruce_viewer_id_t viewer, int text_size);
```

Sets the GUI body font multiplier (1..8) and redraws the viewer.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `viewer` | `bruce_viewer_id_t` | Viewer to update. |
| `text_size` | `int` | New font size multiplier (1..8). |

### Returns

`bruce_result_t`


---

## dialog__viewer_scroll()

```c
bruce_result_t dialog__viewer_scroll(bruce_viewer_id_t viewer, int lines);
```

Scrolls an open viewer by a number of lines.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `viewer` | `bruce_viewer_id_t` | Viewer to scroll. |
| `lines` | `int` | Number of lines to scroll by (negative scrolls up). |

### Returns

`bruce_result_t`


---

## dialog__viewer_close()

```c
bruce_result_t dialog__viewer_close(bruce_viewer_id_t viewer);
```

Closes an open text viewer.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `viewer` | `bruce_viewer_id_t` | Viewer to close. |

### Returns

`bruce_result_t`


---

# `disk.h`

**Disk and partition access.**

**Constants**

| Name | Value |
|---|---|
| `BRUCE_DISK_NAME_MAX` | `17` |
| `BRUCE_DISK_MOUNT_POINT_MAX` | `16` |

---

## bruce_disk_type_t()

```c
typedef enum {
    BRUCE_DISK_TYPE_DISK,
    BRUCE_DISK_TYPE_PARTITION,
} bruce_disk_type_t;
```


---

## bruce_disk_entry_t()

```c
typedef struct {
    char name[BRUCE_DISK_NAME_MAX];
    char parent[BRUCE_DISK_NAME_MAX];
    char mount_point[BRUCE_DISK_MOUNT_POINT_MAX];
    bruce_disk_type_t type;
    uint64_t offset;
    uint64_t size;
    bool removable;
    bool read_only;
} bruce_disk_entry_t;
```


---

## disk__list()

```c
bruce_result_t disk__list(bruce_disk_entry_t *entries, size_t capacity, size_t *out_count);
```

Lists physical block devices and their partitions.

Entries are ordered with each disk before its partitions. Pass NULL with
capacity 0 to query count.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `entries` | `bruce_disk_entry_t *` | Array to receive disk/partition entries, or NULL to only query count. |
| `capacity` | `size_t` | Number of entries the entries array can hold. |
| `out_count` | `size_t *` | Receives the total number of disks/partitions available. |

### Returns

`bruce_result_t`


---

## disk__mount()

```c
bruce_result_t disk__mount(const char *name, const char *mount_point);
```

Mounts a disk/partition.

`name` "sd0" always mounts at "/sdcard"; any other name is looked up
among core/partition_manager's extra partitions (the "bparted" command)
and mounted at the given path, which must be an absolute path other than
"/" or "/sdcard".

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `name` | `const char *` | Disk/partition name to mount, e.g. "sd0". |
| `mount_point` | `const char *` | Absolute path to mount at (ignored for "sd0", which always mounts at "/sdcard"). |

### Returns

`bruce_result_t`

#### Permissions

- _Built-in apps only -- external ELF/JS/WASM callers are always denied._


---

## disk__unmount()

```c
bruce_result_t disk__unmount(const char *name_or_mount_point);
```

Unmounts a disk/partition.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `name_or_mount_point` | `const char *` | Disk/partition name or its current mount point. |

### Returns

`bruce_result_t`

#### Permissions

- _Built-in apps only -- external ELF/JS/WASM callers are always denied._


---

# `display.h`

**Screen drawing and display settings.**

This is the Core-side implementation of the JavaScript `display.*`
surface used by the bjs_interpreter. It is intentionally small and
immediate-mode: buffered drawing updates a Core-owned framebuffer and is
sent to the LCD when a frame is presented; direct drawing is streamed to
the panel immediately. GUI processes render in process-local coordinates
into a Core-assigned fullscreen or tiled viewport. Hidden processes see a
zero-sized viewport and drawing is a successful no-op.

All coordinates are logical and respect the current rotation set by
display__set_rotation(). The framebuffer is stored in the logical
orientation for the current rotation; the ST7789 controller maps it to
the physical panel via esp_lcd_panel_swap_xy() / esp_lcd_panel_mirror().
The default rotation is set per board by
CONFIG_BRUCE_DISPLAY_DEFAULT_ROTATION (see src/Kconfig.projbuild and
boards/<board>/sdkconfig.defaults).

Not permission-gated.

**Constants**

| Name | Value |
|---|---|
| `BRUCE_COLOR_BLACK` | `0x0000` |
| `BRUCE_COLOR_NAVY` | `0x000F` |
| `BRUCE_COLOR_DARKGREEN` | `0x03E0` |
| `BRUCE_COLOR_DARKCYAN` | `0x03EF` |
| `BRUCE_COLOR_MAROON` | `0x7800` |
| `BRUCE_COLOR_PURPLE` | `0x780F` |
| `BRUCE_COLOR_OLIVE` | `0x7BE0` |
| `BRUCE_COLOR_LIGHTGREY` | `0xC618` |
| `BRUCE_COLOR_DARKGREY` | `0x7BEF` |
| `BRUCE_COLOR_BLUE` | `0x001F` |
| `BRUCE_COLOR_GREEN` | `0x07E0` |
| `BRUCE_COLOR_CYAN` | `0x07FF` |
| `BRUCE_COLOR_RED` | `0xF800` |
| `BRUCE_COLOR_MAGENTA` | `0xF81F` |
| `BRUCE_COLOR_YELLOW` | `0xFFE0` |
| `BRUCE_COLOR_WHITE` | `0xFFFF` |
| `BRUCE_COLOR_ORANGE` | `0xFD20` |
| `BRUCE_COLOR_TRANSPARENT` | `0x10000` |
| `BRUCE_DISPLAY_MAX_TILES` | `4` |
| `BRUCE_DISPLAY_MAX_OVERLAYS` | `8` |
| `BRUCE_DISPLAY_OVERLAY_ID_INVALID` | `((bruce_display_overlay_id_t)0)` |

---

## bruce_display_color_t()

```c
typedef uint16_t bruce_display_color_t;
```


---

## bruce_display_rect_t()

```c
typedef struct {
    int16_t x;
    int16_t y;
    int16_t width;
    int16_t height;
} bruce_display_rect_t;
```


---

## bruce_display_tile_t()

```c
typedef struct {
    bruce_process_id_t process_id;
    bruce_display_rect_t rect;
} bruce_display_tile_t;
```


---

## display__width()

```c
int display__width(void);
```

Width of the caller's current viewport, or zero while hidden.

### Returns

`int`


---

## display__height()

```c
int display__height(void);
```

Height of the caller's current viewport, or zero while hidden.

### Returns

`int`


---

## display__screen_width()

```c
int display__screen_width(void);
```

Full physical screen width, independent of the caller's own viewport.

(which may be smaller, tiled, or zero while hidden/background). Meant for
positioning an overlay (see "Overlays" below): an overlay owner commonly
has no normal viewport of its own to measure against. Zero before
display__init().

### Returns

`int`


---

## display__screen_height()

```c
int display__screen_height(void);
```

Full physical screen height, independent of the caller's own viewport.

(which may be smaller, tiled, or zero while hidden/background). Meant for
positioning an overlay (see "Overlays" below): an overlay owner commonly
has no normal viewport of its own to measure against. Zero before
display__init().

### Returns

`int`


---

## display__color565()

```c
bruce_display_color_t display__color565(uint8_t r, uint8_t g, uint8_t b);
```

Convert 8-bit RGB components to an RGB565 color value.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `r` | `uint8_t` | Red component, 0-255. |
| `g` | `uint8_t` | Green component, 0-255. |
| `b` | `uint8_t` | Blue component, 0-255. |

### Returns

`bruce_display_color_t`


---

## display__fill_screen()

```c
bruce_result_t display__fill_screen(bruce_display_color_t color);
```

Fill the entire logical screen with `color`.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `color` | `bruce_display_color_t` | Fill color. |

### Returns

`bruce_result_t`


---

## display__clear()

```c
bruce_result_t display__clear(void);
```

Fill the screen with the current background color (black by default).

### Returns

`bruce_result_t`


---

## display__set_text_color()

```c
bruce_result_t display__set_text_color(bruce_display_color_t color);
```

Set the foreground color used by subsequent text and bitmap drawing.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `color` | `bruce_display_color_t` | New foreground color. |

### Returns

`bruce_result_t`


---

## display__set_text_bg_color()

```c
bruce_result_t display__set_text_bg_color(uint32_t color);
```

Set the background color used for erasing behind text.

Pass a value >= 0x10000 (e.g. BRUCE_COLOR_TRANSPARENT) for transparent
background rendering; otherwise the background rectangle is filled. The
parameter is 32-bit so the transparent sentinel fits.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `color` | `uint32_t` | New background color, or a value >= 0x10000 for transparent. |

### Returns

`bruce_result_t`


---

## display__set_text_size()

```c
bruce_result_t display__set_text_size(float size);
```

Text size multiplier (1 = native character cells, 2 = 2x that, ...).

Clamped to 0.1..8. Values below 1 downscale the native glyph bitmap
(nearest-pixel, not antialiased) instead of upscaling it -- since the
built-in font is only 5px wide to start with, legibility drops off fast
below ~0.5 and glyphs may fall back to a solid dot rather than a hollow
box at the smallest sizes. See display__get_font_metrics() for the native
cell size.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `size` | `float` | New text size multiplier (clamped to 0.1..8). |

### Returns

`bruce_result_t`


---

## display__set_cursor()

```c
bruce_result_t display__set_cursor(int16_t x, int16_t y);
```

Position the text cursor in logical coordinates.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `x` | `int16_t` | New cursor X position. |
| `y` | `int16_t` | New cursor Y position. |

### Returns

`bruce_result_t`


---

## display__get_cursor()

```c
bruce_result_t display__get_cursor(int16_t *x, int16_t *y);
```

Read the current text cursor position in logical coordinates.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `x` | `int16_t *` | Receives the cursor X position. |
| `y` | `int16_t *` | Receives the cursor Y position. |

### Returns

`bruce_result_t`


---

## display__print()

```c
bruce_result_t display__print(const char *text);
```

Print a NUL-terminated UTF-8 string at the cursor and advance the cursor.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `text` | `const char *` | Text to print. |

### Returns

`bruce_result_t`


---

## display__println()

```c
bruce_result_t display__println(const char *text);
```

Same as display__print() but appends a newline.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `text` | `const char *` | Text to print. |

### Returns

`bruce_result_t`


---

## display__draw_string()

```c
bruce_result_t display__draw_string(const char *text, int16_t x, int16_t y);
```

Draw a single-line string with (x, y) at its left edge.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `text` | `const char *` | Text to draw. |
| `x` | `int16_t` | Left edge X position. |
| `y` | `int16_t` | Y position. |

### Returns

`bruce_result_t`


---

## display__draw_centre_string()

```c
bruce_result_t display__draw_centre_string(const char *text, int16_t x, int16_t y);
```

Draw a single-line string horizontally centered on x.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `text` | `const char *` | Text to draw. |
| `x` | `int16_t` | X position to center the text on. |
| `y` | `int16_t` | Y position. |

### Returns

`bruce_result_t`


---

## display__draw_right_string()

```c
bruce_result_t display__draw_right_string(const char *text, int16_t x, int16_t y);
```

Draw a single-line string with its right edge at x.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `text` | `const char *` | Text to draw. |
| `x` | `int16_t` | Right edge X position. |
| `y` | `int16_t` | Y position. |

### Returns

`bruce_result_t`


---

## display__get_font_metrics()

```c
bruce_result_t display__get_font_metrics(int16_t *out_char_width, int16_t *out_char_height);
```

The active font's fixed advance width/height, in px, before text_size scaling.

(scale by the value passed to display__set_text_size() to get
on-screen pixels). Every built-in font is monospace today, so this
single width/height pair is exact for any string; callers doing their
own layout math (row heights, column counts, cursor placement) should
use this instead of a hardcoded guess, so they stay correct if the
active font ever changes.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `out_char_width` | `int16_t *` | Receives the character cell width in px. |
| `out_char_height` | `int16_t *` | Receives the character cell height in px. |

### Returns

`bruce_result_t`


---

## display__draw_pixel()

```c
bruce_result_t display__draw_pixel(int16_t x, int16_t y, bruce_display_color_t color);
```

Draw a single pixel.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `x` | `int16_t` | X position. |
| `y` | `int16_t` | Y position. |
| `color` | `bruce_display_color_t` | Pixel color. |

### Returns

`bruce_result_t`


---

## display__draw_line()

```c
bruce_result_t
display__draw_line(int16_t x0, int16_t y0, int16_t x1, int16_t y1, bruce_display_color_t color);
```

Draw a line between two points.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `x0` | `int16_t` | First point's X position. |
| `y0` | `int16_t` | First point's Y position. |
| `x1` | `int16_t` | Second point's X position. |
| `y1` | `int16_t` | Second point's Y position. |
| `color` | `bruce_display_color_t` | Line color. |

### Returns

`bruce_result_t`


---

## display__draw_rect()

```c
bruce_result_t display__draw_rect(int16_t x, int16_t y, int16_t w, int16_t h, bruce_display_color_t color);
```

Draw a rectangle outline.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `x` | `int16_t` | Left edge X position. |
| `y` | `int16_t` | Top edge Y position. |
| `w` | `int16_t` | Width. |
| `h` | `int16_t` | Height. |
| `color` | `bruce_display_color_t` | Outline color. |

### Returns

`bruce_result_t`


---

## display__fill_rect()

```c
bruce_result_t display__fill_rect(int16_t x, int16_t y, int16_t w, int16_t h, bruce_display_color_t color);
```

Draw a filled rectangle.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `x` | `int16_t` | Left edge X position. |
| `y` | `int16_t` | Top edge Y position. |
| `w` | `int16_t` | Width. |
| `h` | `int16_t` | Height. |
| `color` | `bruce_display_color_t` | Fill color. |

### Returns

`bruce_result_t`


---

## display__draw_circle()

```c
bruce_result_t display__draw_circle(int16_t x, int16_t y, int16_t r, bruce_display_color_t color);
```

Draw a circle outline.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `x` | `int16_t` | Center X position. |
| `y` | `int16_t` | Center Y position. |
| `r` | `int16_t` | Radius. |
| `color` | `bruce_display_color_t` | Outline color. |

### Returns

`bruce_result_t`


---

## display__fill_circle()

```c
bruce_result_t display__fill_circle(int16_t x, int16_t y, int16_t r, bruce_display_color_t color);
```

Draw a filled circle.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `x` | `int16_t` | Center X position. |
| `y` | `int16_t` | Center Y position. |
| `r` | `int16_t` | Radius. |
| `color` | `bruce_display_color_t` | Fill color. |

### Returns

`bruce_result_t`


---

## display__draw_arc()

```c
bruce_result_t display__draw_arc(
    int16_t x, int16_t y, int16_t r, int16_t start_angle, int16_t end_angle, bruce_display_color_t color
);
```

Draw a circular arc.

Angles use the legacy Bruce convention: zero degrees is at six o'clock
and values increase clockwise. An end angle less than the start angle
wraps through zero; a range of 360 degrees draws a full circle.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `x` | `int16_t` | Center X position. |
| `y` | `int16_t` | Center Y position. |
| `r` | `int16_t` | Radius. |
| `start_angle` | `int16_t` | Start angle in degrees (0 = six o'clock, clockwise). |
| `end_angle` | `int16_t` | End angle in degrees. |
| `color` | `bruce_display_color_t` | Arc color. |

### Returns

`bruce_result_t`


---

## display__draw_triangle()

```c
bruce_result_t display__draw_triangle(
    int16_t x0, int16_t y0, int16_t x1, int16_t y1, int16_t x2, int16_t y2, bruce_display_color_t color
);
```

Draw a triangle outline.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `x0` | `int16_t` | First vertex X position. |
| `y0` | `int16_t` | First vertex Y position. |
| `x1` | `int16_t` | Second vertex X position. |
| `y1` | `int16_t` | Second vertex Y position. |
| `x2` | `int16_t` | Third vertex X position. |
| `y2` | `int16_t` | Third vertex Y position. |
| `color` | `bruce_display_color_t` | Outline color. |

### Returns

`bruce_result_t`


---

## display__fill_triangle()

```c
bruce_result_t display__fill_triangle(
    int16_t x0, int16_t y0, int16_t x1, int16_t y1, int16_t x2, int16_t y2, bruce_display_color_t color
);
```

Draw a filled triangle.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `x0` | `int16_t` | First vertex X position. |
| `y0` | `int16_t` | First vertex Y position. |
| `x1` | `int16_t` | Second vertex X position. |
| `y1` | `int16_t` | Second vertex Y position. |
| `x2` | `int16_t` | Third vertex X position. |
| `y2` | `int16_t` | Third vertex Y position. |
| `color` | `bruce_display_color_t` | Fill color. |

### Returns

`bruce_result_t`


---

## display__draw_round_rect()

```c
bruce_result_t
display__draw_round_rect(int16_t x, int16_t y, int16_t w, int16_t h, int16_t r, bruce_display_color_t color);
```

Draw a rounded-rectangle outline.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `x` | `int16_t` | Left edge X position. |
| `y` | `int16_t` | Top edge Y position. |
| `w` | `int16_t` | Width. |
| `h` | `int16_t` | Height. |
| `r` | `int16_t` | Corner radius. |
| `color` | `bruce_display_color_t` | Outline color. |

### Returns

`bruce_result_t`


---

## display__fill_round_rect()

```c
bruce_result_t
display__fill_round_rect(int16_t x, int16_t y, int16_t w, int16_t h, int16_t r, bruce_display_color_t color);
```

Draw a filled rounded rectangle.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `x` | `int16_t` | Left edge X position. |
| `y` | `int16_t` | Top edge Y position. |
| `w` | `int16_t` | Width. |
| `h` | `int16_t` | Height. |
| `r` | `int16_t` | Corner radius. |
| `color` | `bruce_display_color_t` | Fill color. |

### Returns

`bruce_result_t`


---

## display__draw_bitmap()

```c
bruce_result_t display__draw_bitmap(
    int16_t x, int16_t y, const uint8_t *bitmap, int16_t w, int16_t h, bruce_display_color_t color
);
```

Draw a 1bpp MSB-first bitmap.

Each row is byte-aligned; a set bit draws `color`, a clear bit draws the
current background color (or is skipped when the background is
transparent).

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `x` | `int16_t` | Left edge X position. |
| `y` | `int16_t` | Top edge Y position. |
| `bitmap` | `const uint8_t *` | 1bpp MSB-first, byte-aligned-per-row bitmap data. |
| `w` | `int16_t` | Bitmap width in pixels. |
| `h` | `int16_t` | Bitmap height in pixels. |
| `color` | `bruce_display_color_t` | Color drawn for each set bit. |

### Returns

`bruce_result_t`


---

## display__draw_xbitmap()

```c
bruce_result_t display__draw_xbitmap(
    int16_t x, int16_t y, const uint8_t *bitmap, int16_t w, int16_t h, bruce_display_color_t color
);
```

Draw a 1bpp XBM bitmap.

Each row is byte-aligned; a set bit draws `color`, a clear bit is
transparent.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `x` | `int16_t` | Left edge X position. |
| `y` | `int16_t` | Top edge Y position. |
| `bitmap` | `const uint8_t *` | 1bpp XBM, byte-aligned-per-row bitmap data. |
| `w` | `int16_t` | Bitmap width in pixels. |
| `h` | `int16_t` | Bitmap height in pixels. |
| `color` | `bruce_display_color_t` | Color drawn for each set bit. |

### Returns

`bruce_result_t`


---

## display__draw_rgb_bitmap()

```c
bruce_result_t display__draw_rgb_bitmap(int16_t x, int16_t y, const uint16_t *bitmap, int16_t w, int16_t h);
```

Draw a raw RGB565 bitmap from `bitmap` (width * height * 2 bytes).

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `x` | `int16_t` | Left edge X position. |
| `y` | `int16_t` | Top edge Y position. |
| `bitmap` | `const uint16_t *` | Raw RGB565 pixel data, width * height * 2 bytes. |
| `w` | `int16_t` | Bitmap width in pixels. |
| `h` | `int16_t` | Bitmap height in pixels. |

### Returns

`bruce_result_t`


---

## display__draw_bitmap_scaled()

```c
bruce_result_t display__draw_bitmap_scaled(
    int16_t x, int16_t y, const uint8_t *bitmap, int16_t w, int16_t h, int16_t dw, int16_t dh,
    bruce_display_color_t color
);
```

Draw a 1bpp MSB-first bitmap of `w` x `h` pixels scaled to `dw` x `dh`.

Using nearest-neighbor sampling. A set bit draws `color`; a clear bit is
transparent. This is the fast path for built-in icons (see icon.h): pure
integer math, no parsing, no extra RAM.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `x` | `int16_t` | Left edge X position. |
| `y` | `int16_t` | Top edge Y position. |
| `bitmap` | `const uint8_t *` | 1bpp MSB-first, byte-aligned-per-row bitmap data. |
| `w` | `int16_t` | Source bitmap width in pixels. |
| `h` | `int16_t` | Source bitmap height in pixels. |
| `dw` | `int16_t` | Destination (scaled) width in pixels. |
| `dh` | `int16_t` | Destination (scaled) height in pixels. |
| `color` | `bruce_display_color_t` | Color drawn for each set bit. |

### Returns

`bruce_result_t`


---

## display__set_rotation()

```c
bruce_result_t display__set_rotation(uint8_t rotation);
```

Set rotation: 0 = native, 1 = 90deg clockwise, 2 = 180, 3 = 270.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `rotation` | `uint8_t` | New rotation (0..3). |

### Returns

`bruce_result_t`


---

## display__get_rotation()

```c
uint8_t display__get_rotation(void);
```

Return the current rotation (0..3).

### Returns

`uint8_t`


---

## display__snapshot()

```c
bruce_result_t display__snapshot(
    uint16_t *pixels,
    size_t capacity,
    uint16_t *out_width,
    uint16_t *out_height,
    size_t *out_pixel_count
);
```

Copies the composed RGB565 framebuffer for remote display tools.

Pass NULL with capacity 0 to query dimensions and required pixel count.
Returns BRUCE_ERR_UNSUPPORTED when buffered rendering is disabled.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `pixels` | `uint16_t *` | Buffer to receive RGB565 pixel data, or NULL to only query dimensions. |
| `capacity` | `size_t` | Number of pixels the pixels buffer can hold. |
| `out_width` | `uint16_t *` | Receives the framebuffer width in pixels. |
| `out_height` | `uint16_t *` | Receives the framebuffer height in pixels. |
| `out_pixel_count` | `size_t *` | Receives the total pixel count required. |

### Returns

`bruce_result_t`


---

## display__invert_display()

```c
bruce_result_t display__invert_display(bool invert);
```

Invert the panel colors (independent of color_inverted config).

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `invert` | `bool` | True to invert, false to restore normal colors. |

### Returns

`bruce_result_t`


---

## display__set_brightness()

```c
bruce_result_t display__set_brightness(uint8_t brightness);
```

Set backlight brightness, 0..255. Persists via config__set_display_brightness().

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `brightness` | `uint8_t` | New backlight brightness (0..255). |

### Returns

`bruce_result_t`


---

## display__get_brightness()

```c
uint8_t display__get_brightness(void);
```

Return the current backlight brightness, 0..255.

### Returns

`uint8_t`


---

## display__display_on_off()

```c
bruce_result_t display__display_on_off(bool on);
```

Turn the panel display on or off (backlight is controlled separately).

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `on` | `bool` | True to turn the panel on, false to turn it off. |

### Returns

`bruce_result_t`


---

## bruce_display_render_mode_t()

```c
typedef enum {
    BRUCE_DISPLAY_MODE_BUFFERED_DMA = 0,
    BRUCE_DISPLAY_MODE_BUFFERED,
    BRUCE_DISPLAY_MODE_DIRECT,
} bruce_display_render_mode_t;
```

Display rendering modes, from most capable/RAM-hungry to leanest.

BRUCE_DISPLAY_MODE_BUFFERED_DMA is the default: drawing goes to an
off-screen framebuffer allocated in DMA-capable RAM. BRUCE_DISPLAY_MODE_BUFFERED
keeps the off-screen framebuffer but drops its DMA capability (see the
"DMA Buf" setting in Display & UI settings). BRUCE_DISPLAY_MODE_DIRECT drops
the framebuffer entirely: draw calls go straight to the panel, freeing the
framebuffer's RAM at the cost of visible partial-frame updates while
drawing (see display__request_render_mode()).


---

## display__request_render_mode()

```c
bruce_result_t display__request_render_mode(bruce_display_render_mode_t mode);
```

Requests the display be at `mode` (or a leaner one another caller also wants) for as long as the calling process needs it.

Entirely at runtime, no reboot required. Meant to be called by a
memory-hungry ELF/JS app (an emulator, a game with a large asset set,
...) right before it starts allocating, to hand back the RAM a
buffered/DMA framebuffer would otherwise hold for the whole time the app
runs -- or use memory__reclaim(), which calls this on your behalf and
only if it's actually enough to cover what you're about to allocate.

Each process may hold at most one request at a time; calling this again
replaces the calling process's own request. The display's actual mode is
always the leanest one requested by any live caller, so one process
asking for BRUCE_DISPLAY_MODE_DIRECT is never undone by another asking for
BRUCE_DISPLAY_MODE_BUFFERED. Requesting BRUCE_DISPLAY_MODE_BUFFERED_DMA
releases the calling process's request (equivalent to
display__release_render_mode()). A request is released automatically if
the requesting process exits or is killed without releasing it itself.

Fails with BRUCE_ERR_BUSY while any process has an active (begun but not
yet presented) frame.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `mode` | `bruce_display_render_mode_t` | Leanest rendering mode the calling process is willing to accept. |

### Returns

`bruce_result_t`


---

## display__release_render_mode()

```c
bruce_result_t display__release_render_mode(void);
```

Releases the calling process's own display__request_render_mode() request, if any.

A no-op if the calling process holds no request. The display's mode drops
back to the leanest one still requested by another live caller, or to
BRUCE_DISPLAY_MODE_BUFFERED_DMA if none remain.

### Returns

`bruce_result_t`


---

## display__buffer_footprint()

```c
size_t display__buffer_footprint(void);
```

Returns how many bytes the display's current rendering buffers are using.

Includes the off-screen framebuffer (buffered modes only) and its
transfer/direct-mode scratch buffer. Falls as rendering mode gets leaner;
see display__request_render_mode(). Zero if the display isn't initialized.

### Returns

`size_t`


---

## display__begin_frame()

```c
bruce_result_t display__begin_frame(void);
```

Lease the caller's current viewport for one complete frame.

In direct mode, drawing is visible before the frame is presented.

### Returns

`bruce_result_t`


---

## display__present()

```c
bruce_result_t display__present(void);
```

Present an active frame and wait until its LCD transfer completes.

Direct mode has no retained frame to transfer, but still completes the
lease and draws an active notification over the caller's viewport.

### Returns

`bruce_result_t`


---

## display__set_tiles()

```c
bruce_result_t display__set_tiles(const bruce_display_tile_t *tiles, size_t count);
```

Built-in launcher layout operation. Not exported to external runtimes.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `tiles` | `const bruce_display_tile_t *` | New tile layout (one rect per process). |
| `count` | `size_t` | Number of entries in tiles. |

### Returns

`bruce_result_t`


---

## bruce_display_overlay_id_t()

```c
typedef uint32_t bruce_display_overlay_id_t;
```


---

## display__overlay_create()

```c
bruce_result_t
display__overlay_create(int16_t x, int16_t y, int16_t w, int16_t h, bruce_display_overlay_id_t *out_overlay);
```

Creates a hidden overlay at `x, y` sized `w` x `h`.

Must fit on the physical screen. Returns BRUCE_ERR_RESOURCE_LIMIT once
BRUCE_DISPLAY_MAX_OVERLAYS are live.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `x` | `int16_t` | Overlay origin X position. |
| `y` | `int16_t` | Overlay origin Y position. |
| `w` | `int16_t` | Overlay width. |
| `h` | `int16_t` | Overlay height. |
| `out_overlay` | `bruce_display_overlay_id_t *` | Receives the new overlay's id. |

### Returns

`bruce_result_t`


---

## display__overlay_destroy()

```c
bruce_result_t display__overlay_destroy(bruce_display_overlay_id_t overlay);
```

Destroys an overlay early. Also happens automatically on process exit.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `overlay` | `bruce_display_overlay_id_t` | Overlay to destroy. |

### Returns

`bruce_result_t`


---

## display__overlay_show()

```c
bruce_result_t display__overlay_show(bruce_display_overlay_id_t overlay);
```

Shows an overlay without destroying it.

Content persists while hidden. Idempotent.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `overlay` | `bruce_display_overlay_id_t` | Overlay to show. |

### Returns

`bruce_result_t`


---

## display__overlay_hide()

```c
bruce_result_t display__overlay_hide(bruce_display_overlay_id_t overlay);
```

Hides an overlay without destroying it.

Content persists while hidden. Idempotent.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `overlay` | `bruce_display_overlay_id_t` | Overlay to hide. |

### Returns

`bruce_result_t`


---

## display__overlay_move()

```c
bruce_result_t display__overlay_move(bruce_display_overlay_id_t overlay, int16_t x, int16_t y);
```

Repositions an overlay's on-screen origin; size is fixed at creation.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `overlay` | `bruce_display_overlay_id_t` | Overlay to move. |
| `x` | `int16_t` | New origin X position. |
| `y` | `int16_t` | New origin Y position. |

### Returns

`bruce_result_t`


---

## display__overlay_begin()

```c
bruce_result_t display__overlay_begin(bruce_display_overlay_id_t overlay);
```

Opens a drawing session on `overlay` for the calling (owning) process.

While open, every display__* draw call below (fill_rect, draw_string,
draw_bitmap, ...) plus display__width()/display__height() target the
overlay's own w x h buffer in overlay-local coordinates instead of the
caller's normal viewport. Sessions do not nest.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `overlay` | `bruce_display_overlay_id_t` | Overlay to begin drawing into. |

### Returns

`bruce_result_t`


---

## display__overlay_end()

```c
bruce_result_t display__overlay_end(bruce_display_overlay_id_t overlay);
```

Closes the drawing session opened by display__overlay_begin().

If the overlay is currently shown, repaints its rect.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `overlay` | `bruce_display_overlay_id_t` | Overlay whose drawing session to close. |

### Returns

`bruce_result_t`


---

# `environment.h`

**Environment variables.**

**Constants**

| Name | Value |
|---|---|
| `BRUCE_ENVIRONMENT_MAX_VARIABLES` | `24` |
| `BRUCE_ENVIRONMENT_NAME_MAX` | `32` |
| `BRUCE_ENVIRONMENT_VALUE_MAX` | `128` |

---

## bruce_environment_variable_t()

```c
typedef struct {
    const char *name;
    const char *value;
} bruce_environment_variable_t;
```


---

## environment__global_get()

```c
const char *environment__global_get(const char *name);
```

Reads a global environment default.

Global values are runtime defaults for processes created after the
change. They are overridden by inherited parent values and launch
assignments. Persistent defaults are loaded from /config/.env during
boot; these setters intentionally do not modify that file.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `name` | `const char *` | Variable name to look up. |

### Returns

`const char *`


---

## environment__global_set()

```c
bruce_result_t environment__global_set(const char *name, const char *value);
```

Sets a global environment default.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `name` | `const char *` | Variable name to set. |
| `value` | `const char *` | New value. |

### Returns

`bruce_result_t`


---

## environment__global_unset()

```c
bruce_result_t environment__global_unset(const char *name);
```

Removes a global environment default.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `name` | `const char *` | Variable name to remove. |

### Returns

`bruce_result_t`


---

## environment__get()

```c
const char *environment__get(const char *name);
```

Reads a variable from the calling process's own environment.

Process environments are runtime-only and inherited as a deep copy when
a child is launched. The returned value is borrowed until the calling
process changes its environment or exits.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `name` | `const char *` | Variable name to look up. |

### Returns

`const char *`


---

## environment__set()

```c
bruce_result_t environment__set(const char *name, const char *value);
```

Sets a variable in the calling process's own environment.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `name` | `const char *` | Variable name to set. |
| `value` | `const char *` | New value. |

### Returns

`bruce_result_t`


---

## environment__unset()

```c
bruce_result_t environment__unset(const char *name);
```

Removes a variable from the calling process's own environment.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `name` | `const char *` | Variable name to remove. |

### Returns

`bruce_result_t`


---

## environment__count()

```c
size_t environment__count(void);
```

Number of variables in the calling process's own environment.

### Returns

`size_t`


---

## environment__get_at()

```c
bruce_result_t environment__get_at(size_t index, const char **out_name, const char **out_value);
```

Reads a name/value pair from the calling process's own environment by index.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `index` | `size_t` | Zero-based index, below environment__count(). |
| `out_name` | `const char **` | Receives the variable's name. |
| `out_value` | `const char **` | Receives the variable's value. |

### Returns

`bruce_result_t`


---

# `ext_mem_loader.h`

**Loader to external memory.**

A loader module turns a file of one specific extension into a running
Core process by registering itself here. Core ships built-in ELF (".elf")
and JavaScript (".js") loader modules, but neither gets any Core access
that a third-party loader module (a ".py" loader, for example)
registering the same way would not also get.

---

## bruce_loader_process_entry_fn()

```c
typedef int (*bruce_loader_process_entry_fn)(void *context);
```


---

## bruce_loader_process_cleanup_fn()

```c
typedef void (*bruce_loader_process_cleanup_fn)(void *context);
```


---

## bruce_loader_process_stop_fn()

```c
typedef void (*bruce_loader_process_stop_fn)(void *context, bruce_process_signal_t signal);
```


---

## bruce_ext_mem_loader_image_t()

```c
typedef struct {
    const uint8_t *data;
    size_t size;
    bruce_memory_backend_t backend;
} bruce_ext_mem_loader_image_t;
```


---

## bruce_ext_mem_loader_xip_image_t()

```c
typedef struct {
    const uint8_t *instruction;
    const uint8_t *data;
    size_t size;
    bruce_memory_object_t memory;
} bruce_ext_mem_loader_xip_image_t;
```


---

## ext_mem_loader__stage_path()

```c
bruce_result_t ext_mem_loader__stage_path(const char *path, bruce_ext_mem_loader_image_t *out_image);
```

Streams a file into process-owned external memory, verifies it, and returns a read-only mapping.

data[size] is a trailing NUL byte outside the image size, allowing
parsers that require sentinel-terminated input to use the mapping
directly. The caller must release successful mappings promptly.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `path` | `const char *` | Path of the file to stream in. |
| `out_image` | `bruce_ext_mem_loader_image_t *` | Receives the read-only mapping. |

### Returns

`bruce_result_t`


---

## ext_mem_loader__adopt_image()

```c
bruce_result_t ext_mem_loader__adopt_image(bruce_ext_mem_loader_image_t *image);
```

Transfers a staged image to the current process after a loader spawns it.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `image` | `bruce_ext_mem_loader_image_t *` | Image staged by ext_mem_loader__stage_path(). |

### Returns

`bruce_result_t`


---

## ext_mem_loader__release_image()

```c
bruce_result_t ext_mem_loader__release_image(bruce_ext_mem_loader_image_t *image);
```

Releases an image staged/adopted via ext_mem_loader__stage_path()/adopt_image().

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `image` | `bruce_ext_mem_loader_image_t *` | Image to release. |

### Returns

`bruce_result_t`


---

## ext_mem_loader__allocate_xip()

```c
bruce_result_t ext_mem_loader__allocate_xip(size_t size, bruce_ext_mem_loader_xip_image_t *out_image);
```

Allocates executable MMU-page-exclusive space in swap.

The mapping remains valid until release; writes are bounds checked and
cache coherent.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `size` | `size_t` | Number of bytes to allocate. |
| `out_image` | `bruce_ext_mem_loader_xip_image_t *` | Receives the allocated XIP image. |

### Returns

`bruce_result_t`


---

## ext_mem_loader__write_xip()

```c
bruce_result_t
ext_mem_loader__write_xip(
    const bruce_ext_mem_loader_xip_image_t *image, size_t offset, const void *data, size_t size
);
```

Writes into a bounds-checked region of an allocated XIP image.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `image` | `const bruce_ext_mem_loader_xip_image_t *` | XIP image allocated via ext_mem_loader__allocate_xip(). |
| `offset` | `size_t` | Byte offset within the image to write at. |
| `data` | `const void *` | Bytes to write. |
| `size` | `size_t` | Number of bytes to write. |

### Returns

`bruce_result_t`


---

## ext_mem_loader__adopt_xip()

```c
bruce_result_t ext_mem_loader__adopt_xip(bruce_ext_mem_loader_xip_image_t *image);
```

Transfers a staged XIP image to the current process after a loader spawns it.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `image` | `bruce_ext_mem_loader_xip_image_t *` | XIP image to adopt. |

### Returns

`bruce_result_t`


---

## ext_mem_loader__release_xip()

```c
bruce_result_t ext_mem_loader__release_xip(bruce_ext_mem_loader_xip_image_t *image);
```

Releases an XIP image allocated/adopted via allocate_xip()/adopt_xip().

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `image` | `bruce_ext_mem_loader_xip_image_t *` | XIP image to release. |

### Returns

`bruce_result_t`


---

## ext_mem_loader__set_error_message()

```c
void ext_mem_loader__set_error_message(const char *message);
```

Sets the loader implementation's own concise diagnostic for the launcher to show.

Shown when their launch attempt fails. The text is cleared by passing
NULL.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `message` | `const char *` | Diagnostic text to retain, or NULL to clear it. |

### Returns

`void`


---

## ext_mem_loader__last_error_message()

```c
const char *ext_mem_loader__last_error_message(void);
```

Reads back the diagnostic set via ext_mem_loader__set_error_message().

### Returns

`const char *`


---

## ext_mem_loader__format_error_message()

```c
void ext_mem_loader__format_error_message(const char *action, int result, char *out_message, size_t out_size);
```

Formats a user-facing launch failure.

ABI mismatches explain that the app needs a newer Bruce version; other
results use the SDK error description.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `action` | `const char *` | Short description of the action that failed, e.g. "launch". |
| `result` | `int` | BRUCE_ERR_* result code to format. |
| `out_message` | `char *` | Buffer to receive the formatted message. |
| `out_size` | `size_t` | Size of out_message in bytes. |

### Returns

`void`


---

## app_runner__register_loader()

```c
bruce_result_t app_runner__register_loader(const char *extension, const char *program);
```

Registers `program` to handle files ending in `extension`.

(which must start with '.', e.g. ".elf"). `program` is resolved through
the normal AppRunner command registry and receives the matched path as
its first argument. Registration order determines named resolution.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `extension` | `const char *` | File extension to register for, including the leading '.'. |
| `program` | `const char *` | Name of the registered AppRunner command that handles it. |

### Returns

`bruce_result_t`


---

## app_runner__run_path()

```c
int app_runner__run_path(const char *path, const char *arg, bruce_launch_mode_t mode);
```

Starts an arbitrary absolute path via whichever loader is registered for its extension.

Used directly by `execute`-permission callers (file managers, etc.) and
internally by app_runner__run()'s named resolution. `path` must be
normalized with no "." or ".." components. Returns a positive
bruce_process_id_t on success; BRUCE_ERR_INVALID_PATH,
BRUCE_ERR_NOT_FOUND (no loader registered for the extension), or the
loader's own negative BRUCE_ERR_* on failure.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `path` | `const char *` | Normalized absolute path of the file to run. |
| `arg` | `const char *` | Shell-style argument text, or NULL/"" for none. |
| `mode` | `bruce_launch_mode_t` | Whether to launch in the foreground or background. |

### Returns

`int`

#### Permissions

- `execute`


---

## app_runner__run_path_with_environment()

```c
int app_runner__run_path_with_environment(
    const char *path, const char *arg, bruce_launch_mode_t mode,
    const bruce_environment_variable_t *environment, size_t environment_count
);
```

Like app_runner__run_path(), with temporary child environment assignments.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `path` | `const char *` | Normalized absolute path of the file to run. |
| `arg` | `const char *` | Shell-style argument text, or NULL/"" for none. |
| `mode` | `bruce_launch_mode_t` | Whether to launch in the foreground or background. |
| `environment` | `const bruce_environment_variable_t *` | Extra environment variables to give the child. |
| `environment_count` | `size_t` | Number of entries in environment. |

### Returns

`int`

#### Permissions

- `execute`


---

## app_runner__spawn_loader_process()

```c
int app_runner__spawn_loader_process(
    const char *permission_key, bool gui_requested, bruce_launch_mode_t mode, uint32_t stack_size,
    const bruce_environment_variable_t *environment, size_t environment_count,
    bruce_loader_process_entry_fn entry, void *context
);
```

Creates and starts a real Core process that calls entry(context) on its own stack.

The one extra public primitive a loader module needs beyond
app_runner__register(): wired into the same foreground stack, resource
registry, and permission checks (`permission_key`, e.g. "game.elf") as
any other process - without any private Core header. `stack_size` of 0
selects a Core default. Returns a positive bruce_process_id_t on success
or a negative BRUCE_ERR_*.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `permission_key` | `const char *` | Permission-check key for the new process, e.g. "game.elf". |
| `gui_requested` | `bool` | Whether the new process should be a GUI process. |
| `mode` | `bruce_launch_mode_t` | Whether to launch in the foreground or background. |
| `stack_size` | `uint32_t` | Stack size in bytes for the new process, or 0 for the Core default. |
| `environment` | `const bruce_environment_variable_t *` | Extra environment variables to give the process. |
| `environment_count` | `size_t` | Number of entries in environment. |
| `entry` | `bruce_loader_process_entry_fn` | Entry point called with context on the process's own stack. |
| `context` | `void *` | Opaque pointer passed to entry. |

### Returns

`int`


---

## app_runner__spawn_loader_process_owned()

```c
int app_runner__spawn_loader_process_owned(
    const char *permission_key, bool gui_requested, bruce_launch_mode_t mode, uint32_t stack_size,
    const bruce_environment_variable_t *environment, size_t environment_count,
    bruce_loader_process_entry_fn entry, void *context, bruce_loader_process_cleanup_fn cleanup
);
```

Variant that transfers ownership of context to Core.

cleanup runs exactly once after normal return, force-kill, or
cancellation before entry starts.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `permission_key` | `const char *` | Permission-check key for the new process, e.g. "game.elf". |
| `gui_requested` | `bool` | Whether the new process should be a GUI process. |
| `mode` | `bruce_launch_mode_t` | Whether to launch in the foreground or background. |
| `stack_size` | `uint32_t` | Stack size in bytes for the new process, or 0 for the Core default. |
| `environment` | `const bruce_environment_variable_t *` | Extra environment variables to give the process. |
| `environment_count` | `size_t` | Number of entries in environment. |
| `entry` | `bruce_loader_process_entry_fn` | Entry point called with context on the process's own stack. |
| `context` | `void *` | Opaque pointer passed to entry and cleanup; ownership transfers to Core. |
| `cleanup` | `bruce_loader_process_cleanup_fn` | Called exactly once to release context. |

### Returns

`int`


---

## app_runner__spawn_loader_process_owned_with_stop()

```c
int app_runner__spawn_loader_process_owned_with_stop(
    const char *permission_key, bool gui_requested, bruce_launch_mode_t mode, uint32_t stack_size,
    const bruce_environment_variable_t *environment, size_t environment_count,
    bruce_loader_process_entry_fn entry, void *context, bruce_loader_process_cleanup_fn cleanup,
    bruce_loader_process_stop_fn stop
);
```

Owned variant for runtimes which must be explicitly interrupted to observe cooperative INT/TERM.

(for example, a compute-bound WebAssembly VM).

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `permission_key` | `const char *` | Permission-check key for the new process, e.g. "game.elf". |
| `gui_requested` | `bool` | Whether the new process should be a GUI process. |
| `mode` | `bruce_launch_mode_t` | Whether to launch in the foreground or background. |
| `stack_size` | `uint32_t` | Stack size in bytes for the new process, or 0 for the Core default. |
| `environment` | `const bruce_environment_variable_t *` | Extra environment variables to give the process. |
| `environment_count` | `size_t` | Number of entries in environment. |
| `entry` | `bruce_loader_process_entry_fn` | Entry point called with context on the process's own stack. |
| `context` | `void *` | Opaque pointer passed to entry, cleanup, and stop; ownership transfers to Core. |
| `cleanup` | `bruce_loader_process_cleanup_fn` | Called exactly once to release context. |
| `stop` | `bruce_loader_process_stop_fn` | Non-blocking hook invoked when INT/TERM is delivered. |

### Returns

`int`


---

# `filetype.h`

**Identifies what a file is: description, MIME type, icon, and which app_runner program opens it.**

Shared by app_runner (choosing a program to run a path with), filemanager
(icons and "Open"), and the `file` command. A path is identified in this
order, each tier only consulted if the previous one found nothing:

  1. Directory.
  2. Extension, against the user-editable "/config/extensions.conf"
     table (falls back to a built-in default table - see
     embedded_resources/json/extensions.json - the first time the file
     is missing or invalid).
  3. Magic bytes sampled from the start of the file (a small built-in
     signature table: PNG, JPEG, GIF, BMP, ZIP, GZIP, ELF, WASM, RIFF/WAV,
     FLAC, OggS, 7z, RAR).
  4. A `#!` shebang line, matching the interpreter name against the same
     extensions.conf table's "interpreters" lists.
  5. A text/binary heuristic over the sampled bytes (any NUL or unusual
     control byte counts as binary).

Tier 2 is authoritative for `program`/`icon`/`mimetype` when it matches -
it is curated and user-editable, so it wins even when a magic-byte tier
below it could also have matched (e.g. a ".conf" file is plain-text
bytes, but should keep its own icon rather than falling back to "ASCII
text").

**Constants**

| Name | Value |
|---|---|
| `BRUCE_FILETYPE_DESCRIPTION_MAX` | `64` |
| `BRUCE_FILETYPE_MIME_MAX` | `64` |
| `BRUCE_FILETYPE_ICON_MAX` | `32` |
| `BRUCE_FILETYPE_PROGRAM_MAX` | `32` |
| `BRUCE_FILETYPE_ACTION_LABEL_MAX` | `24` |
| `BRUCE_FILETYPE_MAX_ACTIONS` | `2` |

---

## bruce_filetype_action_t()

```c
typedef struct {
    /* Menu label, e.g. "Extract here". Never empty. */
    char label[BRUCE_FILETYPE_ACTION_LABEL_MAX];
    /* app_runner program to run with the file's path as its sole argument
     * when this action is chosen, e.g. "archive-extract". */
    char program[BRUCE_FILETYPE_PROGRAM_MAX];
} bruce_filetype_action_t;
```


---

## bruce_filetype_info_t()

```c
typedef struct {
    /* Short human-readable description, e.g. "PNG image", "POSIX shell
     * script", "ASCII text", "data". Never empty. */
    char description[BRUCE_FILETYPE_DESCRIPTION_MAX];
    /* MIME type, e.g. "image/png"; "" if none is known for this file. */
    char mimetype[BRUCE_FILETYPE_MIME_MAX];
    /* icon__get()-compatible icon name; "file" if nothing more specific matched. */
    char icon[BRUCE_FILETYPE_ICON_MAX];
    /* app_runner program name to open this file with; "" if none configured. */
    char program[BRUCE_FILETYPE_PROGRAM_MAX];
    bool is_directory;
    /* Best-effort text/binary guess, only meaningful when is_directory is false. */
    bool is_binary;
    /* Extra actions this extension offers beyond the default file-manager
     * menu, in configured order; only actions[0..action_count-1] are valid.
     * Kept to a handful (BRUCE_FILETYPE_MAX_ACTIONS) since these are meant
     * for the rare "this filetype needs its own verb" case (an archive's
     * "Extract here") rather than a general-purpose menu builder - most
     * entries have none. Populated by filetype__lookup_extension() and
     * filetype__identify()/filetype__identify_bytes() when an extension
     * matched (tier 2); always empty otherwise. */
    bruce_filetype_action_t actions[BRUCE_FILETYPE_MAX_ACTIONS];
    size_t action_count;
} bruce_filetype_info_t;
```


---

## filetype__lookup_extension()

```c
bruce_result_t filetype__lookup_extension(const char *path, bruce_filetype_info_t *out_info);
```

Looks up description/mimetype/icon/program from the extension table alone - no storage permission required, no I/O, and no fallback to magic-byte/shebang/text-heuristic detection.

This is tier 2 of filetype__identify() in isolation: the same answer a
matching extension would give there, just without ever opening the file,
for callers where that matters - app_runner's launch fast path (the
common case of a recognized extension shouldn't cost a read), and
per-row icon lookups when listing a whole directory (filemanager,
dialog's file picker), where opening every entry just for its icon
isn't affordable. is_directory and is_binary are always false; every
string field is "" (icon is "file") when the path's extension has no
configured entry.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `path` | `const char *` | Path whose extension is looked up. |
| `out_info` | `bruce_filetype_info_t *` | Receives the lookup result. |

### Returns

`bruce_result_t`


---

## filetype__icon_for_path()

```c
const char *filetype__icon_for_path(const char *path);
```

Returns the configured icon for a path's extension.

Same lookup as filetype__lookup_extension()'s icon field, "file" as a
default, but returned as a pointer into Core's own cached config rather
than copied into an out-struct: callers building a whole list of icons
at once (a directory listing's per-row icons, kept live together until
the list is rendered) can hold every returned pointer simultaneously,
which a shared scratch buffer couldn't support. The returned string is
owned by Core and valid for the lifetime of the firmware.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `path` | `const char *` | File path whose extension selects the icon, or NULL. |

### Returns

`const char *`


---

## filetype__identify()

```c
bruce_result_t filetype__identify(const char *path, bruce_filetype_info_t *out_info);
```

Identifies a file already on storage.

Reads a small header sample (and directory-ness) via the calling
process's normal storage permission - see storage__open()'s permission
notes. Prefer filetype__identify_bytes() when the caller already has the
file's bytes in memory, to avoid a second read.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `path` | `const char *` | Absolute or "./"-relative path to identify. |
| `out_info` | `bruce_filetype_info_t *` | Receives the identification. |

### Returns

`bruce_result_t`

#### Permissions

- `storage`


---

## filetype__identify_bytes()

```c
bruce_result_t filetype__identify_bytes(
    const char *path, const uint8_t *bytes, size_t size, bruce_filetype_info_t *out_info
);
```

Identifies a file from bytes already in memory.

`bytes`/`size` should be a prefix of the file - a few hundred bytes is
enough for every detection tier here (magic signatures are at most 12
bytes in, shebang parsing only needs the first line). `path` still
drives the extension tier and is otherwise unused; pass NULL (or "") when
identifying a floating buffer with no associated path, e.g. a download
or clipboard buffer - detection then skips straight to the magic-byte/
shebang/text-heuristic tiers. This never touches storage itself.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `path` | `const char *` | Path associated with bytes, or NULL/"" if there isn't one. |
| `bytes` | `const uint8_t *` | Prefix of the file's contents. |
| `size` | `size_t` | Number of bytes in bytes. |
| `out_info` | `bruce_filetype_info_t *` | Receives the identification. |

### Returns

`bruce_result_t`


---

# `format.h`

**Formatting helpers.**

---

## format__bytes_human()

```c
void format__bytes_human(uint64_t bytes, char *output, size_t capacity);
```

Formats `bytes` as a short human-readable string using 1024-based units (K, M, G, T).

e.g. 500 -> "500", 8400 -> "8.2K", 8832614 -> "8.4M". Plain byte counts
(under 1024) get no unit suffix, just the number. A single decimal digit
is shown once the value no longer fits in whole units; exact multiples
never get a decimal point. Always NUL-terminates within `capacity`; a
NULL output or zero capacity is a no-op.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `bytes` | `uint64_t` | Byte count to format. |
| `output` | `char *` | Buffer to receive the formatted string. |
| `capacity` | `size_t` | Size of output in bytes. |

### Returns

`void`


---

# `gpio.h`

**GPIO pin access.**

---

## bruce_gpio_mode_t()

```c
typedef enum {
    BRUCE_GPIO_MODE_INPUT = 0,
    BRUCE_GPIO_MODE_OUTPUT,
    BRUCE_GPIO_MODE_INPUT_OUTPUT,
} bruce_gpio_mode_t;
```


---

## bruce_gpio_pull_t()

```c
typedef enum {
    BRUCE_GPIO_PULL_NONE = 0,
    BRUCE_GPIO_PULL_UP,
    BRUCE_GPIO_PULL_DOWN,
} bruce_gpio_pull_t;
```


---

## gpio__configure()

```c
bruce_result_t gpio__configure(int pin, bruce_gpio_mode_t mode, bruce_gpio_pull_t pull);
```

Configures a GPIO pin's mode and pull resistor.

Pins and modes are checked against the selected SoC before the ESP-IDF
driver is called.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `pin` | `int` | GPIO pin number. |
| `mode` | `bruce_gpio_mode_t` | Pin direction mode. |
| `pull` | `bruce_gpio_pull_t` | Pull resistor configuration. |

### Returns

`bruce_result_t`

#### Permissions

- `gpio`


---

## gpio__read()

```c
bruce_result_t gpio__read(int pin, int *out_level);
```

Reads a GPIO pin's logic level.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `pin` | `int` | GPIO pin number. |
| `out_level` | `int *` | Receives the pin's level (0 or 1). |

### Returns

`bruce_result_t`

#### Permissions

- `gpio`


---

## gpio__write()

```c
bruce_result_t gpio__write(int pin, int level);
```

Sets a GPIO pin's output logic level.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `pin` | `int` | GPIO pin number. |
| `level` | `int` | New level (0 or 1). |

### Returns

`bruce_result_t`

#### Permissions

- `gpio`


---

# `hash.h`

**Cryptographic and checksum digests.**

**Constants**

| Name | Value |
|---|---|
| `BRUCE_HASH_MD5_SIZE` | `16` |
| `BRUCE_HASH_SHA256_SIZE` | `32` |

---

## bruce_hash_algorithm_t()

```c
typedef enum {
    BRUCE_HASH_MD5 = 0,
    BRUCE_HASH_SHA256,
} bruce_hash_algorithm_t;
```


---

## bruce_hash_ctx_t()

```c
typedef struct bruce_hash_ctx bruce_hash_ctx_t;
```


---

## hash__start()

```c
bruce_hash_ctx_t *hash__start(bruce_hash_algorithm_t algorithm);
```

Starts a new streaming digest.

Feed data to it with hash__update() as it becomes available (e.g. one
file-read chunk at a time), then collect the result with hash__finish().
Use hash__compute() instead when the whole input is already in memory.

@return A new context, or NULL on allocation failure.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `algorithm` | `bruce_hash_algorithm_t` | Which digest to compute. |

### Returns

`bruce_hash_ctx_t *`


---

## hash__update()

```c
bruce_result_t hash__update(bruce_hash_ctx_t *ctx, const void *data, size_t size);
```

Feeds `size` bytes of `data` into an in-progress digest.

May be called any number of times between hash__start() and
hash__finish(). `ctx` is left usable after a failure; the caller still
owns it and must eventually pass it to hash__finish() to free it.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `ctx` | `bruce_hash_ctx_t *` | Context returned by hash__start(). |
| `data` | `const void *` | Bytes to add to the digest. |
| `size` | `size_t` | Number of bytes in data. |

### Returns

`bruce_result_t`


---

## hash__finish()

```c
bruce_result_t hash__finish(bruce_hash_ctx_t *ctx, uint8_t *out);
```

Finishes a digest, writing the result and freeing `ctx`.

`ctx` is freed whether this succeeds or fails; it must not be used again
afterwards either way.

  BRUCE_HASH_SHA256_SIZE raw bytes, matching the algorithm hash__start()
  was called with.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `ctx` | `bruce_hash_ctx_t *` | Context returned by hash__start(). |
| `out` | `uint8_t *` | Receives the digest: BRUCE_HASH_MD5_SIZE or |

### Returns

`bruce_result_t`


---

## hash__compute()

```c
bruce_result_t hash__compute(bruce_hash_algorithm_t algorithm, const void *data, size_t size, uint8_t *out);
```

One-shot digest of a buffer already in memory.

Equivalent to hash__start() + hash__update() + hash__finish() in a
single call.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `algorithm` | `bruce_hash_algorithm_t` | Which digest to compute. |
| `data` | `const void *` | Buffer to digest. |
| `size` | `size_t` | Number of bytes in data. |
| `out` | `uint8_t *` | Receives the digest, sized as in hash__finish(). |

### Returns

`bruce_result_t`


---

## hash__crc32()

```c
uint32_t hash__crc32(uint32_t crc, const void *data, size_t size);
```

Incremental CRC-32.

The poly 0xEDB88320/init/final-XOR 0xFFFFFFFF variant used by
zlib/gzip/PNG/PKZIP (and e.g. Python's zlib.crc32()). Pass 0 as `crc` for
the first chunk of a stream, then thread each call's return value into
the next one as `crc`; the value returned after the last chunk already
is the complete CRC-32 -- there's no separate "finish" step.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `crc` | `uint32_t` | 0 to start a new checksum, or a previous return value to continue one. |
| `data` | `const void *` | Bytes to add to the checksum. |
| `size` | `size_t` | Number of bytes in data. |

### Returns

`uint32_t`


---

# `html.h`

**HTML parser.**

This is intentionally not a DOM: ESP32 RAM is too tight to hold a parsed
tree of an arbitrary web page. Instead the parser is a byte-at-a-time SAX
style tokenizer -- feed it bytes as they arrive over HTTP (see
`bruce_http_response_chunk_cb_t` in core_sdk/http.h) and it reports a
small vocabulary of content events as it recognizes them: plain text
runs, link/heading boundaries, image references, line/paragraph breaks,
and main/article/nav/footer landmark starts. `script,
`style`, and markup the vocabulary below doesn't describe are silently
skipped rather than reported.

Any module that wants "the readable content of an HTML page" -- a
browser, a feed reader, a page-title lookup -- can reuse this instead of
writing its own tag stripper.

Text is whitespace-collapsed (a run of spaces/tabs/newlines becomes one
space) and HTML entities are decoded. `href`/`src` attribute values are
resolved to absolute URLs against the parser's base URL before being
reported.

Not permission-gated.

---

## bruce_html_event_type_t()

```c
typedef enum {
    BRUCE_HTML_EVENT_TITLE,          /* `text` is the decoded <title> content. */
    BRUCE_HTML_EVENT_TEXT,           /* `text` is a whitespace-collapsed text run. */
    BRUCE_HTML_EVENT_LINK_START,     /* `text` is the link's resolved absolute URL. */
    BRUCE_HTML_EVENT_LINK_END,
    BRUCE_HTML_EVENT_IMAGE,          /* `text` is the image's resolved absolute URL;
                                       * `alt` is its optional alt text. */
    BRUCE_HTML_EVENT_ANCHOR,         /* `text` is an element's decoded `id` attribute. */
    BRUCE_HTML_EVENT_HEADING_START,  /* `value` is the heading level, 1-6. */
    BRUCE_HTML_EVENT_HEADING_END,
    BRUCE_HTML_EVENT_LINE_BREAK,     /* <br>, or an <li> closing (kept snug against its siblings). */
    BRUCE_HTML_EVENT_PARAGRAPH_BREAK,/* End of a block element such as <p>, <div>, or a <ul>/<ol>. */
    BRUCE_HTML_EVENT_LANDMARK_START, /* <main>/<article>/<nav>/<footer> open; `value` is a bruce_html_landmark_t. */
    BRUCE_HTML_EVENT_LIST_ITEM_START,/* <li> open; `value` is its <ul>/<ol> nesting depth (1 = not nested). */
} bruce_html_event_type_t;
```


---

## bruce_html_landmark_t()

```c
typedef enum {
    BRUCE_HTML_LANDMARK_MAIN,
    BRUCE_HTML_LANDMARK_ARTICLE,
    BRUCE_HTML_LANDMARK_NAV,
    BRUCE_HTML_LANDMARK_FOOTER,
} bruce_html_landmark_t;
```


---

## bruce_html_event_t()

```c
typedef struct {
    bruce_html_event_type_t type;
    /* Borrowed pointers, valid only for the duration of the callback. Never
     * NUL-terminated by contract; always use the paired _len. */
    const char *text;
    size_t text_len;
    const char *alt;
    size_t alt_len;
    int value;
} bruce_html_event_t;
```


---

## bruce_html_event_cb_t()

```c
typedef void (*bruce_html_event_cb_t)(const bruce_html_event_t *event, void *context);
```


---

## bruce_html_parser_t()

```c
typedef struct bruce_html_parser bruce_html_parser_t;
```


---

## html__parser_create()

```c
bruce_result_t html__parser_create(
    const char *base_url, bruce_html_event_cb_t callback, void *context, bruce_html_parser_t **out_parser
);
```

Creates a streaming parser.

`base_url` is used to resolve relative `href`/`src` attributes and is
copied; it may be NULL if the caller only wants text/structure events
(relative links/images are then reported unresolved, exactly as written
in the markup).

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `base_url` | `const char *` | URL used to resolve relative href/src attributes, or NULL. |
| `callback` | `bruce_html_event_cb_t` | Called synchronously as each event is recognized. |
| `context` | `void *` | Opaque pointer passed back to callback. |
| `out_parser` | `bruce_html_parser_t **` | Receives the new parser. |

### Returns

`bruce_result_t`


---

## html__parser_feed()

```c
bruce_result_t html__parser_feed(bruce_html_parser_t *parser, const void *data, size_t len);
```

Feeds the next chunk of HTML bytes.

May be called repeatedly with partial data (e.g. directly from an HTTP
response chunk callback); the parser carries incomplete tags/entities
across calls.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `bruce_html_parser_t *` | Parser to feed. |
| `data` | `const void *` | HTML bytes to parse. |
| `len` | `size_t` | Number of bytes in data. |

### Returns

`bruce_result_t`


---

## html__parser_finish()

```c
bruce_result_t html__parser_finish(bruce_html_parser_t *parser);
```

Flushes any pending text run and closes out open elements.

Call once after the last html__parser_feed().

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `bruce_html_parser_t *` | Parser to finish. |

### Returns

`bruce_result_t`


---

## html__parser_destroy()

```c
void html__parser_destroy(bruce_html_parser_t *parser);
```

Destroys a parser created by html__parser_create().

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `parser` | `bruce_html_parser_t *` | Parser to destroy. |

### Returns

`void`


---

## html__resolve_url()

```c
bool html__resolve_url(const char *base_url, const char *ref, char *out_url, size_t out_capacity);
```

Resolves `ref` against `base_url` into an absolute URL written to `out_url`.

(absolute, scheme-relative, absolute-path, or relative). Returns false if
the result would not fit `out_capacity` or `ref`/`base_url` is malformed.
`ref` is returned unchanged (copied) when it is already absolute.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `base_url` | `const char *` | Base URL to resolve against. |
| `ref` | `const char *` | URL reference to resolve (absolute, scheme-relative, absolute-path, or relative). |
| `out_url` | `char *` | Buffer to receive the resolved absolute URL. |
| `out_capacity` | `size_t` | Size of out_url in bytes. |

### Returns

`bool`


---

# `http.h`

**HTTP client.**

A single synchronous request primitive used by the JavaScript
`httpFetch()` binding and any ELF app that holds the `http` permission.
`http__request()` does NOT imply the `wifi` permission; callers must
arrange Wi-Fi connectivity separately (e.g. via `wifi__connect()`).

Response memory is allocated with the process-owned allocator and must be
released with `http__response_free()`. Binary bodies may contain embedded
NUL bytes; callers must use `body_len` instead of `strlen(body)`.

**Constants**

| Name | Value |
|---|---|
| `BRUCE_HTTP_DEFAULT_MAX_RESPONSE_BYTES` | `(64u * 1024u)` |

---

## bruce_http_response_chunk_cb_t()

```c
typedef bruce_result_t (*bruce_http_response_chunk_cb_t)(const void *data, size_t data_len, void *context);
```


---

## bruce_http_request_t()

```c
typedef struct {
    const char *url;
    const char *method; /* NULL or GET/POST/PUT/DELETE/HEAD/PATCH/OPTIONS */
    const char *body;
    size_t body_len;

    /* Headers are flattened key/value pairs: headers[0]=key, headers[1]=value,
     * headers[2]=key, ...; `header_count` is the number of pairs. */
    const char *const *headers;
    size_t header_count;

    uint32_t timeout_ms; /* 0 means the implementation default */

    /* Maximum total response-body bytes accepted. 0 selects
     * BRUCE_HTTP_DEFAULT_MAX_RESPONSE_BYTES. The limit applies to buffered and
     * callback responses and returns BRUCE_ERR_RESOURCE_LIMIT when exceeded. */
    size_t max_response_bytes;

    /* When non-NULL, body chunks are delivered here instead of being buffered
     * in the response. `response.body` remains NULL and body_len reports the
     * total bytes delivered. */
    bruce_http_response_chunk_cb_t on_response_chunk;
    void *response_chunk_context;
} bruce_http_request_t;
```


---

## bruce_http_response_t()

```c
typedef struct {
    int status_code;
    char *body;
    size_t body_len;

    /* Response headers as parallel name/value arrays. Up to 32 headers and
     * 4096 bytes of header text are retained; excess headers are omitted. */
    char **header_names;
    char **header_values;
    size_t header_count;

    /* Opaque. Non-NULL when `body` was captured straight into its own
     * PSRAM/swap allocation instead of sharing headers' internal-heap block
     * (a buffered response whose Content-Length fit in
     * memory__external_malloc() - see src/core/http/http.c). Either way,
     * never touch this field directly - just pass the whole response to
     * http__response_free(). */
    const void *body_object;
} bruce_http_response_t;
```


---

## http__request()

```c
bruce_result_t http__request(const bruce_http_request_t *request, bruce_http_response_t *response);
```

Performs an HTTP request and waits for the response.

On success, fills `response` and returns BRUCE_OK. The body is
NUL-terminated in buffered mode, but body_len is authoritative. Headers
are always one process-owned internal-heap allocation; the body shares it
unless it was large enough to instead go through
memory__external_malloc() (PSRAM, or swap when no PSRAM is fitted) -
either way, release both with a single http__response_free() call. On
failure, leaves `response` zeroed and returns a negative BRUCE_ERR_*
value.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `request` | `const bruce_http_request_t *` | Request to perform (url, method, body, headers, ...). |
| `response` | `bruce_http_response_t *` | Receives the response on success; zeroed on failure. |

### Returns

`bruce_result_t`

#### Permissions

- `http`


---

## http__response_free()

```c
void http__response_free(bruce_http_response_t *response);
```

Releases an HTTP response.

NULL or a zero-initialized response is a no-op.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `response` | `bruce_http_response_t *` | Response to release, previously filled by http__request(). |

### Returns

`void`


---

# `http_server.h`

**HTTP server.**

**Constants**

| Name | Value |
|---|---|
| `BRUCE_HTTP_SERVER_MAX_ROUTES` | `24` |

---

## bruce_http_server_method_t()

```c
typedef enum {
    BRUCE_HTTP_SERVER_GET = 0,
    BRUCE_HTTP_SERVER_POST,
    BRUCE_HTTP_SERVER_PUT,
    BRUCE_HTTP_SERVER_DELETE,
} bruce_http_server_method_t;
```


---

## bruce_http_server_request_t()

```c
typedef struct bruce_http_server_request bruce_http_server_request_t;
```


---

## bruce_http_server_route_callback_t()

```c
typedef bruce_result_t (*bruce_http_server_route_callback_t)(
    bruce_http_server_request_t *request,
    void *context
);
```


---

## bruce_http_server_route_t()

```c
typedef struct {
    bruce_http_server_method_t method;
    const char *uri;
    const char *content_type;
    const void *body;
    size_t body_len;
    int status_code; /* 0 selects 200. */
    bruce_http_server_route_callback_t callback;
    void *context;
} bruce_http_server_route_t;
```


---

## bruce_http_server_options_t()

```c
typedef struct {
    uint16_t port; /* 0 selects port 80. */
    const bruce_http_server_route_t *routes;
    size_t route_count;
} bruce_http_server_options_t;
```


---

## bruce_http_server_status_t()

```c
typedef struct {
    bool running;
    uint16_t port;
    size_t route_count;
} bruce_http_server_status_t;
```


---

## http_server__start()

```c
bruce_result_t http_server__start(const bruce_http_server_options_t *options);
```

Starts the device-wide HTTP server.

Each route is either fixed (callback is NULL) or dynamic (callback is
non-NULL and content_type/body/body_len/status_code are unset). URI,
fixed content type, and fixed body data are copied before this function
returns. Callback and context pointer values are retained, not the
objects they refer to; their targets must remain valid and unchanged
until http_server__stop() succeeds.

Dynamic callbacks run in a bounded pool of worker tasks and may execute
concurrently, including multiple calls using the same context. Callback
contexts and any shared state they access must therefore be thread-safe.
Callbacks must not block indefinitely, call
http_server__start()/stop(), retain the request, or use it from another
task. A callback must return BRUCE_OK after sending one full response, or
after finalizing a chunked response. Request headers must be read before
the first response send/chunk. Fixed routes remain synchronous.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `options` | `const bruce_http_server_options_t *` | Routes and listen port to start the server with. |

### Returns

`bruce_result_t`

#### Permissions

- `http`


---

## http_server_request__uri()

```c
const char *http_server_request__uri(const bruce_http_server_request_t *request);
```

Request URI includes the path and may include its query suffix.

The returned pointer follows the request lifetime described above.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `request` | `const bruce_http_server_request_t *` | Request to read. |

### Returns

`const char *`


---

## http_server_request__query_length()

```c
size_t http_server_request__query_length(const bruce_http_server_request_t *request);
```

Returns the raw, undecoded query length, excluding its NUL terminator.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `request` | `const bruce_http_server_request_t *` | Request to read. |

### Returns

`size_t`


---

## http_server_request__get_query()

```c
bruce_result_t http_server_request__get_query(
    bruce_http_server_request_t *request,
    char *buffer,
    size_t buffer_size
);
```

Copies the raw, undecoded query and NUL-terminates it.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `request` | `bruce_http_server_request_t *` | Request to read. |
| `buffer` | `char *` | Buffer to receive the query string. |
| `buffer_size` | `size_t` | Size of buffer in bytes. |

### Returns

`bruce_result_t`


---

## http_server_request__header_length()

```c
size_t http_server_request__header_length(
    bruce_http_server_request_t *request,
    const char *name
);
```

Returns a header value length, excluding its NUL terminator.

Zero also means that the header is absent or empty; get_header
distinguishes absence.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `request` | `bruce_http_server_request_t *` | Request to read. |
| `name` | `const char *` | Header name to look up. |

### Returns

`size_t`


---

## http_server_request__get_header()

```c
bruce_result_t http_server_request__get_header(
    bruce_http_server_request_t *request,
    const char *name,
    char *buffer,
    size_t buffer_size
);
```

Copies a request header value and NUL-terminates it.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `request` | `bruce_http_server_request_t *` | Request to read. |
| `name` | `const char *` | Header name to look up. |
| `buffer` | `char *` | Buffer to receive the header value. |
| `buffer_size` | `size_t` | Size of buffer in bytes. |

### Returns

`bruce_result_t`


---

## http_server_request__content_length()

```c
size_t http_server_request__content_length(const bruce_http_server_request_t *request);
```

Returns the request body's declared Content-Length.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `request` | `const bruce_http_server_request_t *` | Request to read. |

### Returns

`size_t`


---

## http_server_request__recv()

```c
int http_server_request__recv(
    bruce_http_server_request_t *request,
    void *buffer,
    size_t buffer_size
);
```

Reads at most buffer_size bytes and never past content_length.

Returns a byte count (zero when exhausted) or a negative BRUCE_ERR_*
value.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `request` | `bruce_http_server_request_t *` | Request to read the body from. |
| `buffer` | `void *` | Buffer to receive body bytes. |
| `buffer_size` | `size_t` | Size of buffer in bytes. |

### Returns

`int`


---

## http_server_request__set_status()

```c
bruce_result_t http_server_request__set_status(
    bruce_http_server_request_t *request,
    int status_code
);
```

Sets the response status code.

Response metadata must be set before the first send/chunk.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `request` | `bruce_http_server_request_t *` | Request to set the response status on. |
| `status_code` | `int` | HTTP status code to send. |

### Returns

`bruce_result_t`


---

## http_server_request__set_type()

```c
bruce_result_t http_server_request__set_type(
    bruce_http_server_request_t *request,
    const char *content_type
);
```

Sets the response Content-Type.

Response metadata must be set before the first send/chunk. `content_type`
must remain valid until that first send/chunk call.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `request` | `bruce_http_server_request_t *` | Request to set the response content type on. |
| `content_type` | `const char *` | Content-Type value. |

### Returns

`bruce_result_t`


---

## http_server_request__set_header()

```c
bruce_result_t http_server_request__set_header(
    bruce_http_server_request_t *request,
    const char *name,
    const char *value
);
```

Sets a response header.

Response metadata must be set before the first send/chunk. `name`/`value`
must remain valid until that first send/chunk call.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `request` | `bruce_http_server_request_t *` | Request to set the response header on. |
| `name` | `const char *` | Header name. |
| `value` | `const char *` | Header value. |

### Returns

`bruce_result_t`


---

## http_server_request__send()

```c
bruce_result_t http_server_request__send(
    bruce_http_server_request_t *request,
    const void *data,
    size_t data_len
);
```

Sends a complete response. data may be NULL only when data_len is zero.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `request` | `bruce_http_server_request_t *` | Request to respond to. |
| `data` | `const void *` | Response body bytes, or NULL if data_len is zero. |
| `data_len` | `size_t` | Number of bytes in data. |

### Returns

`bruce_result_t`


---

## http_server_request__send_chunk()

```c
bruce_result_t http_server_request__send_chunk(
    bruce_http_server_request_t *request,
    const void *data,
    size_t data_len
);
```

Sends a non-empty response chunk. Call finalize exactly once afterward.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `request` | `bruce_http_server_request_t *` | Request to respond to. |
| `data` | `const void *` | Chunk bytes to send. |
| `data_len` | `size_t` | Number of bytes in data. |

### Returns

`bruce_result_t`


---

## http_server_request__finalize()

```c
bruce_result_t http_server_request__finalize(bruce_http_server_request_t *request);
```

Finalizes a chunked response started with http_server_request__send_chunk().

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `request` | `bruce_http_server_request_t *` | Request whose chunked response to finalize. |

### Returns

`bruce_result_t`


---

## http_server__stop()

```c
bruce_result_t http_server__stop(void);
```

Stops the server.

Returns BRUCE_ERR_BUSY during a lifecycle transition. If a callback does
not finish within the bounded shutdown wait, returns BRUCE_ERR_TIMEOUT
with the server and callback contexts still active; keep them valid and
call stop again.

### Returns

`bruce_result_t`

#### Permissions

- `http`


---

## http_server__get_status()

```c
bruce_result_t http_server__get_status(bruce_http_server_status_t *out_status);
```

Returns a point-in-time server status.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `out_status` | `bruce_http_server_status_t *` | Receives the current server status. |

### Returns

`bruce_result_t`

#### Permissions

- `http`


---

## http_server__is_running()

```c
bool http_server__is_running(void);
```

Returns false when denied or stopped.

### Returns

`bool`


---

# `i2c.h`

**I2C bus access.**

**Constants**

| Name | Value |
|---|---|
| `BRUCE_I2C_PORT_AUTO` | `(-1)` |
| `BRUCE_I2C_MAX_TRANSFER_SIZE` | `4096u` |

---

## bruce_i2c_bus_config_t()

```c
typedef struct {
    int port;
    int sda;
    int scl;
    uint32_t clock_hz;
    bool enable_internal_pullups;
} bruce_i2c_bus_config_t;
```


---

## i2c__open()

```c
bruce_result_t i2c__open(const bruce_i2c_bus_config_t *config, bruce_i2c_id_t *out_bus);
```

Opens an I2C bus.

Bus handles belong to the calling process and close automatically on
process exit.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `config` | `const bruce_i2c_bus_config_t *` | Bus configuration (port, pins, clock). |
| `out_bus` | `bruce_i2c_id_t *` | Receives the new bus handle. |

### Returns

`bruce_result_t`

#### Permissions

- `gpio`


---

## i2c__probe()

```c
bruce_result_t i2c__probe(bruce_i2c_id_t bus, uint8_t address, uint32_t timeout_ms, bool *out_present);
```

Probes an I2C bus for a device at `address`.

A zero timeout performs one immediate, bounded driver attempt.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `bus` | `bruce_i2c_id_t` | Bus handle from i2c__open(). |
| `address` | `uint8_t` | Unshifted 7-bit device address. |
| `timeout_ms` | `uint32_t` | Probe timeout in milliseconds, or 0 for one immediate attempt. |
| `out_present` | `bool *` | Receives whether a device acknowledged the address. |

### Returns

`bruce_result_t`

#### Permissions

- `gpio`


---

## i2c__write()

```c
bruce_result_t
i2c__write(bruce_i2c_id_t bus, uint8_t address, const void *data, size_t size, uint32_t timeout_ms);
```

Writes bytes to an I2C device.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `bus` | `bruce_i2c_id_t` | Bus handle from i2c__open(). |
| `address` | `uint8_t` | Unshifted 7-bit device address. |
| `data` | `const void *` | Bytes to write. |
| `size` | `size_t` | Number of bytes to write. |
| `timeout_ms` | `uint32_t` | Transfer timeout in milliseconds, or 0 for one immediate attempt. |

### Returns

`bruce_result_t`

#### Permissions

- `gpio`


---

## i2c__read()

```c
bruce_result_t i2c__read(bruce_i2c_id_t bus, uint8_t address, void *data, size_t size, uint32_t timeout_ms);
```

Reads bytes from an I2C device.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `bus` | `bruce_i2c_id_t` | Bus handle from i2c__open(). |
| `address` | `uint8_t` | Unshifted 7-bit device address. |
| `data` | `void *` | Buffer to receive the read bytes. |
| `size` | `size_t` | Number of bytes to read. |
| `timeout_ms` | `uint32_t` | Transfer timeout in milliseconds, or 0 for one immediate attempt. |

### Returns

`bruce_result_t`

#### Permissions

- `gpio`


---

## i2c__write_read()

```c
bruce_result_t i2c__write_read(
    bruce_i2c_id_t bus, uint8_t address, const void *write_data, size_t write_size, void *read_data,
    size_t read_size, uint32_t timeout_ms
);
```

Writes then reads in one combined I2C transaction (repeated start).

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `bus` | `bruce_i2c_id_t` | Bus handle from i2c__open(). |
| `address` | `uint8_t` | Unshifted 7-bit device address. |
| `write_data` | `const void *` | Bytes to write first. |
| `write_size` | `size_t` | Number of bytes in write_data. |
| `read_data` | `void *` | Buffer to receive the read bytes. |
| `read_size` | `size_t` | Number of bytes to read. |
| `timeout_ms` | `uint32_t` | Transfer timeout in milliseconds, or 0 for one immediate attempt. |

### Returns

`bruce_result_t`

#### Permissions

- `gpio`


---

## i2c__close()

```c
bruce_result_t i2c__close(bruce_i2c_id_t bus);
```

Closes an I2C bus opened by i2c__open().

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `bus` | `bruce_i2c_id_t` | Bus handle to close. |

### Returns

`bruce_result_t`

#### Permissions

- `gpio`


---

# `icon.h`

**Built-in icons.**

Icons are stored as 24x24 1bpp bitmaps (72 bytes each, MSB-first, three
bytes per row) pre-rasterized from Material Design Icons path data. This
is the cheapest possible representation for filled icons: no parsing, no
floating point, and no working RAM at draw time. The returned pointer is
valid for the lifetime of the firmware and must not be freed by the
caller.

**Constants**

| Name | Value |
|---|---|
| `BRUCE_ICON_SIZE` | `24` |

---

## bruce_icon_t()

```c
typedef struct {
    uint8_t width;        /* pixels, always BRUCE_ICON_SIZE for built-in icons */
    uint8_t height;       /* pixels, always BRUCE_ICON_SIZE for built-in icons */
    const uint8_t *bits;  /* 1bpp MSB-first bitmap, (width + 7) / 8 bytes per row */
} bruce_icon_t;
```


---

## icon__get()

```c
const bruce_icon_t *icon__get(const char *name);
```

Return the built-in icon named `name`, or NULL if `name` is not recognized.

Recognized names (case-sensitive): names match the SVG filenames in
core/icon/assets (without the .svg suffix), for example "wifi",
"bluetooth", "radio-handheld", and "clock-outline".

The returned struct is owned by Core; the caller must not free or modify
it. Draw it with display__draw_bitmap_scaled(), e.g.:

  const bruce_icon_t *icon = icon__get("wifi");
  display__draw_bitmap_scaled(x, y, icon->bits, icon->width, icon->height,
                              size, size, color);

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `name` | `const char *` | Icon name, matching an SVG filename in core/icon/assets (without the .svg suffix). |

### Returns

`const bruce_icon_t *`


---

# `image.h`

**JPEG, PNG and GIF decoding, resizing, and drawing.**

---

## bruce_image_format_t()

```c
typedef enum {
    BRUCE_IMAGE_FORMAT_JPEG,
    BRUCE_IMAGE_FORMAT_PNG,
    BRUCE_IMAGE_FORMAT_GIF,
} bruce_image_format_t;
```


---

## bruce_image_info_t()

```c
typedef struct {
    bruce_image_format_t format;
    uint16_t width;
    uint16_t height;
} bruce_image_info_t;
```


---

## bruce_image_draw_options_t()

```c
typedef struct {
    int16_t x;
    int16_t y;
    bool center;
    bool fit;
    bruce_display_color_t background;
} bruce_image_draw_options_t;
```


---

## image_bitmap_t()

```c
typedef struct {
    uint16_t *pixels;
    uint16_t width;
    uint16_t height;
    uint16_t source_width;
    uint16_t source_height;
    bruce_image_format_t format;
    bool external; /* true when pixels came from memory__external_malloc() and must be freed that way. */
} image_bitmap_t;
```


---

## bruce_gif_t()

```c
typedef struct bruce_gif bruce_gif_t;
```


---

## image__get_bitmap_from_memory()

```c
bruce_result_t image__get_bitmap_from_memory(
    const void *data, size_t size, const bruce_image_draw_options_t *options, image_bitmap_t *out_bitmap
);
```

Decodes JPEG, PNG, or the first frame of a GIF into an owned RGB565 bitmap.

`fit` preserves aspect ratio and only scales down. Transparent pixels are
composited over `background`. Release a successful result with
image__bitmap_release().

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `data` | `const void *` | Encoded image bytes. |
| `size` | `size_t` | Number of bytes in data. |
| `options` | `const bruce_image_draw_options_t *` | Draw/decode options (position, centering, fit, background). |
| `out_bitmap` | `image_bitmap_t *` | Receives the decoded bitmap. |

### Returns

`bruce_result_t`


---

## image__bitmap_resize()

```c
bruce_result_t image__bitmap_resize(
    const image_bitmap_t *source, uint16_t max_width, uint16_t max_height, image_bitmap_t *out_bitmap
);
```

Resizes `source` into an owned RGB565 bitmap, preserving aspect ratio.

While fitting within `max_width` x `max_height`. Core chooses the backing
store; callers must not depend on a particular memory backend. The
result is never upscaled and must be released with
image__bitmap_release().

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `source` | `const image_bitmap_t *` | Bitmap to resize. |
| `max_width` | `uint16_t` | Maximum output width. |
| `max_height` | `uint16_t` | Maximum output height. |
| `out_bitmap` | `image_bitmap_t *` | Receives the resized bitmap. |

### Returns

`bruce_result_t`


---

## image__get_bitmap_from_file()

```c
bruce_result_t image__get_bitmap_from_file(
    const char *path, const bruce_image_draw_options_t *options, image_bitmap_t *out_bitmap
);
```

Opens and decodes an image through process-owned Core storage.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `path` | `const char *` | Path of the image file to decode. |
| `options` | `const bruce_image_draw_options_t *` | Draw/decode options (position, centering, fit, background). |
| `out_bitmap` | `image_bitmap_t *` | Receives the decoded bitmap. |

### Returns

`bruce_result_t`


---

## image__draw_path()

```c
bruce_result_t
image__draw_path(const char *path, const bruce_image_draw_options_t *options, bruce_image_info_t *out_info);
```

Decodes, draws, and releases an image from Core storage.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `path` | `const char *` | Path of the image file to decode and draw. |
| `options` | `const bruce_image_draw_options_t *` | Draw/decode options (position, centering, fit, background). |
| `out_info` | `bruce_image_info_t *` | Receives the decoded image's format/dimensions. |

### Returns

`bruce_result_t`


---

## image__draw_bitmap()

```c
bruce_result_t image__draw_bitmap(const image_bitmap_t *bitmap, const bruce_image_draw_options_t *options);
```

Draws an owned bitmap into the active render target without releasing it.

The caller controls frame presentation.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `bitmap` | `const image_bitmap_t *` | Bitmap to draw. |
| `options` | `const bruce_image_draw_options_t *` | Draw options (position, centering, fit, background). |

### Returns

`bruce_result_t`


---

## image__bitmap_release()

```c
void image__bitmap_release(image_bitmap_t *bitmap);
```

Releases a bitmap returned by image__get_bitmap_from_memory()/from_file()/resize().

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `bitmap` | `image_bitmap_t *` | Bitmap to release. |

### Returns

`void`


---

## image__is_supported_path()

```c
bool image__is_supported_path(const char *path);
```

Returns true for the filename extensions accepted by image__get_bitmap_from_file().

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `path` | `const char *` | Path (or filename) to check. |

### Returns

`bool`


---

## image__gif_open()

```c
bruce_result_t image__gif_open(
    const char *path, const bruce_image_draw_options_t *options, bruce_gif_t **out_gif,
    bruce_image_info_t *out_info
);
```

Opens an animated GIF from Core storage and decodes its first frame.

The returned object owns its file data and must be released with
image__gif_close().

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `path` | `const char *` | Path of the GIF file to open. |
| `options` | `const bruce_image_draw_options_t *` | Draw/decode options (position, centering, fit, background). |
| `out_gif` | `bruce_gif_t **` | Receives the new GIF object. |
| `out_info` | `bruce_image_info_t *` | Receives the decoded image's format/dimensions. |

### Returns

`bruce_result_t`


---

## image__gif_draw()

```c
bruce_result_t image__gif_draw(bruce_gif_t *gif, uint32_t *out_delay_ms);
```

Draws the current composited frame and optionally returns its display time.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `gif` | `bruce_gif_t *` | GIF object to draw. |
| `out_delay_ms` | `uint32_t *` | Receives the current frame's display time in milliseconds, or NULL. |

### Returns

`bruce_result_t`


---

## image__gif_increment()

```c
bruce_result_t image__gif_increment(bruce_gif_t *gif, bool *out_looped);
```

Advances and decodes the next frame.

At the trailer this loops to the first frame and sets `out_looped`, when
non-NULL, to true.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `gif` | `bruce_gif_t *` | GIF object to advance. |
| `out_looped` | `bool *` | Set to true if this advance looped back to the first frame, or NULL. |

### Returns

`bruce_result_t`


---

## image__gif_close()

```c
void image__gif_close(bruce_gif_t *gif);
```

Closes a GIF object opened by image__gif_open().

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `gif` | `bruce_gif_t *` | GIF object to close. |

### Returns

`void`


---

# `input.h`

**Keyboard, button, touch, and encoder event queue.**

**Constants**

| Name | Value |
|---|---|
| `BRUCE_INPUT_CODE_UP` | `0x03B` |
| `BRUCE_INPUT_CODE_DOWN` | `0x02E` |
| `BRUCE_INPUT_CODE_LEFT` | `0x02C` |
| `BRUCE_INPUT_CODE_RIGHT` | `0x02F` |
| `BRUCE_INPUT_CODE_SELECT` | `0x00A` |
| `BRUCE_INPUT_CODE_BACK` | `0x060` |
| `BRUCE_INPUT_CODE_MENU` | `0x106` |
| `BRUCE_INPUT_CODE_HOME` | `0x107` |
| `BRUCE_INPUT_CODE_DELETE` | `0x108` |
| `BRUCE_INPUT_CODE_PREV` | `0x109` |
| `BRUCE_INPUT_CODE_NEXT` | `0x10A` |
| `BRUCE_INPUT_CODE_ZOOM_OUT` | `0x10B` |
| `BRUCE_INPUT_CODE_ZOOM_IN` | `0x10C` |
| `BRUCE_INPUT_CODE_BUTTON_A` | `0x200` |
| `BRUCE_INPUT_CODE_BUTTON_B` | `0x201` |
| `BRUCE_INPUT_CODE_BUTTON_C` | `0x202` |
| `BRUCE_INPUT_CODE_BUTTON_X` | `0x203` |
| `BRUCE_INPUT_CODE_BUTTON_Y` | `0x204` |
| `BRUCE_INPUT_CODE_BUTTON_L1` | `0x205` |
| `BRUCE_INPUT_CODE_BUTTON_R1` | `0x206` |
| `BRUCE_INPUT_CODE_BUTTON_L2` | `0x207` |
| `BRUCE_INPUT_CODE_BUTTON_R2` | `0x208` |
| `BRUCE_INPUT_CODE_BUTTON_START` | `0x209` |
| `BRUCE_INPUT_CODE_BUTTON_SELECT` | `0x20A` |
| `BRUCE_INPUT_CODE_BUTTON_THUMB_L` | `0x20B` |
| `BRUCE_INPUT_CODE_BUTTON_THUMB_R` | `0x20C` |
| `BRUCE_INPUT_CODE_GAMEPAD_AXIS_X` | `0x300` |
| `BRUCE_INPUT_CODE_GAMEPAD_AXIS_Y` | `0x301` |
| `BRUCE_INPUT_CODE_GAMEPAD_AXIS_RX` | `0x302` |
| `BRUCE_INPUT_CODE_GAMEPAD_AXIS_RY` | `0x303` |

---

## bruce_input_type_t()

```c
typedef enum {
    BRUCE_INPUT_KEY,
    BRUCE_INPUT_BUTTON,
    BRUCE_INPUT_TOUCH,
    BRUCE_INPUT_ENCODER,
    BRUCE_INPUT_CUSTOM,
} bruce_input_type_t;
```


---

## bruce_input_action_t()

```c
typedef enum {
    BRUCE_INPUT_PRESS,
    BRUCE_INPUT_RELEASE,
    BRUCE_INPUT_CHANGE,
} bruce_input_action_t;
```


---

## bruce_input_event_t()

```c
typedef struct {
    bruce_input_type_t type;
    bruce_input_action_t action;
    int32_t code;
    int32_t value;
    uint64_t timestamp_ms;
    bruce_process_id_t source_process_id;
} bruce_input_event_t;
```


---

## input__read()

```c
bruce_result_t input__read(bruce_input_event_t *out_event, uint32_t timeout_ms);
```

Returns the next input event for the foreground app.

`timeout_ms` controls blocking:
  - 0      : non-blocking; returns BRUCE_ERR_TIMEOUT immediately if no event
            is queued.
  - >0     : blocks up to the requested total elapsed milliseconds.
  - 0xFFFFFFFF (portMAX_DELAY equivalent) : block until an event arrives.

Returns BRUCE_OK with *out_event filled, BRUCE_ERR_TIMEOUT if no event
arrived within the timeout, or BRUCE_ERR_NOT_FOREGROUND if the caller is not
the current foreground process. A blocked read is revoked immediately when its
foreground tenure ends; regaining foreground requires a new call. An
indefinite read is also interrupted by input deinitialization.

Physical input is delivered only to the foreground process; background
processes must call process__foreground() or use input__inject() (which
requires the `input` permission).

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `out_event` | `bruce_input_event_t *` | Receives the popped event. |
| `timeout_ms` | `uint32_t` | 0 for non-blocking, >0 to block up to that many milliseconds, or 0xFFFFFFFF to block indefinitely. |

### Returns

`bruce_result_t`


---

## input__poll()

```c
static inline bruce_result_t input__poll(bruce_input_event_t *out_event) { return input__read(out_event, 0); }
```

Returns the next input event without waiting.

It returns BRUCE_OK with *out_event filled, or BRUCE_ERR_TIMEOUT if no
event is available. It is equivalent to input__read(out_event, 0).

Typical polling loop:
  bruce_input_event_t ev;
  while (input__poll(&ev) == BRUCE_OK) { ... handle ev ... }

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `out_event` | `bruce_input_event_t *` | Receives the popped event. |

### Returns

`bruce_result_t`


---

## input__flush()

```c
bruce_result_t input__flush(void);
```

Removes all queued input events.

It is useful when switching screens so that stale presses do not affect
the new UI. Returns BRUCE_OK or BRUCE_ERR_NOT_FOREGROUND if the caller is
not the foreground process.

### Returns

`bruce_result_t`


---

## input__peek()

```c
bruce_result_t input__peek(bruce_input_event_t *out_event);
```

Returns the next queued event without removing it.

Returns BRUCE_OK with *out_event filled, BRUCE_ERR_TIMEOUT if the queue
is empty, or BRUCE_ERR_NOT_FOREGROUND if the caller is not the foreground
process.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `out_event` | `bruce_input_event_t *` | Receives the next queued event. |

### Returns

`bruce_result_t`


---

## input__wait()

```c
bruce_result_t input__wait(uint32_t timeout_ms, int32_t *out_code);
```

Waits for a press event.

Returns BRUCE_OK with the event code in *out_code, BRUCE_ERR_TIMEOUT if
no press event arrived within the timeout, or BRUCE_ERR_NOT_FOREGROUND if
the caller is not the foreground process. Release and change events are
ignored.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `timeout_ms` | `uint32_t` | Maximum time to wait, in milliseconds. |
| `out_code` | `int32_t *` | Receives the pressed event's code. |

### Returns

`bruce_result_t`


---

## input__check()

```c
bool input__check(int32_t code, bool consume);
```

Checks whether a press event for `code` is queued.

If `consume` is true the first matching press event is removed from the
queue; if false the event is left in place. Returns true if a matching
press event exists, false otherwise. Always returns false for
non-foreground processes.

This is the Core equivalent of the legacy `check(PrevPress)` /
`check(SelPress)` pattern used by the old Arduino loop code and JS
bindings.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `code` | `int32_t` | Event code to test for, e.g. BRUCE_INPUT_CODE_SELECT. |
| `consume` | `bool` | If true, removes the matching event from the queue. |

### Returns

`bool`


---

## input__inject()

```c
bruce_result_t input__inject(const bruce_input_event_t *event);
```

Adds an event to the input queue.

It is used by input adapters (Bluetooth, GPIO, I2C, serial, ...) to feed
the same event pipeline as physical buttons and keyboard.

Returns BRUCE_OK, BRUCE_ERR_PERMISSION if the caller lacks the `input`
permission, BRUCE_ERR_INVALID_ARGUMENT for a malformed event, or another
BRUCE_ERR_* value for internal failures.

`event->timestamp_ms` and `event->source_process_id` are ignored by the
core; the core fills them in from the current time/process before
queuing.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `event` | `const bruce_input_event_t *` | Event to inject. |

### Returns

`bruce_result_t`

#### Permissions

- `input`


---

# `ir.h`

**Infrared sending and receiving.**

**Constants**

| Name | Value |
|---|---|
| `BRUCE_IR_DEFAULT_FREQUENCY_HZ` | `38000u` |
| `BRUCE_IR_MAX_RAW_TIMINGS` | `512u` |

---

## ir__transmit_raw()

```c
bruce_result_t
ir__transmit_raw(const uint32_t *timings_us, size_t timing_count, uint32_t frequency_hz, uint8_t repeats);
```

Transmits raw timing data over RMT.

`repeats` means additional complete transmissions after the initial
transmission, matching the Bruce IR file/menu convention.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `timings_us` | `const uint32_t *` | Raw mark/space durations in microseconds. |
| `timing_count` | `size_t` | Number of entries in timings_us. |
| `frequency_hz` | `uint32_t` | Carrier frequency in Hz, e.g. BRUCE_IR_DEFAULT_FREQUENCY_HZ. |
| `repeats` | `uint8_t` | Additional complete transmissions after the initial one. |

### Returns

`bruce_result_t`

#### Permissions

- `ir`


---

## ir__transmit()

```c
bruce_result_t ir__transmit(const char *data_hex, const char *protocol, uint8_t bits, uint8_t repeats);
```

Transmits hexadecimal scalar data using NEC, NECext, Samsung32, SIRC, SIRC15, or SIRC20.

Protocol matching is case-insensitive.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `data_hex` | `const char *` | Scalar data to transmit, as hex text. |
| `protocol` | `const char *` | Protocol name, e.g. "NEC" (case-insensitive). |
| `bits` | `uint8_t` | Number of data bits to send. |
| `repeats` | `uint8_t` | Additional complete transmissions after the initial one. |

### Returns

`bruce_result_t`

#### Permissions

- `ir`


---

## ir__transmit_parsed()

```c
bruce_result_t
ir__transmit_parsed(const char *protocol, const char *address_hex, const char *command_hex, uint8_t repeats);
```

Transmits a parsed Bruce/Flipper address and command pair.

Byte strings may be contiguous ("04000000") or space-separated
("04 00 00 00").

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `protocol` | `const char *` | Protocol name. |
| `address_hex` | `const char *` | Address bytes, as hex text. |
| `command_hex` | `const char *` | Command bytes, as hex text. |
| `repeats` | `uint8_t` | Additional complete transmissions after the initial one. |

### Returns

`bruce_result_t`

#### Permissions

- `ir`


---

## ir__receive()

```c
bruce_result_t ir__receive(bool raw, uint32_t timeout_ms, char *out, size_t out_size);
```

Receives one signal and writes a complete NUL-terminated version-1 IR file record.

Decoded mode recognizes NEC; unrecognized signals return
BRUCE_ERR_UNSUPPORTED so callers can retry with raw=true.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `raw` | `bool` | If true, records raw timings instead of attempting to decode a known protocol. |
| `timeout_ms` | `uint32_t` | Time to wait for a signal, in milliseconds. |
| `out` | `char *` | Buffer to receive the NUL-terminated .ir record. |
| `out_size` | `size_t` | Size of out in bytes. |

### Returns

`bruce_result_t`

#### Permissions

- `ir`


---

## ir__transmit_file()

```c
bruce_result_t ir__transmit_file(const char *path, uint8_t repeats);
```

Replays every valid parsed/raw record in a Bruce/Flipper version-1 .ir file.

Supported parsed records are the same protocols accepted by
ir__transmit().

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `path` | `const char *` | Path of the .ir file to replay. |
| `repeats` | `uint8_t` | Additional complete transmissions of each record after its initial one. |

### Returns

`bruce_result_t`

#### Permissions

- `ir`
- `storage`


---

## ir__transmit_record()

```c
bruce_result_t ir__transmit_record(const char *contents, uint8_t repeats);
```

Replays one or more version-1 records already held in memory.

This is useful for testing a learned capture before it is saved and
requires only `ir`.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `contents` | `const char *` | Version-1 .ir file records, as text already held in memory. |
| `repeats` | `uint8_t` | Additional complete transmissions of each record after its initial one. |

### Returns

`bruce_result_t`

#### Permissions

- `ir`


---

## ir__tx_pin()

```c
int ir__tx_pin(void);
```

Returns the infrared transmitter pin.

### Returns

`int`


---

## ir__rx_pin()

```c
int ir__rx_pin(void);
```

Returns the infrared receiver pin.

### Returns

`int`


---

# `manifest.h`

**App manifest parsing.**

**Constants**

| Name | Value |
|---|---|
| `BRUCE_CORE_ABI_VERSION` | `5u` |
| `BRUCE_MANIFEST_APP_NAME_MAX` | `64` |
| `BRUCE_MANIFEST_ICON_BYTES` | `128` |
| `BRUCE_MANIFEST_MAX_PERMISSIONS` | `BRUCE_PERMISSION_COUNT` |
| `BRUCE_MANIFEST_PERMISSION_NAME_MAX` | `16` |
| `BRUCE_MANIFEST_STACK_MIN` | `4096u` |
| `BRUCE_MANIFEST_STACK_MAX` | `16384u` |
| `BRUCE_MANIFEST_HEAP_MAX` | `0x800000u /* 8 MiB */` |

---

## bruce_manifest_t()

```c
typedef struct {
    char app_name[BRUCE_MANIFEST_APP_NAME_MAX];
    uint8_t app_icon[BRUCE_MANIFEST_ICON_BYTES];
    uint32_t core_abi_version;
    uint32_t stack_size;
    /* Advisory expected peak memory__malloc()/memory__external_malloc() use;
     * see BRUCE_MANIFEST_HEAP_MAX. 0 (the default) if the manifest omits
     * heapSize. */
    uint32_t heap_size;
    char permissions[BRUCE_MANIFEST_MAX_PERMISSIONS][BRUCE_MANIFEST_PERMISSION_NAME_MAX];
    size_t permission_count;
} bruce_manifest_t;
```


---

## bruce_app_kind_t()

```c
typedef enum {
    BRUCE_APP_KIND_ELF,
    BRUCE_APP_KIND_JAVASCRIPT,
    BRUCE_APP_KIND_WEBASSEMBLY,
} bruce_app_kind_t;
```


---

## bruce_app_inspection_t()

```c
typedef struct {
    bruce_app_kind_t kind;
    bruce_manifest_t manifest;
    int abi_warning;
} bruce_app_inspection_t;
```


---

## manifest__parse()

```c
bruce_manifest_t *manifest__parse(const char *json, size_t json_len);
```

Parses and validates canonical manifest JSON bytes.

(see migration_plan.md, "ELF contract"): required appName/appIcon
(base64, decodes to exactly BRUCE_MANIFEST_ICON_BYTES bytes)/
coreAbiVersion/stackSize (BRUCE_MANIFEST_STACK_MIN-
BRUCE_MANIFEST_STACK_MAX inclusive); an optional heapSize (0-
BRUCE_MANIFEST_HEAP_MAX inclusive, see bruce_manifest_t.heap_size); and an
optional permissions array (each name must be a known bruce_permission_t
name, no duplicates). Every caller extracts raw manifest bytes from the
file format and calls this one shared parser instead of reimplementing
JSON/base64 handling. Returns a process-owned manifest that must be
released with memory__free(), or NULL for invalid input or allocation
failure.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `json` | `const char *` | Canonical manifest JSON bytes. |
| `json_len` | `size_t` | Number of bytes in json. |

### Returns

`bruce_manifest_t *`


---

## manifest__inspect_path()

```c
char *manifest__inspect_path(const char *path);
```

Reads a manifest from any supported app file.

Opens `path`, auto-detects the file format (ELF section, JS comment
block, or WebAssembly `bruce.manifest` custom section), and returns raw
NUL-terminated manifest JSON. The returned buffer is process-owned and
must be released with memory__free(). Never launches an app. This is the
one function every program -- the launcher, file manager, terminal tools
-- uses to extract the manifest from any file.

Does NOT parse the JSON or do format-specific validation (e_machine, ABI
warning). Callers that need a parsed manifest call manifest__parse() on
the returned bytes. Format-specific full inspection is handled by
manifest__inspect_elf() and similar per-format functions.

Returns NULL for an invalid path, inaccessible file, unsupported format,
missing manifest, or allocation failure.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `path` | `const char *` | Path of the app file to inspect. |

### Returns

`char *`


---

## manifest__inspect_elf()

```c
bruce_app_inspection_t *manifest__inspect_elf(const char *path);
```

Reads a manifest from an ELF app.

Opens `path`, validates the ELF32 header (magic, e_machine vs. this
build's target), extracts and parses the .bruce.manifest section via
manifest__parse(), and fills *out_inspection with the parsed manifest,
BRUCE_APP_KIND_ELF, and the ABI-warning flag. A valid ELF with a missing
or invalid manifest receives fallback metadata: its filename, current
Core ABI, an 8192-byte stack, no heap-size hint (0), and no predeclared
permissions. Loader
modules that know they are loading an ELF file (the built-in ELF loader,
for example) call this directly.

The returned inspection is process-owned and must be released with
memory__free(). Returns NULL for invalid paths, missing files, malformed
ELF headers, target mismatches, or allocation failures.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `path` | `const char *` | Path of the ELF file to inspect. |

### Returns

`bruce_app_inspection_t *`


---

## manifest__inspect_javascript()

```c
bruce_app_inspection_t *manifest__inspect_javascript(const char *path);
```

Reads a manifest from a JavaScript app.

Opens `path`, detects a leading block comment (slash-asterisk ...
asterisk-slash) containing the canonical manifest JSON, and parses it.
Fills *out_inspection with the parsed manifest, BRUCE_APP_KIND_JAVASCRIPT,
and ABI-warning flag. If there is no leading manifest block, the script
still gets a valid inspection using its filename as the app name, a
generic icon, the current ABI version, and zero permissions.

The returned inspection is process-owned and must be released with
memory__free(). Returns NULL for invalid paths, inaccessible files, or
allocation failures.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `path` | `const char *` | Path of the JavaScript file to inspect. |

### Returns

`bruce_app_inspection_t *`


---

## manifest__inspect_wasm()

```c
bruce_app_inspection_t *manifest__inspect_wasm(const char *path);
```

Reads a manifest from a WebAssembly app.

Opens a `.wasm` path, validates the standard WebAssembly magic and
version, safely walks bounded u32 LEB128 section envelopes, and parses
the payload of a `bruce.manifest` custom section as canonical manifest
JSON. This is envelope validation only; WAMR performs full WebAssembly
semantic validation at launch. The returned inspection has
BRUCE_APP_KIND_WEBASSEMBLY and an ABI-warning flag.

An envelope-valid WebAssembly module with a missing, duplicate,
oversized, or invalid manifest receives filename fallback metadata with
a generic icon, current Core ABI, an 8192-byte stack, no heap-size hint
(0), and no permissions. Malformed modules, invalid paths, inaccessible
files, and allocation failures return NULL. The returned inspection is
process-owned and must be released with memory__free().

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `path` | `const char *` | Path of the .wasm file to inspect. |

### Returns

`bruce_app_inspection_t *`


---

# `memory.h`

**Memory allocation (RAM, PSRAM, swap).**

---

## bruce_memory_stats_t()

```c
typedef struct {
    size_t internal_total;
    size_t internal_free;
    size_t internal_largest_block;
    size_t internal_minimum_free;
    size_t internal_free_blocks;
    size_t psram_total;
    size_t psram_free;
    size_t psram_largest_block;
    size_t swap_total;
    size_t swap_free;
    size_t swap_largest_block;
} bruce_memory_stats_t;
```


---

## bruce_memory_backend_t()

```c
typedef enum {
    BRUCE_MEMORY_BACKEND_INVALID = 0,
    BRUCE_MEMORY_BACKEND_PSRAM,
    BRUCE_MEMORY_BACKEND_SWAP,
    BRUCE_MEMORY_BACKEND_INTERNAL,
} bruce_memory_backend_t;
```


---

## bruce_memory_object_t()

```c
typedef struct {
    uint32_t handle;
    size_t size;
    bruce_memory_backend_t backend;
} bruce_memory_object_t;
```


---

## bruce_memory_region_t()

```c
typedef enum {
    BRUCE_MEMORY_REGION_UNKNOWN = 0,
    BRUCE_MEMORY_REGION_DRAM,
    BRUCE_MEMORY_REGION_DIRAM,
    BRUCE_MEMORY_REGION_IRAM,
    BRUCE_MEMORY_REGION_RTC_FAST,
    BRUCE_MEMORY_REGION_PSRAM,
    BRUCE_MEMORY_REGION_SWAP,
} bruce_memory_region_t;
```

Physical memory kind containing a layout block.


---

## bruce_memory_layout_block_t()

```c
typedef struct {
    uintptr_t address;
    size_t size;              /* Allocator block size, or swap reserved size. */
    uintptr_t region_start;   /* Start of the containing allocator region. */
    uintptr_t region_end;     /* Exclusive end of the containing region. */
    size_t requested_size;    /* Bruce-requested bytes; zero when unknown/free. */
    bruce_memory_backend_t backend;
    bruce_memory_region_t region;
    bruce_process_id_t owner_id; /* Invalid for free or untracked blocks. */
    uint32_t handle;          /* External-object handle; zero for heap blocks. */
    bool used;
    bool tracked;
    bool executable;
} bruce_memory_layout_block_t;
```

One block in a point-in-time memory layout snapshot.


---

## memory__malloc()

```c
void *memory__malloc(size_t size);
```

Process-owned tracked heap allocator.

ELF apps never receive libc malloc/free (those imports are rejected by
the loader); JS and built-in code should also prefer this allocator so
memory is accounted against the owning process and released
automatically if the process exits or is killed without freeing it
first.

memory__malloc() must be called from within a Core-managed process (i.e.
a process started by process_registry__create()/AppRunner); calling it
with no current process returns NULL, the same result as an allocation
failure.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `size` | `size_t` | Number of bytes to allocate. |

### Returns

`void *`


---

## memory__calloc()

```c
void *memory__calloc(size_t count, size_t size);
```

Allocates and zero-initializes count objects of size bytes.

Returns NULL when either argument is zero or their product overflows
size_t.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `count` | `size_t` | Number of objects to allocate. |
| `size` | `size_t` | Size of each object in bytes. |

### Returns

`void *`


---

## memory__realloc()

```c
void *memory__realloc(void *ptr, size_t size);
```

Resizes a tracked allocation while preserving its process ownership.

The libc realloc rules apply for NULL ptr and zero size.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `ptr` | `void *` | Allocation to resize, or NULL to allocate a new one. |
| `size` | `size_t` | New size in bytes. |

### Returns

`void *`


---

## memory__free()

```c
void memory__free(void *ptr);
```

Frees memory obtained from memory__malloc()/memory__calloc()/memory__realloc().

NULL is a no-op. Passing any other pointer, or one already freed, is
undefined behaviour, matching libc free().

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `ptr` | `void *` | Allocation to free, or NULL. |

### Returns

`void`


---

## memory__external_malloc()

```c
const void *memory__external_malloc(size_t size);
```

Allocates process-owned external memory (PSRAM, swap, or internal RAM) and returns a read-only pointer.

PSRAM is preferred when a sufficiently large block is available;
otherwise complete 64 KiB pages are allocated from swap. If neither is
available -- no PSRAM on this board, and no "swap" partition has been
committed yet (see partitions.csv) -- plain internal RAM is used as a
last resort so callers still get a usable allocation instead of NULL.
The allocation is released automatically when its process exits.

The returned pointer is read-only: swap allocations are flash-mapped and
not directly CPU-writable, so writes must go through
memory__external_memcpy()/memset(), which know how to update each backend.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `size` | `size_t` | Number of bytes to allocate. Returns NULL for zero. |

### Returns

`const void *`


---

## memory__external_malloc_writable()

```c
const void *memory__external_malloc_writable(size_t size);
```

Allocates process-owned external memory that is guaranteed directly CPU-writable.

Same PSRAM-or-internal-RAM allocation memory__external_malloc() may also
land on, but never swap: unlike memory__external_malloc(), this never
falls back to a flash-mapped allocation, so callers that write into the
returned buffer through an ordinary pointer (rather than
memory__external_memcpy()/memset()) never end up with one that silently
corrupts on write. Returns NULL if neither PSRAM nor internal RAM has
room for `size` -- callers needing a guaranteed allocation should fall
back to memory__malloc() in that case.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `size` | `size_t` | Number of bytes to allocate. Returns NULL for zero. |

### Returns

`const void *`


---

## memory__external_calloc()

```c
const void *memory__external_calloc(size_t count, size_t size);
```

Allocates zero-initialized external memory for count objects of size bytes.

Returns NULL when either argument is zero or their product overflows size_t.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `count` | `size_t` | Number of objects to allocate. |
| `size` | `size_t` | Size of each object in bytes. |

### Returns

`const void *`


---

## memory__external_memcpy()

```c
bruce_result_t memory__external_memcpy(const void *ptr, size_t offset, const void *data, size_t size);
```

Copies bytes into external memory without exposing a writable flash pointer.

Swap updates that require changing a zero bit back to one rewrite the
affected flash sectors. Callers must synchronize writes with readers of
an already shared mapping.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `ptr` | `const void *` | Allocation obtained from memory__external_malloc()/calloc(). |
| `offset` | `size_t` | Byte offset within the allocation to write at. |
| `data` | `const void *` | Bytes to copy in. |
| `size` | `size_t` | Number of bytes to copy. |

### Returns

`bruce_result_t`


---

## memory__external_memset()

```c
bruce_result_t memory__external_memset(const void *ptr, size_t offset, int value, size_t size);
```

Fills a range of external memory with a repeated byte value.

Semantics otherwise match memory__external_memcpy(); prefer this over a
memcpy() of a manually filled buffer since a zero fill is always a direct
flash write (no sector erase needed).

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `ptr` | `const void *` | Allocation obtained from memory__external_malloc()/calloc(). |
| `offset` | `size_t` | Byte offset within the allocation to fill at. |
| `value` | `int` | Byte value to repeat, as an unsigned char. |
| `size` | `size_t` | Number of bytes to fill. |

### Returns

`bruce_result_t`


---

## memory__external_free()

```c
bruce_result_t memory__external_free(const void *ptr);
```

Frees memory obtained from memory__external_malloc()/calloc().

NULL is a no-op. Payload bytes are not zeroed. Passing any other pointer
not currently owned by the calling process, or one already freed, is
rejected with BRUCE_ERR_INVALID_ARGUMENT / BRUCE_ERR_PERMISSION.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `ptr` | `const void *` | Allocation to free, or NULL. |

### Returns

`bruce_result_t`


---

## memory__get_stats()

```c
bruce_result_t memory__get_stats(bruce_memory_stats_t *out_stats);
```

Returns current heap capacity plus fragmentation/high-water diagnostics.

`internal_free_blocks` counts currently free allocator blocks across all
internal-capable heap regions; it includes the regions' natural
boundaries.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `out_stats` | `bruce_memory_stats_t *` | Receives the current heap statistics. |

### Returns

`bruce_result_t`


---

## memory__get_layout()

```c
bruce_result_t memory__get_layout(
    bruce_memory_backend_t backend, bruce_memory_layout_block_t *blocks,
    size_t capacity, size_t *out_count
);
```

Captures allocator blocks for one memory backend without allocating.

INTERNAL and PSRAM include both free and occupied ESP-IDF heap blocks.
Occupied blocks allocated through memory__malloc() carry their Bruce owner;
other occupied blocks are returned as untracked. SWAP returns exact free
runs and process-owned external allocations. If capacity is too small, the
prefix that fits is written and out_count still reports the required count.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `backend` | `bruce_memory_backend_t` |  |
| `blocks` | `bruce_memory_layout_block_t *` |  |
| `capacity` | `size_t` |  |
| `out_count` | `size_t *` |  |

### Returns

`bruce_result_t`

#### Permissions

- `process`


---

## memory__read()

```c
bruce_result_t memory__read(
    bruce_memory_backend_t backend, uintptr_t address, void *buffer, size_t size
);
```

Validates or copies bytes from a diagnostic memory range.

INTERNAL and PSRAM addresses must lie wholly inside one ESP-IDF heap block;
MMIO, allocator metadata gaps, and unrelated address ranges are rejected.
SWAP addresses are byte offsets in the swap partition and must lie wholly
inside one currently allocated external object (free/stale pages are never
exposed). This is a best-effort live snapshot: separate calls may observe
concurrent changes.
Passing NULL for buffer performs validation without copying.

@note Built-in modules only. External ELF/JS/WASM apps are denied even if
they hold the process permission, and the symbol is not exported to ELF.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `backend` | `bruce_memory_backend_t` |  |
| `address` | `uintptr_t` |  |
| `buffer` | `void *` |  |
| `size` | `size_t` |  |

### Returns

`bruce_result_t`


---

## memory__readable_size()

```c
bruce_result_t memory__readable_size(
    bruce_memory_backend_t backend, uintptr_t address, size_t *out_size
);
```

Returns readable bytes from an address to the end of its heap block or active swap object.

@note Built-in modules only, under the same restrictions as memory__read().

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `backend` | `bruce_memory_backend_t` |  |
| `address` | `uintptr_t` |  |
| `out_size` | `size_t *` |  |

### Returns

`bruce_result_t`


---

## estimate()

```c
typedef struct {
    const char *name; /* For logs; not required to be unique. */
    size_t (*estimate)(void);
    size_t (*reclaim)(void);
    void (*restore)(void);
} bruce_reclaim_provider_t;
```

One subsystem's contribution to memory__reclaim(): how much it could give back right now, and the calls to actually shrink or grow it.

estimate() must be side-effect-free and cheap -- memory__reclaim() calls it
on every registered provider just to decide whether reclaiming is worth
disturbing anything for. reclaim() actually shrinks the subsystem and
returns the bytes it freed, which may be less than estimate() reported (the
state may have changed, or another caller reclaimed it first) or zero if
there was nothing left to give. restore() undoes exactly the shrink that
reclaim() performed; it runs automatically, best-effort, when the process
credited with the reclaim exits (see memory__reclaim()), so it must be safe
to call from any context and a no-op if there is nothing to restore.


---

## memory__register_reclaimable()

```c
bruce_result_t memory__register_reclaimable(const bruce_reclaim_provider_t *provider);
```

Registers a subsystem as a source memory__reclaim() can draw from.

Providers are consulted in registration order and are never unregistered
-- meant for a long-lived Core subsystem (e.g. the display framebuffer) to
call once from its own init function, not for per-allocation use.

@note Built-in modules only; not exported to ELF.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `provider` | `const bruce_reclaim_provider_t *` |  |

### Returns

`bruce_result_t`


---

## bruce_memory_reclaim_token_t()

```c
typedef struct {
    bruce_process_id_t owner_id;
    bruce_resource_id_t resource_id;
} bruce_memory_reclaim_token_t;
```

Handle to a memory__reclaim() performed on another process's behalf, to be handed to memory__reclaim_adopt() by that process.


---

## memory__reclaim()

```c
bruce_result_t memory__reclaim(
    size_t needed_bytes, size_t *out_freed, bruce_memory_reclaim_token_t *out_token
);
```

Tries to free at least `needed_bytes` from registered subsystems before a big allocation.

Sums every registered provider's estimate() first and only calls reclaim()
on any of them if the total would actually meet `needed_bytes` -- a
request that can't be satisfied never disturbs anything (e.g. never
changes the display's rendering mode) for no benefit. Reclaiming stops as
soon as enough has been freed, so it never shrinks more subsystems than it
has to.

On success, what was reclaimed is restored automatically when the calling
process exits -- the same crash-safety memory__malloc() gives ordinary
allocations. A caller reclaiming on behalf of a *different* process (e.g.
a loader, before spawning it) should pass out_token and have that process
call memory__reclaim_adopt() with it, so the restore is tied to the
process that actually needs the memory instead of to the caller.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `needed_bytes` | `size_t` | Bytes the caller is about to need. |
| `out_freed` | `size_t *` | Bytes actually freed; 0 if nothing was reclaimed (out_token is also cleared). May be NULL. |
| `out_token` | `bruce_memory_reclaim_token_t *` | Set to a token for memory__reclaim_adopt(), or the zero token if nothing was reclaimed. May be NULL. |

### Returns

`bruce_result_t`


---

## memory__reclaim_adopt()

```c
bruce_result_t memory__reclaim_adopt(bruce_memory_reclaim_token_t token);
```

Transfers a memory__reclaim() restore obligation to the calling process.

Call once from the process meant to benefit from a reclaim performed by a
different process -- mirrors ext_mem_loader__adopt_xip()'s handoff
pattern. The zero token (or one already adopted) is a no-op.

@note Built-in modules only; not exported to ELF.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `token` | `bruce_memory_reclaim_token_t` | Token returned by memory__reclaim(). |

### Returns

`bruce_result_t`


---

# `notification.h`

**On-screen notifications.**

**Constants**

| Name | Value |
|---|---|
| `BRUCE_NOTIFICATION_TEXT_MAX` | `96` |
| `BRUCE_NOTIFICATION_DURATION_MIN_MS` | `250u` |
| `BRUCE_NOTIFICATION_DURATION_MAX_MS` | `30000u` |

---

## notification__push()

```c
bruce_result_t notification__push(const char *text, uint32_t duration_ms);
```

Shows or replaces the global transient notification.

Text is copied before return. Duration is clamped to the public minimum
and maximum. This is a fire-and-forget request: it is queued for
whatever is currently consuming notification__wait_request() below
(normally modules/notification_service) and always returns BRUCE_OK,
whether or not anything is listening yet. Only the single latest unread
push/dismiss is kept -- an older one still unread is replaced, not
queued behind it.

Callers don't choose or need to know how the notification will be shown:
whether the calling process was launched with GUI interaction (see
core_sdk/runtime.h's runtime__gui_requested()) is captured here, at push
time, and travels with the request so the consumer can render a GUI
banner or print a console line as appropriate -- see
notification__wait_request().

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `text` | `const char *` | Notification text (copied before return). |
| `duration_ms` | `uint32_t` | How long to show it, clamped to BRUCE_NOTIFICATION_DURATION_MIN/MAX_MS. |

### Returns

`bruce_result_t`


---

## notification__dismiss()

```c
bruce_result_t notification__dismiss(void);
```

Dismisses the current notification. This operation is idempotent.

### Returns

`bruce_result_t`


---

## notification__wait_request()

```c
bruce_result_t notification__wait_request(
    char *out_text, size_t text_size, uint32_t *out_duration_ms, bool *out_dismiss, bool *out_gui_requested,
    bruce_stdio_session_t *out_session, uint32_t timeout_ms
);
```

Waits for the next notification change.

Blocks up to `timeout_ms` (UINT32_MAX blocks forever) for the next
request. On a push, copies its text into `out_text` (size `text_size`,
always NUL-terminated), its duration into `*out_duration_ms`, whether the
pushing process wanted GUI interaction into `*out_gui_requested`, and the
pushing process's own routed stdio session (BRUCE_STDIO_SESSION_INVALID
if it had none) into `*out_session`, and sets `*out_dismiss` to false; on
a dismiss, sets `*out_dismiss` to true and leaves the other four
untouched. Returns BRUCE_ERR_TIMEOUT if nothing arrived in time.

This is the hand-off point for whatever process renders notifications --
normally modules/notification_service, which uses `*out_gui_requested`
to choose between a GUI banner (display__overlay_*) and a console line
(stdio__write_to(*out_session, ...) -- not stdio__write()/stdio__printf(),
since the consumer's own routed session, if any, is unrelated to the
pusher's), so nothing about rendering is special-cased in Core. A user
who wants a different notification UI does not need this function at
all: they can just call display__overlay_create() directly from their
own app instead of going through notification__push().

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `out_text` | `char *` | Receives the pushed notification text (NUL-terminated). |
| `text_size` | `size_t` | Size of out_text in bytes. |
| `out_duration_ms` | `uint32_t *` | Receives the pushed notification's requested duration. |
| `out_dismiss` | `bool *` | Set to true if this was a dismiss request rather than a push. |
| `out_gui_requested` | `bool *` | Receives whether the pushing process wanted GUI interaction. |
| `out_session` | `bruce_stdio_session_t *` | Receives the pushing process's routed stdio session, or BRUCE_STDIO_SESSION_INVALID. |
| `timeout_ms` | `uint32_t` | Time to wait for the next request, in milliseconds (UINT32_MAX blocks forever). |

### Returns

`bruce_result_t`

#### Permissions

- _Built-in apps only -- external ELF/JS/WASM callers are always denied._


---

# `nrf24.h`

**nRF24 radio control and scanning.**

**Constants**

| Name | Value |
|---|---|
| `BRUCE_NRF24_CHANNEL_MIN` | `0u` |
| `BRUCE_NRF24_CHANNEL_MAX` | `125u` |
| `BRUCE_NRF24_DEFAULT_CHANNEL` | `76u` |

---

## bruce_nrf24_pins_t()

```c
typedef struct {
    int sck;
    int miso;
    int mosi;
    int cs;
    int ce;
} bruce_nrf24_pins_t;
```


---

## nrf24__probe()

```c
bruce_result_t nrf24__probe(bool *out_connected);
```

Probes for a connected nRF24 radio.

The radio attaches to the shared external SPI3_HOST bus and does not use
display-owned SPI2_HOST.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `out_connected` | `bool *` | Receives whether a radio was detected. |

### Returns

`bruce_result_t`

#### Permissions

- `rf`


---

## nrf24__set_channel()

```c
bruce_result_t nrf24__set_channel(uint8_t channel);
```

Sets the radio's channel.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `channel` | `uint8_t` | New channel (BRUCE_NRF24_CHANNEL_MIN..BRUCE_NRF24_CHANNEL_MAX). |

### Returns

`bruce_result_t`

#### Permissions

- `rf`


---

## nrf24__get_channel()

```c
bruce_result_t nrf24__get_channel(uint8_t *out_channel);
```

Reads the radio's current channel.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `out_channel` | `uint8_t *` | Receives the current channel. |

### Returns

`bruce_result_t`

#### Permissions

- `rf`


---

## nrf24__scan()

```c
bruce_result_t
nrf24__scan(uint8_t first_channel, size_t channel_count, uint8_t samples, uint8_t *out_activity);
```

Samples the nRF24 RPD threshold detector on consecutive channels.

Each output value is the number of positive samples (0..samples). RPD is
a threshold activity indication, not calibrated RSSI or packet decoding.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `first_channel` | `uint8_t` | First channel to sample. |
| `channel_count` | `size_t` | Number of consecutive channels to sample. |
| `samples` | `uint8_t` | Number of samples to take per channel. |
| `out_activity` | `uint8_t *` | Array to receive one positive-sample count per channel. |

### Returns

`bruce_result_t`

#### Permissions

- `rf`


---

## nrf24__get_pins()

```c
void nrf24__get_pins(bruce_nrf24_pins_t *out_pins);
```

Returns the board's configured nRF24 SPI/CS/CE pins.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `out_pins` | `bruce_nrf24_pins_t *` | Receives the pin configuration. |

### Returns

`void`


---

# `partition_manager.h`

**Manages the "user area" partitions.**

The flash left over after the static partitions in partitions.csv
(bootloader, partition_table, nvs, coredump, factory), minus one sector
at the very end of the flash reserved for this module's own partition
table. The user area always holds exactly one LittleFS partition labeled
"littlefs" mounted at "/" (BRUCE_PARTITION_ROOT_LABEL), at offset 0, plus
any number of extra partitions the user creates.

The root partition is elastic: it is never given a size directly, it
simply spans everything below the lowest extra partition. Creating an
extra partition therefore takes that space away from "/" and reformats
it on the next boot (LittleFS cannot be shrunk in place); deleting the
lowest extra partition gives the space back the same way. Both UIs
(modules/bparted) warn about that before anything is written.

Nothing here ever mounts, erases or reformats storage the running system
is using: stage_create()/stage_delete()/stage_format() only edit an
in-RAM working table, commit() persists that table, and the erase/format
work it describes happens once, automatically, early on the next boot -
see list_current() vs list_planned() and bruce_partition_status_t's
reboot_required.

"swap" (BRUCE_PARTITION_SWAP_LABEL) is a reserved label: a
BRUCE_PARTITION_KIND_SWAP entry is always labeled "swap" and uses the
exact type/subtype core/memory's flash-backed swap allocator already
looks for, so creating one is all that is needed to enable it. Any other
label is an additional LittleFS volume that gets formatted but is not
auto-mounted.

Mutating calls (stage_create/stage_delete/stage_format/commit/discard)
are restricted to built-in modules (see the "bparted" command), the same
restriction core_sdk/disk.h's disk__mount()/disk__unmount() use; any app
may read the layout.

**Constants**

| Name | Value |
|---|---|
| `BRUCE_PARTITION_LABEL_MAX` | `BRUCE_DISK_NAME_MAX` |
| `BRUCE_PARTITION_MAX_ENTRIES` | `8` |
| `BRUCE_PARTITION_ROOT_LABEL` | `"littlefs"` |
| `BRUCE_PARTITION_SWAP_LABEL` | `"swap"` |
| `BRUCE_PARTITION_ROOT_MIN_BYTES` | `(64u * 1024u)` |

---

## bruce_partition_kind_t()

```c
typedef enum {
    BRUCE_PARTITION_KIND_SWAP,
    BRUCE_PARTITION_KIND_LITTLEFS,
} bruce_partition_kind_t;
```


---

## bruce_partition_state_t()

```c
typedef enum {
    BRUCE_PARTITION_STATE_UNCHANGED,
    BRUCE_PARTITION_STATE_NEW,     /* Does not exist yet; created on the next boot. */
    BRUCE_PARTITION_STATE_DELETED, /* Exists now; gone after the next boot. */
    BRUCE_PARTITION_STATE_FORMAT,  /* Exists now; erased/reformatted on the next boot. */
} bruce_partition_state_t;
```


---

## bruce_partition_entry_t()

```c
typedef struct {
    char label[BRUCE_PARTITION_LABEL_MAX];
    bruce_partition_kind_t kind;
    uint64_t offset; /* Relative to the start of the user area. */
    uint64_t size;
    bruce_partition_state_t state;
    bool is_root; /* The BRUCE_PARTITION_ROOT_LABEL entry mounted at "/". */
} bruce_partition_entry_t;
```


---

## bruce_partition_status_t()

```c
typedef struct {
    /* An edit is staged that commit() has not written yet - i.e. list_planned()
     * shows something discard() would take back. */
    bool has_pending_changes;
    /* A committed layout differs from the one running now, so a reboot is
     * needed to apply it. Independent of has_pending_changes. */
    bool reboot_required;
    uint64_t total_bytes;       /* Size of the whole user area. */
    uint64_t used_bytes;        /* Covered by planned entries, root included. */
    uint64_t unallocated_bytes; /* Gaps between extra partitions, usable only by a new one. */
    uint64_t max_new_size;      /* Largest partition stage_create() would accept right now. */
} bruce_partition_status_t;
```


---

## partition_manager__list_current()

```c
bruce_result_t
partition_manager__list_current(bruce_partition_entry_t *entries, size_t capacity, size_t *out_count);
```

Returns the current partition layout.

Pass NULL with capacity 0 to query count; a too-small non-NULL buffer
fills what fits and returns BRUCE_ERR_RESOURCE_LIMIT. Entries are
ordered by offset.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `entries` | `bruce_partition_entry_t *` | Array to receive partition entries, or NULL to only query count. |
| `capacity` | `size_t` | Number of entries the entries array can hold. |
| `out_count` | `size_t *` | Receives the total number of entries available. |

### Returns

`bruce_result_t`


---

## partition_manager__list_planned()

```c
bruce_result_t
partition_manager__list_planned(bruce_partition_entry_t *entries, size_t capacity, size_t *out_count);
```

Returns the partition layout for the next boot.

list_current() plus every staged and committed change, each entry tagged
with how it differs (including BRUCE_PARTITION_STATE_DELETED rows for
entries that are about to go away, which is why this can return more
entries than list_current()). Same ordering and capacity convention as
list_current().

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `entries` | `bruce_partition_entry_t *` | Array to receive partition entries, or NULL to only query count. |
| `capacity` | `size_t` | Number of entries the entries array can hold. |
| `out_count` | `size_t *` | Receives the total number of entries available. |

### Returns

`bruce_result_t`


---

## partition_manager__status()

```c
bruce_result_t partition_manager__status(bruce_partition_status_t *out_status);
```

Returns partition space and pending-change information.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `out_status` | `bruce_partition_status_t *` | Receives the current status. |

### Returns

`bruce_result_t`


---

## partition_manager__stage_create()

```c
bruce_result_t
partition_manager__stage_create(const char *label, bruce_partition_kind_t kind, uint64_t size_bytes);
```

Stages a new partition, taking its space from the tail of the root partition.

(or from a gap a previous delete left behind). `label` must be 1-16
chars of [A-Za-z0-9_-] and a BRUCE_PARTITION_KIND_SWAP entry's label must
be exactly BRUCE_PARTITION_SWAP_LABEL. `size_bytes` is rounded up to the
nearest 4096-byte flash sector. Returns BRUCE_ERR_ALREADY_EXISTS for a
duplicate label and BRUCE_ERR_RESOURCE_LIMIT when the table is full or
the request is larger than bruce_partition_status_t.max_new_size. Call
commit() to persist.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `label` | `const char *` | New partition's label, 1-16 chars of [A-Za-z0-9_-]. |
| `kind` | `bruce_partition_kind_t` | Partition kind (swap or littlefs). |
| `size_bytes` | `uint64_t` | Requested size in bytes, rounded up to the nearest 4096-byte sector. |

### Returns

`bruce_result_t`

#### Permissions

- _Built-in apps only -- external ELF/JS/WASM callers are always denied._


---

## partition_manager__stage_delete()

```c
bruce_result_t partition_manager__stage_delete(const char *label);
```

Stages removal of `label`.

The root entry cannot be deleted (BRUCE_ERR_PERMISSION) - reformat it
instead. Its space is given back to the root partition when it sits
directly above it, and is otherwise left as a gap only a new partition
can reuse.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `label` | `const char *` | Label of the partition to remove. |

### Returns

`bruce_result_t`

#### Permissions

- _Built-in apps only -- external ELF/JS/WASM callers are always denied._


---

## partition_manager__stage_format()

```c
bruce_result_t partition_manager__stage_format(const char *label);
```

Stages an in-place erase + reformat of `label`, keeping its size, kind and position.

Allowed for the root entry (wipes and reformats "/" on the next boot).
Returns BRUCE_ERR_INVALID_STATE for an entry that does not exist yet (a
staged create is formatted on creation anyway).

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `label` | `const char *` | Label of the partition to reformat. |

### Returns

`bruce_result_t`

#### Permissions

- _Built-in apps only -- external ELF/JS/WASM callers are always denied._


---

## partition_manager__commit()

```c
bruce_result_t partition_manager__commit(void);
```

Persists the staged table to flash.

Does not itself erase or format anything - that happens once,
automatically, the next time the device boots.

### Returns

`bruce_result_t`

#### Permissions

- _Built-in apps only -- external ELF/JS/WASM callers are always denied._


---

## partition_manager__discard()

```c
bruce_result_t partition_manager__discard(void);
```

Discards every not-yet-committed staged edit.

Resets list_planned() back to the last committed layout. A no-op
(BRUCE_OK) when has_pending_changes is already false.

### Returns

`bruce_result_t`

#### Permissions

- _Built-in apps only -- external ELF/JS/WASM callers are always denied._


---

# `permission.h`

**App permissions.**

Every ELF/JS/WASM app is identified, for permission purposes, by its
filename including extension and without its path (e.g. "game.elf",
"weather.js"). Decisions are persisted in Core-owned
/config/permissions.json, keyed by that filename; apps sharing a
basename deliberately share the same decision. Built-in modules are
implicitly granted every permission and never consult the saved-decision
store.

**Constants**

| Name | Value |
|---|---|
| `BRUCE_PERMISSION_FILE_NAME_MAX` | `48` |

---

## bruce_permission_t()

```c
typedef enum {
    BRUCE_PERMISSION_HTTP = 0,
    BRUCE_PERMISSION_WIFI,
    BRUCE_PERMISSION_BT,
    BRUCE_PERMISSION_GPS,
    BRUCE_PERMISSION_RF,
    BRUCE_PERMISSION_INPUT,
    BRUCE_PERMISSION_GPIO,
    BRUCE_PERMISSION_IR,
    BRUCE_PERMISSION_RFID,
    BRUCE_PERMISSION_MICROPHONE,
    BRUCE_PERMISSION_HID,
    BRUCE_PERMISSION_EXECUTE,
    BRUCE_PERMISSION_PROCESS,
    BRUCE_PERMISSION_STORAGE,
    BRUCE_PERMISSION_CONFIG,
    BRUCE_PERMISSION_SERIAL,
    BRUCE_PERMISSION_SSH,
    BRUCE_PERMISSION_COUNT,
} bruce_permission_t;
```


---

## permission__name()

```c
const char *permission__name(bruce_permission_t permission);
```

Returns the canonical lowercase name ("wifi", "http", ...).

Used in manifests and /config/permissions.json, or NULL for an
out-of-range value.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `permission` | `bruce_permission_t` | Permission to name. |

### Returns

`const char *`


---

## permission__from_name()

```c
bool permission__from_name(const char *name, bruce_permission_t *out_permission);
```

Resolves a manifest/JSON permission name to its enum value.

Returns false (leaving *out_permission untouched) for an unknown name.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `name` | `const char *` | Canonical lowercase permission name, e.g. "wifi". |
| `out_permission` | `bruce_permission_t *` | Receives the resolved enum value. |

### Returns

`bool`


---

## permission__check()

```c
bruce_result_t permission__check(bruce_permission_t permission);
```

Checks whether the *calling* process currently holds `permission`.

This is the function every protected Core API (wifi__*, http__*,
storage__*, config__*, process__* control of another process,
app_runner__run, ...) calls internally; app/module code never needs to
call it directly.

A built-in process always returns BRUCE_OK. An external (ELF/JS/WASM)
process with an existing saved decision returns immediately (BRUCE_OK or
BRUCE_ERR_PERMISSION) with no prompt. With no saved decision yet, this is
the dynamic first-use request: it shows an allow/deny dialog__choice(),
persists the answer keyed by the process's permission file name, and
returns accordingly. If the dialog itself fails (e.g. is cancelled) the
decision is left unresolved (not persisted) and BRUCE_ERR_PERMISSION is
returned, so a later call may prompt again. Returns
BRUCE_ERR_INVALID_ARGUMENT for an out-of-range `permission`.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `permission` | `bruce_permission_t` | Permission to check for the calling process. |

### Returns

`bruce_result_t`


---

## permission__preflight()

```c
bruce_result_t
permission__preflight(const char *file_name, const char *const *permission_names, size_t count);
```

Requests several permissions before launching an app.

For every name in `permission_names` that `file_name` has no saved
decision for yet, prompts (unchecked/undecided by default) and persists
the user's choice; already-known permissions are left untouched and not
re-prompted. Intended for the ELF/JS/WASM loaders (Stage 3) to call with
the manifest's declared permission list before a new process's first
instruction runs. Returns BRUCE_OK once every name has been processed
(regardless of individual allow/deny outcomes) or
BRUCE_ERR_INVALID_ARGUMENT for an invalid `file_name` or an unknown
permission name.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `file_name` | `const char *` | Permission file name the decisions are keyed by, e.g. "game.elf". |
| `permission_names` | `const char *const *` | Permission names from the app's manifest. |
| `count` | `size_t` | Number of entries in permission_names. |

### Returns

`bruce_result_t`


---

## permission__get_saved()

```c
bruce_result_t permission__get_saved(const char *file_name, bruce_permission_t permission, bool *out_allowed);
```

Returns the saved decision without prompting.

e.g. for a permissions-management UI. Returns BRUCE_ERR_NOT_FOUND if
`file_name` has no saved decision yet for `permission`.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `file_name` | `const char *` | Permission file name to look up, e.g. "game.elf". |
| `permission` | `bruce_permission_t` | Permission to look up. |
| `out_allowed` | `bool *` | Receives the saved allow/deny decision. |

### Returns

`bruce_result_t`


---

## permission__set()

```c
bruce_result_t permission__set(const char *file_name, bruce_permission_t permission, bool allowed);
```

Persists an explicit allow/deny decision for `file_name`.

e.g. from a permissions-management UI.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `file_name` | `const char *` | Permission file name to set the decision for, e.g. "game.elf". |
| `permission` | `bruce_permission_t` | Permission to set. |
| `allowed` | `bool` | New allow/deny decision. |

### Returns

`bruce_result_t`


---

# `process.h`

**Running app and process control.**

All process APIs below return BRUCE_OK or a documented BRUCE_ERR_*
result. Foregrounding, switching, signalling, pausing, resuming, or
killing *another* process requires the `process` permission; a process
may always perform these operations on itself.

**Constants**

| Name | Value |
|---|---|
| `BRUCE_PROCESS_ID_INVALID` | `((bruce_process_id_t)0)` |
| `BRUCE_RESOURCE_ID_INVALID` | `((bruce_resource_id_t)0)` |
| `BRUCE_FILE_ID_INVALID` | `((bruce_file_id_t)0)` |
| `BRUCE_VIEWER_ID_INVALID` | `((bruce_viewer_id_t)0)` |
| `BRUCE_TCP_ID_INVALID` | `((bruce_tcp_id_t)0)` |
| `BRUCE_I2C_ID_INVALID` | `((bruce_i2c_id_t)0)` |
| `BRUCE_SPI_ID_INVALID` | `((bruce_spi_id_t)0)` |
| `BRUCE_SSH_ID_INVALID` | `((bruce_ssh_id_t)0)` |
| `BRUCE_PUBSUB_ID_INVALID` | `((bruce_pubsub_id_t)0)` |
| `BRUCE_PROCESS_NAME_MAX` | `64` |

---

## bruce_process_id_t()

```c
typedef uint32_t bruce_process_id_t;
```


---

## bruce_resource_id_t()

```c
typedef uint32_t bruce_resource_id_t;
```


---

## bruce_file_id_t()

```c
typedef uint32_t bruce_file_id_t;
```


---

## bruce_viewer_id_t()

```c
typedef uint32_t bruce_viewer_id_t;
```


---

## bruce_tcp_id_t()

```c
typedef uint32_t bruce_tcp_id_t;
```


---

## bruce_i2c_id_t()

```c
typedef uint32_t bruce_i2c_id_t;
```


---

## bruce_spi_id_t()

```c
typedef uint32_t bruce_spi_id_t;
```


---

## bruce_ssh_id_t()

```c
typedef uint32_t bruce_ssh_id_t;
```


---

## bruce_pubsub_id_t()

```c
typedef uint32_t bruce_pubsub_id_t;
```


---

## bruce_process_state_t()

```c
typedef enum {
    BRUCE_PROCESS_STARTING = 0,
    BRUCE_PROCESS_FOREGROUND,
    BRUCE_PROCESS_BACKGROUND,
    BRUCE_PROCESS_PAUSED,
    BRUCE_PROCESS_STOPPING,
} bruce_process_state_t;
```


---

## bruce_process_end_reason_t()

```c
typedef enum {
    BRUCE_PROCESS_EXITED = 0,
    BRUCE_PROCESS_TERMINATED,
    BRUCE_PROCESS_KILLED,
} bruce_process_end_reason_t;
```


---

## bruce_process_signal_t()

```c
typedef enum {
    BRUCE_PROCESS_SIGNAL_INT = 2,
    BRUCE_PROCESS_SIGNAL_KILL = 9,
    BRUCE_PROCESS_SIGNAL_TERM = 15,
} bruce_process_signal_t;
```


---

## bruce_process_status_t()

```c
typedef struct {
    bruce_process_end_reason_t reason;
    int exit_code;
    /* Meaningful for TERMINATED/KILLED; zero for EXITED. */
    bruce_process_signal_t signal;
} bruce_process_status_t;
```


---

## bruce_process_snapshot_t()

```c
typedef struct {
    bruce_process_id_t id;
    bruce_process_state_t state;
    char name[BRUCE_PROCESS_NAME_MAX];
    uint32_t stack_high_water_bytes;
    uint32_t stack_total_bytes;
    uint32_t cpu_percent;
    size_t memory_bytes;
    size_t swap_bytes;
    size_t resource_count;
    bool built_in;
    bool gui_requested;
    bool presentable;
} bruce_process_snapshot_t;
```


---

## process__current_id()

```c
bruce_process_id_t process__current_id(void);
```

Returns the calling process ID.

### Returns

`bruce_process_id_t`


---

## process__current_signal()

```c
bruce_process_signal_t process__current_signal(void);
```

Returns the pending cooperative INT/TERM signal for the calling process.

Zero when no cooperative signal is pending.

### Returns

`bruce_process_signal_t`


---

## process__list()

```c
bruce_result_t process__list(bruce_process_snapshot_t *snapshots, size_t capacity, size_t *out_count);
```

Lists live processes.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `snapshots` | `bruce_process_snapshot_t *` | Array to receive process snapshots. |
| `capacity` | `size_t` | Number of entries the snapshots array can hold. |
| `out_count` | `size_t *` | Receives the total number of live processes. |

### Returns

`bruce_result_t`


---

## process__snapshot()

```c
bruce_result_t process__snapshot(bruce_process_id_t process_id, bruce_process_snapshot_t *out_snapshot);
```

Reads a point-in-time snapshot of one process.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `process_id` | `bruce_process_id_t` | Process to snapshot. |
| `out_snapshot` | `bruce_process_snapshot_t *` | Receives the snapshot. |

### Returns

`bruce_result_t`


---

## process__switch_next()

```c
bruce_result_t process__switch_next(void);
```

Switches the foreground process to the next one in launch order.

### Returns

`bruce_result_t`

#### Permissions

- `process`


---

## process__switch_previous()

```c
bruce_result_t process__switch_previous(void);
```

Switches the foreground process to the previous one in launch order.

### Returns

`bruce_result_t`

#### Permissions

- `process`


---

## process__to_background()

```c
bruce_result_t process__to_background(void);
```

Moves the calling process to the background. Self-only.

### Returns

`bruce_result_t`


---

## process__to_foreground()

```c
bruce_result_t process__to_foreground(void);
```

Declares the calling process GUI-capable and gives it foreground ownership.

### Returns

`bruce_result_t`

#### Permissions

_Not permission-gated._


---

## process__foreground()

```c
bruce_result_t process__foreground(bruce_process_id_t process_id);
```

Gives another process foreground ownership.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `process_id` | `bruce_process_id_t` | Process to foreground. |

### Returns

`bruce_result_t`

#### Permissions

- `process`


---

## process__signal()

```c
bruce_result_t process__signal(bruce_process_id_t process_id, bruce_process_signal_t signal);
```

Sends a signal to a process.

INT and TERM are cooperative; KILL is forced and equivalent to
process__kill().

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `process_id` | `bruce_process_id_t` | Process to signal. |
| `signal` | `bruce_process_signal_t` | Signal to send. |

### Returns

`bruce_result_t`

#### Permissions

- `process`


---

## process__terminate()

```c
bruce_result_t process__terminate(bruce_process_id_t process_id);
```

Sends a cooperative TERM signal to a process.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `process_id` | `bruce_process_id_t` | Process to terminate. |

### Returns

`bruce_result_t`

#### Permissions

- `process`


---

## process__pause()

```c
bruce_result_t process__pause(bruce_process_id_t process_id);
```

Pauses a process.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `process_id` | `bruce_process_id_t` | Process to pause. |

### Returns

`bruce_result_t`

#### Permissions

- `process`


---

## process__resume()

```c
bruce_result_t process__resume(bruce_process_id_t process_id);
```

Resumes a paused process.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `process_id` | `bruce_process_id_t` | Process to resume. |

### Returns

`bruce_result_t`

#### Permissions

- `process`


---

## process__kill()

```c
bruce_result_t process__kill(bruce_process_id_t process_id);
```

Forcibly kills a process, equivalent to process__signal() with BRUCE_PROCESS_SIGNAL_KILL.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `process_id` | `bruce_process_id_t` | Process to kill. |

### Returns

`bruce_result_t`

#### Permissions

- `process`


---

## process__clear_signal()

```c
bruce_result_t process__clear_signal(void);
```

Cancels a pending INT/TERM on the calling process instead of stopping.

Normally a cooperative signal means "stop soon". This lets a process say
"I saw it, I'm fine, keep going" instead -- e.g. an interactive shell that
wants to just abort the line it's reading on Ctrl+C, not exit. Does
nothing if no signal is pending. Sets state back to BRUCE_PROCESS_BACKGROUND,
same as process__resume(). Self-only, no permission check. KILL can't be
cancelled this way -- it kills the process directly instead.

### Returns

`bruce_result_t`


---

## process__wait()

```c
bruce_result_t process__wait(bruce_process_id_t process_id, uint32_t timeout_ms);
```

Non-consuming wait for a process to complete.

Succeeds for either a live process that completes before the timeout or
an already-retained completion. UINT32_MAX waits forever and zero polls.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `process_id` | `bruce_process_id_t` | Process to wait for. |
| `timeout_ms` | `uint32_t` | Maximum time to wait, in milliseconds (UINT32_MAX waits forever, 0 polls). |

### Returns

`bruce_result_t`

#### Permissions

_Not permission-gated._


---

## process__wait_status()

```c
bruce_result_t
process__wait_status(bruce_process_id_t process_id, uint32_t timeout_ms, bruce_process_status_t *out_status);
```

Atomically consumes one retained completion.

Exactly one concurrent caller can succeed. UINT32_MAX waits forever and
zero polls. On failure, *out_status is unchanged.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `process_id` | `bruce_process_id_t` | Process to wait for. |
| `timeout_ms` | `uint32_t` | Maximum time to wait, in milliseconds (UINT32_MAX waits forever, 0 polls). |
| `out_status` | `bruce_process_status_t *` | Receives the process's completion status. |

### Returns

`bruce_result_t`

#### Permissions

- `process`


---

# `pubsub.h`

**Named message topics.**

(e.g. a shared I2C bus driver) to publish semantic events - touch taps,
sensor readings - that any number of other processes, including ELF/JS
apps, can subscribe to without ever touching the underlying hardware or
bus themselves. It exists so that when a physical resource can only
safely have one owner (see core/device/board_i2c.h), that owner is the
only thing that ever opens it; everyone else reads its output as
messages.

Topics are plain strings with no reservation system. Publishing is
restricted to built-in Core processes so an ELF/JS app cannot forge,
e.g., "device.touch" and fool another app; any process may subscribe.

**Constants**

| Name | Value |
|---|---|
| `BRUCE_PUBSUB_TOPIC_MAX` | `32` |
| `BRUCE_PUBSUB_MESSAGE_MAX` | `32` |

---

## bruce_pubsub_message_t()

```c
typedef struct {
    uint8_t data[BRUCE_PUBSUB_MESSAGE_MAX];
    size_t size;
    uint64_t timestamp_ms;
} bruce_pubsub_message_t;
```


---

## pubsub__publish()

```c
bruce_result_t pubsub__publish(const char *topic, const void *data, size_t size);
```

Publishes `data` (up to BRUCE_PUBSUB_MESSAGE_MAX bytes) to `topic`.

Delivered to every subscription currently open on that topic. If a
subscriber's queue is full, its oldest unread message is dropped to make
room for this one - subscribers are expected to drain promptly. Returns
BRUCE_ERR_PERMISSION for a non-built-in caller, BRUCE_ERR_INVALID_ARGUMENT
for a NULL/empty topic or an oversized message.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `topic` | `const char *` | Topic name to publish to. |
| `data` | `const void *` | Message bytes, up to BRUCE_PUBSUB_MESSAGE_MAX. |
| `size` | `size_t` | Number of bytes in data. |

### Returns

`bruce_result_t`

#### Permissions

- _Built-in apps only -- external ELF/JS/WASM callers are always denied._


---

## pubsub__subscribe()

```c
bruce_result_t pubsub__subscribe(const char *topic, bruce_pubsub_id_t *out_sub);
```

Subscribes the calling process to `topic`.

Delivery starts from the first message published after this call
returns; nothing published earlier is replayed.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `topic` | `const char *` | Topic name to subscribe to. |
| `out_sub` | `bruce_pubsub_id_t *` | Receives the new subscription handle. |

### Returns

`bruce_result_t`


---

## pubsub__unsubscribe()

```c
bruce_result_t pubsub__unsubscribe(bruce_pubsub_id_t sub);
```

Ends a subscription.

Subscriptions also close automatically when the owning process exits.
Returns BRUCE_ERR_NOT_FOUND if `sub` does not belong to the calling
process.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `sub` | `bruce_pubsub_id_t` | Subscription to end. |

### Returns

`bruce_result_t`


---

## pubsub__read()

```c
bruce_result_t pubsub__read(bruce_pubsub_id_t sub, bruce_pubsub_message_t *out_message, uint32_t timeout_ms);
```

Pops the next queued message for `sub`.

Returns BRUCE_ERR_NOT_FOUND if `sub` does not belong to the caller.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `sub` | `bruce_pubsub_id_t` | Subscription to read from. |
| `out_message` | `bruce_pubsub_message_t *` | Receives the next queued message. |
| `timeout_ms` | `uint32_t` | 0 for non-blocking, >0 to block up to that many milliseconds, or 0xFFFFFFFF to block indefinitely. |

### Returns

`bruce_result_t`


---

# `result.h`

**Common Core SDK result codes.**

Functions returning bruce_result_t use BRUCE_OK on success and one of the
negative BRUCE_ERR_* values below on failure. Other return conventions
are documented on the individual function. Result values are part of the
Core ABI and must not be renumbered.

---

## bruce_result_t()

```c
typedef enum {
    BRUCE_OK = 0,
    BRUCE_ERR_INVALID_ARGUMENT = -1,
    BRUCE_ERR_NOT_FOUND = -2,
    BRUCE_ERR_PERMISSION = -3,
    BRUCE_ERR_BUSY = -4,
    BRUCE_ERR_NO_MEMORY = -5,
    BRUCE_ERR_IO = -6,
    BRUCE_ERR_UNSUPPORTED = -7,
    BRUCE_ERR_INVALID_STATE = -8,
    BRUCE_ERR_CANCELLED = -9,
    BRUCE_ERR_TIMEOUT = -10,
    BRUCE_ERR_ALREADY_EXISTS = -11,
    BRUCE_ERR_INVALID_PATH = -12,
    BRUCE_ERR_MANIFEST_INVALID = -13,
    BRUCE_ERR_ABI_MISMATCH = -14,
    BRUCE_ERR_TARGET_MISMATCH = -15,
    BRUCE_ERR_RESOURCE_LIMIT = -16,
    BRUCE_ERR_NOT_FOREGROUND = -17,
    BRUCE_ERR_NOT_INITIALIZED = -18,
    BRUCE_ERR_INTERNAL = -19,
} bruce_result_t;
```

Common Core SDK result codes.

Functions returning bruce_result_t use BRUCE_OK on success and one of the
negative BRUCE_ERR_* values below on failure. Other return conventions
are documented on the individual function. Result values are part of the
Core ABI and must not be renumbered.


---

## result__to_string()

```c
const char *result__to_string(int result);
```

Translates a bruce_result_t into a short human-readable description.

(or the BRUCE_OK/BRUCE_ERR_* range of any int returned by the
app_runner__run*() family), e.g. BRUCE_ERR_NOT_FOUND -> "Not found".
Positive values (process ids) and unrecognized codes return "Unknown
error". Always returns a non-NULL, statically-allocated string.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `result` | `int` | Result code to translate. |

### Returns

`const char *`


---

# `runtime.h`

**Time, delays, timers, and isGUI request.**

**Constants**

| Name | Value |
|---|---|
| `BRUCE_TIMER_ID_INVALID` | `((bruce_timer_id_t)0)` |

---

## bruce_timer_id_t()

```c
typedef uint32_t bruce_timer_id_t;
```


---

## runtime__now()

```c
uint64_t runtime__now(void);
```

Monotonic milliseconds since boot.

Use differences between readings for elapsed-time measurement; this is
not wall-clock time.

### Returns

`uint64_t`


---

## runtime__sleep()

```c
bruce_result_t runtime__sleep(uint32_t milliseconds);
```

Sleeps/delays for the given duration.

Both return BRUCE_OK once the full duration has elapsed and
BRUCE_ERR_CANCELLED if interrupted early. runtime__sleep is interrupted
when the calling process is paused (it blocks until resumed, then keeps
waiting), stopped, or foregrounded while it was background when the call
began. runtime__delay honours pause/stop the same way but is never
interrupted merely by foregrounding; it waits its requested duration.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `milliseconds` | `uint32_t` | Duration to sleep, in milliseconds. |

### Returns

`bruce_result_t`


---

## runtime__delay()

```c
bruce_result_t runtime__delay(uint32_t milliseconds);
```

Delays for the given duration, ignoring foregrounding (see runtime__sleep()).

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `milliseconds` | `uint32_t` | Duration to delay, in milliseconds. |

### Returns

`bruce_result_t`


---

## runtime__timer_start()

```c
bruce_result_t runtime__timer_start(
    uint32_t period_us, volatile uint32_t *counter, bruce_timer_id_t *out_timer_id
);
```

Starts a process-owned periodic timer that atomically increments `counter`.

Every `period_us` microseconds. Timer callbacks execute entirely inside
Core and never call application code, so one process's timer cannot
block another. The timer is automatically stopped when its owner exits
or is killed; callers may stop it early with runtime__timer_stop().

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `period_us` | `uint32_t` | Tick period in microseconds. |
| `counter` | `volatile uint32_t *` | Counter atomically incremented on every tick. |
| `out_timer_id` | `bruce_timer_id_t *` | Receives the new timer's id. |

### Returns

`bruce_result_t`


---

## runtime__timer_wait()

```c
bruce_result_t runtime__timer_wait(bruce_timer_id_t timer_id, uint32_t timeout_ms);
```

Waits for the next tick from a timer owned by the calling process.

A zero timeout polls; UINT32_MAX waits indefinitely.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `timer_id` | `bruce_timer_id_t` | Timer to wait on. |
| `timeout_ms` | `uint32_t` | Maximum time to wait, in milliseconds (0 polls, UINT32_MAX waits indefinitely). |

### Returns

`bruce_result_t`


---

## runtime__timer_stop()

```c
bruce_result_t runtime__timer_stop(bruce_timer_id_t timer_id);
```

Stops a timer started with runtime__timer_start().

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `timer_id` | `bruce_timer_id_t` | Timer to stop. |

### Returns

`bruce_result_t`


---

## runtime__gui_requested()

```c
bool runtime__gui_requested(void);
```

Checks whether the current app is running in GUI mode.

Use this to decide whether the app should draw to the display or interact
through standard input and output.

### Returns

`bool`


---

# `spi.h`

**SPI device access.**

**Constants**

| Name | Value |
|---|---|
| `BRUCE_SPI_MAX_TRANSFER_SIZE` | `64u` |

---

## bruce_spi_device_config_t()

```c
typedef struct {
    int sck;
    int miso;
    int mosi;
    int cs;
    uint32_t clock_hz;
    uint8_t mode;
} bruce_spi_device_config_t;
```


---

## spi__open()

```c
bruce_result_t spi__open(const bruce_spi_device_config_t *config, bruce_spi_id_t *out_device);
```

Opens an SPI device on the board's external hardware SPI bus.

Devices with the same SCK/MISO/MOSI tuple share that bus. Device handles
belong to the calling process and close automatically on process exit.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `config` | `const bruce_spi_device_config_t *` | Device configuration (pins, clock, mode). |
| `out_device` | `bruce_spi_id_t *` | Receives the new device handle. |

### Returns

`bruce_result_t`

#### Permissions

- `gpio`


---

## spi__transfer()

```c
bruce_result_t spi__transfer(bruce_spi_id_t device, const void *tx_data, void *rx_data, size_t size);
```

Performs a full-duplex SPI transfer.

`tx_data` may be NULL for receive-only transfers and `rx_data` may be
NULL to discard input.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `device` | `bruce_spi_id_t` | Device handle from spi__open(). |
| `tx_data` | `const void *` | Bytes to transmit, or NULL for receive-only. |
| `rx_data` | `void *` | Buffer to receive bytes, or NULL to discard input. |
| `size` | `size_t` | Number of bytes to transfer. |

### Returns

`bruce_result_t`

#### Permissions

- `gpio`


---

## spi__close()

```c
bruce_result_t spi__close(bruce_spi_id_t device);
```

Closes an SPI device opened by spi__open().

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `device` | `bruce_spi_id_t` | Device handle to close. |

### Returns

`bruce_result_t`

#### Permissions

- `gpio`


---

# `ssh.h`

**SSH connections and remote shells.**

**Constants**

| Name | Value |
|---|---|
| `BRUCE_SSH_HOST_KEY_SHA256_SIZE` | `32` |
| `BRUCE_SSH_PRIVATE_KEY_MAX_SIZE` | `512` |
| `BRUCE_SSH_PUBLIC_KEY_MAX_SIZE` | `256` |

---

## bruce_ssh_key_type_t()

```c
typedef enum {
    BRUCE_SSH_KEY_ECDSA_P256 = 0,
    BRUCE_SSH_KEY_ED25519,
} bruce_ssh_key_type_t;
```


---

## ssh__connect()

```c
bruce_result_t ssh__connect(
    const char *host, uint16_t port, uint32_t timeout_ms, bruce_ssh_id_t *out_session
);
```

Connects and reads the negotiated host key.

SSH sessions are opaque, owned by the calling process, and close
automatically when that process exits. The caller must verify the
host-key fingerprint before authenticating.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `host` | `const char *` | Hostname or IP address to connect to. |
| `port` | `uint16_t` | TCP port to connect to. |
| `timeout_ms` | `uint32_t` | Connection timeout in milliseconds. |
| `out_session` | `bruce_ssh_id_t *` | Receives the new session handle. |

### Returns

`bruce_result_t`

#### Permissions

- `ssh`


---

## ssh__host_key_sha256()

```c
bruce_result_t ssh__host_key_sha256(
    bruce_ssh_id_t session, uint8_t out_fingerprint[BRUCE_SSH_HOST_KEY_SHA256_SIZE]
);
```

Reads the negotiated host key's SHA-256 fingerprint.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `session` | `bruce_ssh_id_t` | Session to query. |
| `out_fingerprint` | `uint8_t [BRUCE_SSH_HOST_KEY_SHA256_SIZE]` | Receives the SHA-256 fingerprint. |

### Returns

`bruce_result_t`

#### Permissions

- `ssh`


---

## ssh__verify_host_key_sha256()

```c
bruce_result_t ssh__verify_host_key_sha256(
    bruce_ssh_id_t session, const uint8_t expected[BRUCE_SSH_HOST_KEY_SHA256_SIZE]
);
```

Compares an expected fingerprint to the negotiated host key.

Authentication remains unavailable until this succeeds.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `session` | `bruce_ssh_id_t` | Session to verify. |
| `expected` | `const uint8_t [BRUCE_SSH_HOST_KEY_SHA256_SIZE]` | Expected SHA-256 fingerprint to compare against. |

### Returns

`bruce_result_t`

#### Permissions

- `ssh`


---

## ssh__authenticate_password()

```c
bruce_result_t ssh__authenticate_password(
    bruce_ssh_id_t session, const char *username, const char *password, uint32_t timeout_ms
);
```

Authenticates a session with a password.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `session` | `bruce_ssh_id_t` | Session to authenticate. |
| `username` | `const char *` | Username to authenticate as. |
| `password` | `const char *` | Password to authenticate with. |
| `timeout_ms` | `uint32_t` | Authentication timeout in milliseconds. |

### Returns

`bruce_result_t`

#### Permissions

- `ssh`


---

## ssh__generate_keypair_ex()

```c
bruce_result_t ssh__generate_keypair_ex(
    bruce_ssh_key_type_t type, char *private_key, size_t private_capacity,
    size_t *out_private_size, char *public_key, size_t public_capacity,
    size_t *out_public_size
);
```

Generates the selected key type.

Ed25519 private keys use the unencrypted OpenSSH format; ECDSA P-256
uses SEC1 PEM. Text sizes exclude the trailing NUL, and the caller owns
both buffers.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `type` | `bruce_ssh_key_type_t` | Key type to generate. |
| `private_key` | `char *` | Buffer to receive the generated private key text. |
| `private_capacity` | `size_t` | Size of private_key in bytes. |
| `out_private_size` | `size_t *` | Receives the private key text length (excluding NUL). |
| `public_key` | `char *` | Buffer to receive the generated public key text. |
| `public_capacity` | `size_t` | Size of public_key in bytes. |
| `out_public_size` | `size_t *` | Receives the public key text length (excluding NUL). |

### Returns

`bruce_result_t`

#### Permissions

- `ssh`


---

## ssh__authenticate_key()

```c
bruce_result_t ssh__authenticate_key(
    bruce_ssh_id_t session, const char *username, const void *private_key,
    size_t private_key_size, uint32_t timeout_ms
);
```

Authenticates with an unencrypted ECDSA P-256 SEC1 PEM key or Ed25519 private key.

(or an unencrypted OpenSSH Ed25519 private key).

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `session` | `bruce_ssh_id_t` | Session to authenticate. |
| `username` | `const char *` | Username to authenticate as. |
| `private_key` | `const void *` | Private key bytes (SEC1 PEM or OpenSSH format). |
| `private_key_size` | `size_t` | Number of bytes in private_key. |
| `timeout_ms` | `uint32_t` | Authentication timeout in milliseconds. |

### Returns

`bruce_result_t`

#### Permissions

- `ssh`


---

## ssh__open_shell()

```c
bruce_result_t ssh__open_shell(
    bruce_ssh_id_t session, const char *terminal_type, uint16_t columns, uint16_t rows,
    uint32_t timeout_ms
);
```

Opens an interactive shell channel on an authenticated session.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `session` | `bruce_ssh_id_t` | Authenticated session to open a shell on. |
| `terminal_type` | `const char *` | Terminal type to request, e.g. "xterm". |
| `columns` | `uint16_t` | Initial terminal width in columns. |
| `rows` | `uint16_t` | Initial terminal height in rows. |
| `timeout_ms` | `uint32_t` | Open timeout in milliseconds. |

### Returns

`bruce_result_t`

#### Permissions

- `ssh`


---

## ssh__resize_pty()

```c
bruce_result_t ssh__resize_pty(
    bruce_ssh_id_t session, uint16_t columns, uint16_t rows, uint32_t timeout_ms
);
```

Notifies the remote end of a terminal size change on an open shell.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `session` | `bruce_ssh_id_t` | Session with an open shell. |
| `columns` | `uint16_t` | New terminal width in columns. |
| `rows` | `uint16_t` | New terminal height in rows. |
| `timeout_ms` | `uint32_t` | Request timeout in milliseconds. |

### Returns

`bruce_result_t`

#### Permissions

- `ssh`


---

## ssh__read()

```c
bruce_result_t ssh__read(
    bruce_ssh_id_t session, bool stderr_stream, void *buffer, size_t capacity,
    uint32_t timeout_ms, size_t *out_size
);
```

Reads from an SSH channel.

EOF is BRUCE_OK with *out_size == 0. Set `stderr_stream` to read the SSH
channel's extended-data stream.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `session` | `bruce_ssh_id_t` | Session to read from. |
| `stderr_stream` | `bool` | If true, reads the channel's extended-data (stderr) stream. |
| `buffer` | `void *` | Buffer to receive read bytes. |
| `capacity` | `size_t` | Size of buffer in bytes. |
| `timeout_ms` | `uint32_t` | Read timeout in milliseconds. |
| `out_size` | `size_t *` | Receives the number of bytes read. |

### Returns

`bruce_result_t`

#### Permissions

- `ssh`


---

## ssh__write()

```c
bruce_result_t ssh__write(
    bruce_ssh_id_t session, const void *buffer, size_t size, uint32_t timeout_ms,
    size_t *out_size
);
```

Writes to an SSH channel.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `session` | `bruce_ssh_id_t` | Session to write to. |
| `buffer` | `const void *` | Bytes to write. |
| `size` | `size_t` | Number of bytes in buffer. |
| `timeout_ms` | `uint32_t` | Write timeout in milliseconds. |
| `out_size` | `size_t *` | Receives the number of bytes written. |

### Returns

`bruce_result_t`

#### Permissions

- `ssh`


---

## ssh__close()

```c
bruce_result_t ssh__close(bruce_ssh_id_t session);
```

Closes an SSH session opened by ssh__connect().

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `session` | `bruce_ssh_id_t` | Session to close. |

### Returns

`bruce_result_t`

#### Permissions

- `ssh`


---

# `status_icon.h`

**Status bar icons.**

Not permission-gated; the registry is global and unrestricted.

**Constants**

| Name | Value |
|---|---|
| `BRUCE_STATUS_ICON_KEY_MAX` | `32` |
| `BRUCE_STATUS_ICON_MAX` | `16` |
| `BRUCE_STATUS_ICON_MAX_WIDTH` | `16` |
| `BRUCE_STATUS_ICON_MAX_HEIGHT` | `16` |
| `BRUCE_STATUS_ICON_BITMAP_MAX` | `32` |

---

## bruce_status_icon_t()

```c
typedef struct {
    char key[BRUCE_STATUS_ICON_KEY_MAX];
    uint8_t width;
    uint8_t height;
    uint8_t bitmap[BRUCE_STATUS_ICON_BITMAP_MAX];
} bruce_status_icon_t;
```


---

## status_icon__push()

```c
bruce_result_t status_icon__push(const char *key, const uint8_t *bitmap, uint8_t width, uint8_t height);
```

Publishes/replaces the icon at `key`.

The registry is global and unrestricted. Existing keys are replaced.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `key` | `const char *` | Icon key to publish under. |
| `bitmap` | `const uint8_t *` | 1bpp, row-major, MSB-first bitmap data, each row byte-aligned. |
| `width` | `uint8_t` | Bitmap width in pixels (up to BRUCE_STATUS_ICON_MAX_WIDTH). |
| `height` | `uint8_t` | Bitmap height in pixels (up to BRUCE_STATUS_ICON_MAX_HEIGHT). |

### Returns

`bruce_result_t`


---

## status_icon__push_named()

```c
bruce_result_t status_icon__push_named(const char *key, const char *icon_name);
```

Looks up a built-in icon by name, scales it to fit, and publishes it under key.

Scales it proportionally to fit the status bar limit.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `key` | `const char *` | Icon key to publish under. |
| `icon_name` | `const char *` | Built-in icon name (see icon.h). |

### Returns

`bruce_result_t`


---

## status_icon__remove()

```c
bruce_result_t status_icon__remove(const char *key);
```

Removes the icon published at `key`.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `key` | `const char *` | Icon key to remove. |

### Returns

`bruce_result_t`


---

## status_icon__list()

```c
bruce_result_t
status_icon__list(bruce_status_icon_t *icons, size_t capacity, size_t *out_count, uint32_t *out_revision);
```

Returns the total entry count and current revision.

Up to capacity entries are copied in lexicographic key order.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `icons` | `bruce_status_icon_t *` | Array to receive icon entries. |
| `capacity` | `size_t` | Number of entries the icons array can hold. |
| `out_count` | `size_t *` | Receives the total number of published icons. |
| `out_revision` | `uint32_t *` | Receives the current registry revision. |

### Returns

`bruce_result_t`


---

## status_icon__get()

```c
bruce_result_t
status_icon__get(size_t index, bruce_status_icon_t *out_icon, uint32_t *out_revision);
```

Copies one entry by its lexicographic list index.

Without requiring callers to reserve a full BRUCE_STATUS_ICON_MAX
snapshot. The optional revision is captured under the same lock as the
icon.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `index` | `size_t` | Zero-based lexicographic index of the icon to copy. |
| `out_icon` | `bruce_status_icon_t *` | Receives the icon entry. |
| `out_revision` | `uint32_t *` | Receives the current registry revision, or NULL. |

### Returns

`bruce_result_t`


---

# `stdio.h`

**Standard input and output.**

**Constants**

| Name | Value |
|---|---|
| `BRUCE_STDIO_SESSION_INVALID` | `((bruce_stdio_session_t)0)` |

---

## bruce_stdio_session_t()

```c
typedef uint32_t bruce_stdio_session_t;
```


---

## stdio__read_line()

```c
int stdio__read_line(char *buffer, size_t buffer_size, bool mask_input);
```

Read a line from stdin, waiting if the console driver is non-blocking.

Echoes characters unless mask_input is true. The returned string does
NOT include the trailing newline. Returns the number of characters read,
BRUCE_ERR_CANCELLED if the calling process receives cooperative
INT/TERM, or -1 on invalid arguments / EOF.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `buffer` | `char *` | Buffer to receive the line (without a trailing newline). |
| `buffer_size` | `size_t` | Size of buffer in bytes. |
| `mask_input` | `bool` | If true, suppresses echoing typed characters. |

### Returns

`int`


---

## stdio__read()

```c
bruce_result_t stdio__read(void *buffer, size_t capacity, uint32_t timeout_ms, size_t *out_size);
```

Reads up to `capacity` raw bytes from stdin without echoing.

A zero timeout polls and UINT32_MAX waits indefinitely. Finite waits use
the original absolute deadline. Returns BRUCE_ERR_TIMEOUT when no input
arrives before that deadline, or BRUCE_ERR_CANCELLED if the calling
process receives cooperative INT/TERM. Cancellation takes precedence
over queued input.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `buffer` | `void *` | Buffer to receive read bytes. |
| `capacity` | `size_t` | Size of buffer in bytes. |
| `timeout_ms` | `uint32_t` | 0 to poll, or UINT32_MAX to wait indefinitely. |
| `out_size` | `size_t *` | Receives the number of bytes read. |

### Returns

`bruce_result_t`


---

## stdio__write()

```c
bruce_result_t stdio__write(const void *data, size_t size);
```

Writes app-visible output to the calling process's routed session.

Or to the physical serial console when no session is routed. Core
diagnostics should continue to use the normal logging/stdio functions.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `data` | `const void *` | Bytes to write. |
| `size` | `size_t` | Number of bytes in data. |

### Returns

`bruce_result_t`


---

## stdio__write_to()

```c
bruce_result_t stdio__write_to(bruce_stdio_session_t session, const void *data, size_t size);
```

Same as stdio__write(), but targets an explicit session.

Instead of the calling process's own routed session -- for the rare case
(e.g. a background service rendering console fallback output on another
process's behalf, see modules/notification_service) where the writer and
the intended reader are not the same process. BRUCE_STDIO_SESSION_INVALID
writes to the physical serial console, same as stdio__write() with no
session routed.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `session` | `bruce_stdio_session_t` | Session to write to, or BRUCE_STDIO_SESSION_INVALID for the physical console. |
| `data` | `const void *` | Bytes to write. |
| `size` | `size_t` | Number of bytes in data. |

### Returns

`bruce_result_t`


---

## stdio__printf()

```c
int stdio__printf(const char *format, ...) __attribute__((format(printf, 1, 2)));
```

printf-style form of bruce_stdio_write().

Returns the number of formatted bytes written, or a negative BRUCE_ERR_*
value.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `format` | `const char *` | printf-style format string. |
| `...` | `` | Arguments matching format's conversion specifiers. |

### Returns

`int`


---

## stdio__vprintf()

```c
int stdio__vprintf(const char *format, va_list args) __attribute__((format(printf, 1, 0)));
```

va_list form of stdio__printf().

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `format` | `const char *` | printf-style format string. |
| `args` | `va_list` | Arguments matching format, as a va_list. |

### Returns

`int`


---

## stdio__session_create()

```c
bruce_result_t stdio__session_create(bruce_stdio_session_t *out_session);
```

Creates a process-owned, bounded input/output session.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `out_session` | `bruce_stdio_session_t *` | Receives the new session handle. |

### Returns

`bruce_result_t`


---

## stdio__session_close()

```c
bruce_result_t stdio__session_close(bruce_stdio_session_t session);
```

Closes a session created by stdio__session_create().

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `session` | `bruce_stdio_session_t` | Session to close. |

### Returns

`bruce_result_t`


---

## stdio__session_route_children()

```c
bruce_result_t stdio__session_route_children(bruce_stdio_session_t session);
```

Routes subsequently launched child processes' stdio through `session`.

Routing a session makes subsequently launched child processes use it
through bruce_stdio_read(), bruce_stdio_write(), and stdio__printf();
call route_children(INVALID) after launching to restore the default of
routing children into the calling process's own session -- it does not
disconnect them, so plain commands launched afterward keep going to the
same terminal as before.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `session` | `bruce_stdio_session_t` | Session to route children through, or BRUCE_STDIO_SESSION_INVALID to restore the default. |

### Returns

`bruce_result_t`


---

## stdio__session_write_input()

```c
bruce_result_t stdio__session_write_input(bruce_stdio_session_t session, const void *data, size_t size);
```

Writes input bytes into a session, as if typed by its consumer.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `session` | `bruce_stdio_session_t` | Session to write into. |
| `data` | `const void *` | Bytes to write. |
| `size` | `size_t` | Number of bytes in data. |

### Returns

`bruce_result_t`


---

## stdio__session_read_output()

```c
bruce_result_t
stdio__session_read_output(bruce_stdio_session_t session, void *buffer, size_t capacity, size_t *out_size);
```

Reads output bytes a session's owning process has written.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `session` | `bruce_stdio_session_t` | Session to read from. |
| `buffer` | `void *` | Buffer to receive read bytes. |
| `capacity` | `size_t` | Size of buffer in bytes. |
| `out_size` | `size_t *` | Receives the number of bytes read. |

### Returns

`bruce_result_t`


---

# `storage.h`

**File and directory access.**

Each fallible storage API returns BRUCE_OK or BRUCE_ERR_PERMISSION,
BRUCE_ERR_NOT_FOUND, BRUCE_ERR_INVALID_PATH, BRUCE_ERR_IO, or a related
BRUCE_ERR_* result. Open files are process-owned and automatically
closed at process exit/kill even if the caller never calls
storage__close() itself.

storage__open()/storage__list() check the calling process's `storage`
permission (built-ins always pass) and additionally refuse
"/config/bruce.conf", "/config/permissions.json", and their atomic-write
".tmp" siblings entirely - BRUCE_ERR_PERMISSION - regardless of any
granted permission; storage__list() silently omits those two names
rather than erroring. Every other mounted path (LittleFS or SD) is
otherwise reachable. `path` must be an absolute, normalized path with no
"." or ".." components, else BRUCE_ERR_INVALID_PATH. A file handle can
only be used by the process that opened it; any other process (or a
stale/unknown id) gets BRUCE_ERR_NOT_FOUND/BRUCE_ERR_PERMISSION from
storage__read()/write()/seek()/close().

**Constants**

| Name | Value |
|---|---|
| `BRUCE_STORAGE_PATH_MAX` | `192` |
| `BRUCE_STORAGE_NAME_MAX` | `96` |

---

## bruce_storage_open_flags_t()

```c
typedef enum {
    BRUCE_STORAGE_OPEN_READ = 1u << 0,
    BRUCE_STORAGE_OPEN_WRITE = 1u << 1,
    BRUCE_STORAGE_OPEN_APPEND = 1u << 2,
    BRUCE_STORAGE_OPEN_CREATE = 1u << 3,
    BRUCE_STORAGE_OPEN_TRUNCATE = 1u << 4,
} bruce_storage_open_flags_t;
```


---

## bruce_storage_entry_type_t()

```c
typedef enum {
    BRUCE_STORAGE_ENTRY_FILE,
    BRUCE_STORAGE_ENTRY_DIRECTORY,
} bruce_storage_entry_type_t;
```


---

## bruce_storage_entry_t()

```c
typedef struct {
    char name[BRUCE_STORAGE_NAME_MAX];
    bruce_storage_entry_type_t type;
    size_t size;
} bruce_storage_entry_t;
```


---

## storage__exists()

```c
bruce_result_t storage__exists(const char *path, bool *out_exists);
```

Reports whether a path exists.

Applies the same permission, normalization, and protected-path policy as
storage__open().

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `path` | `const char *` | Path to check. |
| `out_exists` | `bool *` | Receives whether the path exists. |

### Returns

`bruce_result_t`

#### Permissions

- `storage`


---

## storage__open()

```c
bruce_result_t storage__open(const char *path, uint32_t flags, bruce_file_id_t *out_file);
```

Opens a file.

See the module-level notes above for the permission, normalization, and
protected-path policy both this and storage__list() share.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `path` | `const char *` | Absolute, normalized path to open. |
| `flags` | `uint32_t` | Bitwise OR of bruce_storage_open_flags_t values. |
| `out_file` | `bruce_file_id_t *` | Receives the new file handle. |

### Returns

`bruce_result_t`

#### Permissions

- `storage`


---

## storage__read()

```c
bruce_result_t storage__read(bruce_file_id_t file, void *buffer, size_t capacity, size_t *out_size);
```

Reads from an open file.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `file` | `bruce_file_id_t` | File handle from storage__open(). |
| `buffer` | `void *` | Buffer to receive read bytes. |
| `capacity` | `size_t` | Size of buffer in bytes. |
| `out_size` | `size_t *` | Receives the number of bytes read. |

### Returns

`bruce_result_t`


---

## storage__write()

```c
bruce_result_t storage__write(bruce_file_id_t file, const void *buffer, size_t size, size_t *out_size);
```

Writes to an open file.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `file` | `bruce_file_id_t` | File handle from storage__open(). |
| `buffer` | `const void *` | Bytes to write. |
| `size` | `size_t` | Number of bytes in buffer. |
| `out_size` | `size_t *` | Receives the number of bytes written. |

### Returns

`bruce_result_t`


---

## storage__seek()

```c
bruce_result_t storage__seek(bruce_file_id_t file, int64_t offset, int whence, uint64_t *out_position);
```

Seeks an open file.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `file` | `bruce_file_id_t` | File handle from storage__open(). |
| `offset` | `int64_t` | Offset to seek by, interpreted per whence. |
| `whence` | `int` | Reference point for offset (SEEK_SET/SEEK_CUR/SEEK_END). |
| `out_position` | `uint64_t *` | Receives the new absolute file position. |

### Returns

`bruce_result_t`


---

## storage__close()

```c
bruce_result_t storage__close(bruce_file_id_t file);
```

Closes an open file.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `file` | `bruce_file_id_t` | File handle to close. |

### Returns

`bruce_result_t`


---

## storage__list()

```c
bruce_result_t
storage__list(const char *path, bruce_storage_entry_t *entries, size_t capacity, size_t *out_count);
```

Lists a directory.

See the module-level notes above for the permission, normalization, and
protected-path policy both this and storage__open() share.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `path` | `const char *` | Absolute, normalized directory path to list. |
| `entries` | `bruce_storage_entry_t *` | Array to receive directory entries. |
| `capacity` | `size_t` | Number of entries the entries array can hold. |
| `out_count` | `size_t *` | Receives the total number of entries available. |

### Returns

`bruce_result_t`

#### Permissions

- `storage`


---

## storage__mkdir()

```c
bruce_result_t storage__mkdir(const char *path);
```

Creates one directory.

The parent must already exist. Returns BRUCE_OK when the path already
names a directory.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `path` | `const char *` | Directory path to create. |

### Returns

`bruce_result_t`


---

## storage__remove()

```c
bruce_result_t storage__remove(const char *path);
```

Removes one file or empty directory.

Root and protected configuration files are never removable through the
public SDK.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `path` | `const char *` | Path of the file or empty directory to remove. |

### Returns

`bruce_result_t`


---

## storage__rename()

```c
bruce_result_t storage__rename(const char *from, const char *to);
```

Renames a file or directory without replacing an existing destination.

Both paths must be on the same mounted filesystem.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `from` | `const char *` | Existing path. |
| `to` | `const char *` | New path; must not already exist. |

### Returns

`bruce_result_t`


---

## storage__get_usage()

```c
bruce_result_t storage__get_usage(const char *path, size_t *total_bytes, size_t *used_bytes);
```

Returns filesystem capacity and used bytes for the volume containing path.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `path` | `const char *` | Path identifying which mounted volume to report on. |
| `total_bytes` | `size_t *` | Receives the volume's total capacity in bytes. |
| `used_bytes` | `size_t *` | Receives the volume's used bytes. |

### Returns

`bruce_result_t`


---

# `tcp.h`

**TCP network connections.**

**Constants**

| Name | Value |
|---|---|
| `BRUCE_TCP_HOST_MAX` | `64` |

---

## bruce_tcp_endpoint_t()

```c
typedef struct {
    char host[BRUCE_TCP_HOST_MAX];
    uint16_t port;
} bruce_tcp_endpoint_t;
```


---

## tcp__connect()

```c
bruce_result_t tcp__connect(const char *host, uint16_t port, uint32_t timeout_ms, bruce_tcp_id_t *out_socket);
```

Connects to a TCP endpoint.

TCP handles are owned by the calling process and are closed
automatically when that process exits.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `host` | `const char *` | Hostname or IP address to connect to. |
| `port` | `uint16_t` | TCP port to connect to. |
| `timeout_ms` | `uint32_t` | Connection timeout in milliseconds (0 polls). |
| `out_socket` | `bruce_tcp_id_t *` | Receives the new socket handle. |

### Returns

`bruce_result_t`

#### Permissions

- `wifi`


---

## tcp__listen()

```c
bruce_result_t tcp__listen(uint16_t port, bruce_tcp_id_t *out_listener);
```

Starts listening for inbound TCP connections on a port.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `port` | `uint16_t` | TCP port to listen on. |
| `out_listener` | `bruce_tcp_id_t *` | Receives the new listener handle. |

### Returns

`bruce_result_t`

#### Permissions

- `wifi`


---

## tcp__accept()

```c
bruce_result_t tcp__accept(
    bruce_tcp_id_t listener, uint32_t timeout_ms, bruce_tcp_id_t *out_socket, bruce_tcp_endpoint_t *out_peer
);
```

Accepts one inbound connection on a listener.

A zero timeout polls.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `listener` | `bruce_tcp_id_t` | Listener handle from tcp__listen(). |
| `timeout_ms` | `uint32_t` | Time to wait for a connection, in milliseconds (0 polls). |
| `out_socket` | `bruce_tcp_id_t *` | Receives the new connected socket handle. |
| `out_peer` | `bruce_tcp_endpoint_t *` | Receives the peer's address/port. |

### Returns

`bruce_result_t`

#### Permissions

- `wifi`


---

## tcp__read()

```c
bruce_result_t
tcp__read(bruce_tcp_id_t socket, void *buffer, size_t capacity, uint32_t timeout_ms, size_t *out_size);
```

Reads from a connected TCP socket.

A zero timeout polls. TCP EOF is BRUCE_OK with *out_size == 0.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `socket` | `bruce_tcp_id_t` | Connected socket handle. |
| `buffer` | `void *` | Buffer to receive read bytes. |
| `capacity` | `size_t` | Size of buffer in bytes. |
| `timeout_ms` | `uint32_t` | Read timeout in milliseconds (0 polls). |
| `out_size` | `size_t *` | Receives the number of bytes read. |

### Returns

`bruce_result_t`

#### Permissions

- `wifi`


---

## tcp__write()

```c
bruce_result_t
tcp__write(bruce_tcp_id_t socket, const void *buffer, size_t size, uint32_t timeout_ms, size_t *out_size);
```

Writes to a connected TCP socket.

A zero timeout polls.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `socket` | `bruce_tcp_id_t` | Connected socket handle. |
| `buffer` | `const void *` | Bytes to write. |
| `size` | `size_t` | Number of bytes in buffer. |
| `timeout_ms` | `uint32_t` | Write timeout in milliseconds (0 polls). |
| `out_size` | `size_t *` | Receives the number of bytes written. |

### Returns

`bruce_result_t`

#### Permissions

- `wifi`


---

## tcp__close()

```c
bruce_result_t tcp__close(bruce_tcp_id_t socket);
```

Closes a TCP socket or listener.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `socket` | `bruce_tcp_id_t` | Handle to close. |

### Returns

`bruce_result_t`

#### Permissions

- `wifi`


---

# `tty.h`

**Terminal size and mode settings.**

BruceOS's stdio__session_t (core/stdio/stdio.c) already behaves like a
pty: it's a process-owned, bounded input/output byte channel inherited
transitively down a process tree (see stdio__session_route_children).
There is no separate line-discipline layer -- nothing at the OS level
echoes or edits input, so "raw mode" is already the default behavior for
any reader that isn't itself an editor (see stdio__read). What's missing
is terminal *geometry* and a place for programs to agree on it, which is
what this header adds: a size (columns/rows) with a generation counter
for polling-based resize detection, and a cooperative mode flag for
bookkeeping/introspection (`stty`).

Not permission-gated (ownership checks are used instead).

---

## bruce_tty_mode_t()

```c
typedef enum {
    BRUCE_TTY_MODE_COOKED = 0,
    BRUCE_TTY_MODE_RAW = 1,
} bruce_tty_mode_t;
```


---

## bruce_tty_size_t()

```c
typedef struct {
    uint16_t columns;
    uint16_t rows;
    /* Increments on every tty__set_size call that changes the size. Poll
     * this from a redraw loop to detect a resize without any signal --
     * BruceOS has no SIGWINCH equivalent by design (see process.h's
     * single-slot cooperative signal model), so this is the resize path. */
    uint32_t generation;
} bruce_tty_size_t;
```


---

## tty__isatty()

```c
bool tty__isatty(void);
```

True when the calling process's routed stdio session has a known terminal size.

i.e. some owner (a terminal app, the ssh client, ...) has called
tty__set_size on it at least once. False when no session is routed (the
physical console) or the session's size was never set (a plain pipe).

### Returns

`bool`


---

## tty__get_size()

```c
bruce_result_t tty__get_size(bruce_tty_size_t *out_size);
```

Reads the calling process's routed session's terminal geometry.

Returns BRUCE_ERR_NOT_FOUND if no session is routed.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `out_size` | `bruce_tty_size_t *` | Receives the terminal geometry. |

### Returns

`bruce_result_t`


---

## tty__set_size()

```c
bruce_result_t tty__set_size(bruce_stdio_session_t session, uint16_t columns, uint16_t rows);
```

Sets the terminal geometry of `session`.

Only the session's owner (the process that created it -- typically the
terminal app or the ssh client) may call this: it represents a physical
resize, not a request from a tenant process running inside the session.
Returns BRUCE_ERR_PERMISSION if the caller isn't the owner,
BRUCE_ERR_NOT_FOUND if the session doesn't exist.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `session` | `bruce_stdio_session_t` | Session to resize; caller must be its owner. |
| `columns` | `uint16_t` | New terminal width in columns. |
| `rows` | `uint16_t` | New terminal height in rows. |

### Returns

`bruce_result_t`


---

## tty__get_mode()

```c
bruce_tty_mode_t tty__get_mode(void);
```

Reads the terminal mode.

BruceOS never echoes or line-edits at the session level, so switching
modes changes no OS behavior by itself. This exists so a process can
declare its own intent (surfaced by `stty`, and as the landing spot for
any future tcsetattr()-shaped compatibility shim) and so a nested reader
can tell whether the terminal already considers itself raw. Operates on
the calling process's own routed session; no ownership check, since it's
just self-description.

### Returns

`bruce_tty_mode_t`


---

## tty__set_mode()

```c
bruce_result_t tty__set_mode(bruce_tty_mode_t mode);
```

Sets the cooperative mode of the calling process's own routed session.

Cooperative bookkeeping only -- see tty__get_mode().

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `mode` | `bruce_tty_mode_t` | New cooperative mode. |

### Returns

`bruce_result_t`


---

## tty__get_mode_of()

```c
bruce_result_t tty__get_mode_of(bruce_stdio_session_t session, bruce_tty_mode_t *out_mode);
```

Reads the mode of `session`. Caller must own the session (like tty__set_size()).

Lets an owner (terminal app, ssh client, ...) see if the program using
this session asked for raw mode, before deciding things like whether
Ctrl+C should send SIGINT or just be passed through as a normal byte.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `session` | `bruce_stdio_session_t` | Session to query; caller must be its owner. |
| `out_mode` | `bruce_tty_mode_t *` | Receives the session's mode. |

### Returns

`bruce_result_t`


---

# `wifi.h`

**Wi-Fi connections, scanning, and status.**

**Constants**

| Name | Value |
|---|---|
| `BRUCE_WIFI_SSID_MAX_LEN` | `32` |

---

## wifi__network_t()

```c
typedef struct {
    char ssid[BRUCE_WIFI_SSID_MAX_LEN + 1];
    int8_t rssi;
    uint8_t channel;
    uint8_t authmode;
} wifi__network_t;
```


---

## wifi__disconnect()

```c
bruce_result_t wifi__disconnect(void);
```

Disconnects from the current Wi-Fi network, if any.

### Returns

`bruce_result_t`


---

## wifi__connect()

```c
bruce_result_t wifi__connect(const char *ssid, const char *password, uint32_t timeout_ms);
```

Connects to a Wi-Fi network by SSID/password.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `ssid` | `const char *` | Network SSID to connect to. |
| `password` | `const char *` | Network password, or NULL/"" for an open network. |
| `timeout_ms` | `uint32_t` | Connection timeout in milliseconds. |

### Returns

`bruce_result_t`


---

## wifi__connect_known()

```c
bruce_result_t wifi__connect_known(void);
```

Connects using a previously saved Wi-Fi credential (see config.h).

### Returns

`bruce_result_t`


---

## wifi__setup_ap()

```c
bruce_result_t wifi__setup_ap(void);
```

Starts the configured Wi-Fi access point.

### Returns

`bruce_result_t`


---

## wifi__scan()

```c
int wifi__scan(wifi__network_t *networks, size_t capacity);
```

Scans for nearby Wi-Fi networks.

Returns the number of networks copied into `networks` (0 on an empty
scan) or a negative BRUCE_ERR_* value on failure. Blocks the calling
process until the scan completes or times out; a caller that wants to
animate progress or offer cancellation should use wifi__scan_start() /
wifi__scan_poll() / wifi__scan_cancel() instead.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `networks` | `wifi__network_t *` | Array to receive scanned networks. |
| `capacity` | `size_t` | Number of entries networks can hold. |

### Returns

`int`


---

## wifi__scan_start()

```c
bruce_result_t wifi__scan_start(void);
```

Starts a Wi-Fi scan without waiting for it to finish.

Only one scan runs on the radio at a time; calling this while another
caller's scan is in flight (or uncollected) joins that round instead of
restarting it, so concurrent scanners share one result set rather than
stepping on each other. Must be matched by one wifi__scan_poll() call
that doesn't return BRUCE_ERR_TIMEOUT, or one wifi__scan_cancel().

### Returns

`bruce_result_t`


---

## wifi__scan_poll()

```c
int wifi__scan_poll(wifi__network_t *networks, size_t capacity, uint32_t timeout_ms);
```

Waits up to `timeout_ms` for a scan started with wifi__scan_start() to finish, returning its results if it has.

Returns the number of networks copied into `networks` (0 on an empty
scan), BRUCE_ERR_TIMEOUT if the scan is still running when `timeout_ms`
elapses (the scan itself is unaffected -- call again to keep waiting, or
wifi__scan_cancel() to give up on it), or another negative BRUCE_ERR_*
value on failure. Calling this without a scan in flight (no prior
wifi__scan_start(), or one already collected) just waits out the timeout
and returns BRUCE_ERR_TIMEOUT.

A caller that joined the same round as another process still gets its
own copy, capped to its own `capacity`.

### Parameters

| Parameter | Type | Description |
| --- | --- | --- |
| `networks` | `wifi__network_t *` | Array to receive scanned networks. |
| `capacity` | `size_t` | Number of entries networks can hold. |
| `timeout_ms` | `uint32_t` | How long to wait for this poll before giving up. |

### Returns

`int`


---

## wifi__scan_cancel()

```c
bruce_result_t wifi__scan_cancel(void);
```

Aborts a scan started with wifi__scan_start(), if one is running.

Best-effort and safe to call with no scan in flight.

### Returns

`bruce_result_t`


---

## wifi__is_connected()

```c
bool wifi__is_connected(void);
```

Whether Wi-Fi is currently connected to a network. Returns false when unavailable or denied.

### Returns

`bool`


---

## wifi__is_ap_running()

```c
bool wifi__is_ap_running(void);
```

Whether the Wi-Fi access point is currently running. Returns false when unavailable or denied.

### Returns

`bool`


---

## wifi__get_ssid()

```c
const char *wifi__get_ssid(void);
```

Currently connected SSID.

Returns a pointer into Core-owned storage, or NULL when not currently
available; callers must not free it and should copy it before making
another wifi__* call.

### Returns

`const char *`


---

## wifi__get_ip()

```c
const char *wifi__get_ip(void);
```

Currently assigned IP address.

Returns a pointer into Core-owned storage, or NULL when not currently
available; callers must not free it and should copy it before making
another wifi__* call.

### Returns

`const char *`


---

## wifi__get_mac()

```c
const char *wifi__get_mac(void);
```

Wi-Fi station MAC address.

Returns a pointer into Core-owned storage, or NULL when not currently
available; callers must not free it and should copy it before making
another wifi__* call.

### Returns

`const char *`


---
