#pragma once

/* Core-private display declarations.  The public API is declared exactly once
 * in "core_sdk/display.h"; this header intentionally holds no additional
 * declarations because the display implementation is self-contained.
 * Built-in modules and external apps must use only core_sdk/display.h. */

#include "core_sdk/display.h"
