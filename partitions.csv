# Flash-size-independent static layout. Everything after the final static
# partition ("factory") is the "user area", owned at runtime by
# core/partition_manager. By default (no user partition table has ever been
# committed) the whole user area is registered as one LittleFS partition
# labeled "littlefs" and mounted at "/", exactly as if it were static here.
# Once a user partition table is committed (see the "partition" CLI command
# / Partition Manager app), that table - not this file - decides how the
# user area is carved up (e.g. a "swap" partition for core/memory's
# flash-backed swap allocator, additional labeled LittleFS volumes, ...).
# Name,          Type,            SubType, Offset,    Size,     Flags
bootloader,      bootloader,      primary,  N/A,      N/A,
partition_table, partition_table, primary,  N/A,      N/A,
nvs,             data,            nvs,      ,         24K,
coredump,        data,            coredump, ,         64K,
factory,         app,             factory,  ,         2560K,
